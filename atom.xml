<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>个人博客</title>
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://yoursite.com/"/>
  <updated>2017-02-10T08:28:47.412Z</updated>
  <id>http://yoursite.com/</id>
  
  <author>
    <name>Petercao</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>《JavaScript权威指南》阅读笔记</title>
    <link href="http://yoursite.com/2017/02/07/JavaScript%E6%9D%83%E5%A8%81%E6%8C%87%E5%8D%97%E7%AC%94%E8%AE%B0/"/>
    <id>http://yoursite.com/2017/02/07/JavaScript权威指南笔记/</id>
    <published>2017-02-07T02:27:12.000Z</published>
    <updated>2017-02-10T08:28:47.412Z</updated>
    
    <content type="html">&lt;h4 id=&quot;通过-访问对象属性&quot;&gt;&lt;a href=&quot;#通过-访问对象属性&quot; class=&quot;headerlink&quot; title=&quot;通过[]访问对象属性&quot;&gt;&lt;/a&gt;通过[]访问对象属性&lt;/h4&gt;&lt;p&gt;当通过点运算符(.)访问对象的属性时，属性名用一个标识符来表示，标识符必须直接出现在JavaScript程序中，他们不是数据类型，程序无法直接修改它们。  &lt;/p&gt;
&lt;p&gt;当通过[]来访问对象的属性时，属性名通过字符串来表示，字符串是JavaScript的数据类型，在程序运行时可以修改和创建它们。代码如下  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var addr = &amp;quot;&amp;quot;;
for(var i =0; i &amp;lt; 4 ; i++){
    addr += customer[&amp;quot;address&amp;quot;+i]+&amp;apos;\n&amp;apos;;
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;这段代码读取customer对象的address0,address1,address2和address3属性，并将它们连接起来。&lt;br&gt;&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;&lt;/p&gt;
&lt;h4 id=&quot;属性getter和setter&quot;&gt;&lt;a href=&quot;#属性getter和setter&quot; class=&quot;headerlink&quot; title=&quot;属性getter和setter&quot;&gt;&lt;/a&gt;属性getter和setter&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;var serialnum = {
    $n:0,
    get next(){return this.$n++;},
    set next(n){
        if(n &amp;gt;= this.$n){ this.$n = n}
        else throw &amp;quot;序列号的值不能比当前值小&amp;quot;
    }
}
serialnum.next   // ==&amp;gt;0
serialnum.next   // ==&amp;gt;1
serialnum.next = 3;
serialnum.next   // ==&amp;gt;3
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;数组方法&quot;&gt;&lt;a href=&quot;#数组方法&quot; class=&quot;headerlink&quot; title=&quot;数组方法&quot;&gt;&lt;/a&gt;数组方法&lt;/h4&gt;&lt;p&gt;ECMAScript3中的方法：&lt;/p&gt;
&lt;h5 id=&quot;join&quot;&gt;&lt;a href=&quot;#join&quot; class=&quot;headerlink&quot; title=&quot;join()&quot;&gt;&lt;/a&gt;join()&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;var a = [1,2,3];
a.join(&amp;quot; &amp;quot;)         // ==&amp;gt;&amp;quot;1 2 3&amp;quot;
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;reverse&quot;&gt;&lt;a href=&quot;#reverse&quot; class=&quot;headerlink&quot; title=&quot;reverse()&quot;&gt;&lt;/a&gt;reverse()&lt;/h5&gt;&lt;p&gt;数组倒序&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var a = [1,2,3];
a.reverse();           // =&amp;gt;a为[3,2,1]
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;sort&quot;&gt;&lt;a href=&quot;#sort&quot; class=&quot;headerlink&quot; title=&quot;sort()&quot;&gt;&lt;/a&gt;sort()&lt;/h5&gt;&lt;p&gt;数组排序&lt;br&gt;不带参数调用sort()时，数组元素以字母表顺序排序&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var a = [33,4,1111,222];
a.sort();                // 字母表顺序为1111,222,33,4
a.sort(function(a,b){   //数值顺序4,33,22,1111
    return a-b;
});
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;concat&quot;&gt;&lt;a href=&quot;#concat&quot; class=&quot;headerlink&quot; title=&quot;concat()&quot;&gt;&lt;/a&gt;concat()&lt;/h5&gt;&lt;p&gt;创建并返回一个新数组，它的元素包括调用concat()的原始数组的元素和concat()的每个参数。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var a = [1,2,3];
a.concat(4,5);            //返回[1,2,3,4,5]
a.concat([4,5]);        //返回[1,2,3,4,5]
a.concat([4,5],[6,7])    //返回[1,2,3,4,5,6,7]
a.concat(4,[5,[6,7]])    //返回[1,2,3,4,5[6,7]]
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;slice&quot;&gt;&lt;a href=&quot;#slice&quot; class=&quot;headerlink&quot; title=&quot;slice()&quot;&gt;&lt;/a&gt;slice()&lt;/h5&gt;&lt;p&gt;返回指定数组的一个片段或字数组。返回的数组包含第一个参数指定的位置和所有到但不包含第二个参数指定的位置之间的所有数组元素。且不会修改调用的数组。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var a = [1,2,3,4,5];
a.slice(0,3);            //返回[1,2,3]
a.slice(3);                //返回[4,5]
a.slice(1,-1);            //返回[2,3,4](当出现负数时,用负数加上数组的length，此处相当于a.slice(1,4))
a.slice(-3,-2);            //返回[3](相当于a.slice(2,3))
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;splice&quot;&gt;&lt;a href=&quot;#splice&quot; class=&quot;headerlink&quot; title=&quot;splice()&quot;&gt;&lt;/a&gt;splice()&lt;/h5&gt;&lt;p&gt;在数组中插入或删除元素的通用方法，且会修改调用的数组。&lt;br&gt;splice()的第一个参数指定了插入或删除的其实位置，第二个参数指定了应从数组中删除的元素个数。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var a = [1,2,3,4,5,6,7,8];
a.splice(4);        //返回[5,6,7,8];a是[1,2,3,4]
a.splice(1,2);        //返回[2,3];a是[1,4]
a.splice(1,1);        //返回[4];a是[1]    
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;splice()前两个参数之后的参数指定了需要插入到数组中的元素，从第一个参数指定的位置开始插入。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var a = [1,2,3,4,5];
a.splice(2,0,&amp;apos;a&amp;apos;,&amp;apos;&amp;apos;b);    //返回[];a是[1,2,a,b,3,4,5]
a.splice(2,2,[1,2],3);  //返回[a,b];a是[1,2,[1,2],3,3,4,5]
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;push-和pop&quot;&gt;&lt;a href=&quot;#push-和pop&quot; class=&quot;headerlink&quot; title=&quot;push()和pop()&quot;&gt;&lt;/a&gt;push()和pop()&lt;/h5&gt;&lt;p&gt;push()在数组末尾添加一个或多个元素，返回数组新的长度。pop()则相反。&lt;/p&gt;
&lt;h5 id=&quot;unshift-和shift&quot;&gt;&lt;a href=&quot;#unshift-和shift&quot; class=&quot;headerlink&quot; title=&quot;unshift()和shift()&quot;&gt;&lt;/a&gt;unshift()和shift()&lt;/h5&gt;&lt;p&gt;unshift()在数组的头部添加一个或多个元素，返回数组新的长度。shift()删除数组的第一个元素并将其返回。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var a = [];            //a:[]
a.unshift(1);        //a:[1]返回1
a.unshift(22);        //a:[22,1]返回2
a.shift();            //a:[1]返回22
a.unshift(3,[4,5]);    //a:[3,[4,5],1]返回3
a.shift();            //a:[[4,5],1]返回3
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;ECMAScript5中的方法：&lt;/p&gt;
&lt;h5 id=&quot;forEach&quot;&gt;&lt;a href=&quot;#forEach&quot; class=&quot;headerlink&quot; title=&quot;forEach()&quot;&gt;&lt;/a&gt;forEach()&lt;/h5&gt;&lt;p&gt;从头至尾偏离数组，为每个元素调用指定的函数。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var data = [1,2,3,4,5];
var sum = 0;
data.forEach(function(v){ sum += v;});//累加
sum            // ==&amp;gt;15
data.forEach(function(v,i,a){ a[i] = v+1;});//三个参数代表：数组元素、元素的索引、数组本身
data         //==&amp;gt;[2,3,4,5,6]
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;map&quot;&gt;&lt;a href=&quot;#map&quot; class=&quot;headerlink&quot; title=&quot;map()&quot;&gt;&lt;/a&gt;map()&lt;/h5&gt;&lt;p&gt;调用数组的每个元素传递给指定的函数，并返回一个数组。不修改调用的数组。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var a = [1,2,3];
var b = a.map(function(x){return x*x;});//b是[1,4,9]
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;filter&quot;&gt;&lt;a href=&quot;#filter&quot; class=&quot;headerlink&quot; title=&quot;filter()&quot;&gt;&lt;/a&gt;filter()&lt;/h5&gt;&lt;p&gt;filter()方法返回的数组元素是调用的数组的一个子集。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var a = [5,4,3,2,1];
smallvalues = a.filter(function(x){return x &amp;lt; 3;});//返回[2,1]
other = a.filter(function(x,i){return i%2 == 0}); //返回[5,3,1]
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;every-和some&quot;&gt;&lt;a href=&quot;#every-和some&quot; class=&quot;headerlink&quot; title=&quot;every()和some()&quot;&gt;&lt;/a&gt;every()和some()&lt;/h5&gt;&lt;p&gt;every():当且仅当数组中的所有元素调用判定函数都返回true时，返回true&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;a = [1,2,3,4,5];
a.every(function(x){ return x &amp;lt; 10;});//返回true
a.every(function(x){ return x%2 ===0; })//返回false
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;some():只要有一个元素调用判定函数返回true，则返回true&lt;/p&gt;
&lt;h5 id=&quot;reduce-和reduceRight&quot;&gt;&lt;a href=&quot;#reduce-和reduceRight&quot; class=&quot;headerlink&quot; title=&quot;reduce()和reduceRight()&quot;&gt;&lt;/a&gt;reduce()和reduceRight()&lt;/h5&gt;&lt;p&gt;使用指定的函数将数组元素进行组合，生成单个值。&lt;/p&gt;
&lt;h5 id=&quot;indexOf-和lastIndexOf&quot;&gt;&lt;a href=&quot;#indexOf-和lastIndexOf&quot; class=&quot;headerlink&quot; title=&quot;indexOf()和lastIndexOf()&quot;&gt;&lt;/a&gt;indexOf()和lastIndexOf()&lt;/h5&gt;&lt;p&gt;搜索整个数组中具有给定值的元素，返回找到的第一个元素的索引，若没找到返回-1&lt;/p&gt;
&lt;h5 id=&quot;Array-isArray&quot;&gt;&lt;a href=&quot;#Array-isArray&quot; class=&quot;headerlink&quot; title=&quot;Array.isArray()&quot;&gt;&lt;/a&gt;Array.isArray()&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;Array.isArray([])      //=&amp;gt;true
Array.isArray({})    //=&amp;gt;false
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;用闭包模拟私有方法&quot;&gt;&lt;a href=&quot;#用闭包模拟私有方法&quot; class=&quot;headerlink&quot; title=&quot;用闭包模拟私有方法&quot;&gt;&lt;/a&gt;用闭包模拟私有方法&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;var Counter = (function() {
    var privateCounter = 0;
    function changeBy(val) {
        privateCounter += val;
    }
    return {
        increment: function() {
          changeBy(1);
        },
        decrement: function() {
          changeBy(-1);
        },
        value: function() {
          return privateCounter;
        }
    }   
})();
console.log(Counter.value()); // logs 0 
Counter.increment();
Counter.increment();
console.log(Counter.value()); // logs 2 
Counter.decrement();
console.log(Counter.value()); // logs 1 
var Counter1 = makeCounter();
var Counter2 = makeCounter();
console.log(Counter1.value()); /* logs 0 */
Counter1.increment();
Counter1.increment();
console.log(Counter1.value()); /* logs 2 */
Counter1.decrement();
console.log(Counter1.value()); /* logs 1 */
console.log(Counter2.value()); /* logs 0 */
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Counter创建于一个匿名函数体内，该函数一经定义立刻执行。环境中包含两个私有项：名为 privateCounter 的变量和名为 changeBy 的函数。 这两项都无法在匿名函数外部直接访问。必须通过匿名包装器返回的三个公共函数(increment、decrement、value)访问。&lt;br&gt;每次调用 makeCounter() 函数期间，其环境是不同的。每次调用中， privateCounter 中含有不同的实例。&lt;/p&gt;
</content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;通过-访问对象属性&quot;&gt;&lt;a href=&quot;#通过-访问对象属性&quot; class=&quot;headerlink&quot; title=&quot;通过[]访问对象属性&quot;&gt;&lt;/a&gt;通过[]访问对象属性&lt;/h4&gt;&lt;p&gt;当通过点运算符(.)访问对象的属性时，属性名用一个标识符来表示，标识符必须直接出现在JavaScript程序中，他们不是数据类型，程序无法直接修改它们。  &lt;/p&gt;
&lt;p&gt;当通过[]来访问对象的属性时，属性名通过字符串来表示，字符串是JavaScript的数据类型，在程序运行时可以修改和创建它们。代码如下  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var addr = &amp;quot;&amp;quot;;
for(var i =0; i &amp;lt; 4 ; i++){
    addr += customer[&amp;quot;address&amp;quot;+i]+&amp;apos;\n&amp;apos;;
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;这段代码读取customer对象的address0,address1,address2和address3属性，并将它们连接起来。&lt;br&gt;
    
    </summary>
    
    
      <category term="JavaScript" scheme="http://yoursite.com/tags/JavaScript/"/>
    
  </entry>
  
  <entry>
    <title>前端测试相关</title>
    <link href="http://yoursite.com/2016/12/09/%E6%B5%8B%E8%AF%95%E7%9B%B8%E5%85%B3/"/>
    <id>http://yoursite.com/2016/12/09/测试相关/</id>
    <published>2016-12-09T07:52:34.000Z</published>
    <updated>2016-12-09T08:56:11.394Z</updated>
    
    <content type="html">&lt;h3 id=&quot;Mocha&quot;&gt;&lt;a href=&quot;#Mocha&quot; class=&quot;headerlink&quot; title=&quot;Mocha&quot;&gt;&lt;/a&gt;Mocha&lt;/h3&gt;&lt;p&gt;Mocha是一个功能丰富的JavaScript测试框架，在Node.js和浏览器上运行，使异步测试简单而有趣.&lt;br&gt;&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;&lt;/p&gt;
&lt;h4 id=&quot;安装&quot;&gt;&lt;a href=&quot;#安装&quot; class=&quot;headerlink&quot; title=&quot;安装&quot;&gt;&lt;/a&gt;安装&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;npm install --global mocha
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;使用入门&quot;&gt;&lt;a href=&quot;#使用入门&quot; class=&quot;headerlink&quot; title=&quot;使用入门&quot;&gt;&lt;/a&gt;使用入门&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;var assert = require(&amp;apos;assert&amp;apos;);
describe(&amp;apos;Array&amp;apos;, function() {
  describe(&amp;apos;#indexOf()&amp;apos;, function() {
    it(&amp;apos;should return -1 when the value is not present&amp;apos;, function() {
      assert.equal(-1, [1,2,3].indexOf(4));
    });
  });
});
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;执行&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;mocha test.js
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;输出  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Array
    #indexOf()
      â should return -1 when the value is not present


  1 passing (7ms)
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;参考：  &lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;http://mochajs.org/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;http://mochajs.org/&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;http://www.ruanyifeng.com/blog/2015/12/a-mocha-tutorial-of-examples.html&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;http://www.ruanyifeng.com/blog/2015/12/a-mocha-tutorial-of-examples.html&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;JSHint&quot;&gt;&lt;a href=&quot;#JSHint&quot; class=&quot;headerlink&quot; title=&quot;JSHint&quot;&gt;&lt;/a&gt;JSHint&lt;/h3&gt;&lt;p&gt;JSHint是一个JavaScript代码质量检测工具&lt;/p&gt;
&lt;h4 id=&quot;安装-1&quot;&gt;&lt;a href=&quot;#安装-1&quot; class=&quot;headerlink&quot; title=&quot;安装&quot;&gt;&lt;/a&gt;安装&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;npm install -g jshint
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;使用&quot;&gt;&lt;a href=&quot;#使用&quot; class=&quot;headerlink&quot; title=&quot;使用&quot;&gt;&lt;/a&gt;使用&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;jshint test.js
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;例如，如果test.js中含有如下代码&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;if( app.thing == null ) console.log( &amp;apos;bleat!&amp;apos; );
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;会提示应该使用’===’而不是’==’&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Use &amp;apos;===&amp;apos; to compare with &amp;apos;null&amp;apos;.
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;编辑器集成&quot;&gt;&lt;a href=&quot;#编辑器集成&quot; class=&quot;headerlink&quot; title=&quot;编辑器集成&quot;&gt;&lt;/a&gt;编辑器集成&lt;/h4&gt;&lt;p&gt;jshint提供了很多插件来集成到编辑器中&lt;br&gt;参考：&lt;a href=&quot;http://jshint.com/install/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;http://jshint.com/install/&lt;/a&gt;&lt;/p&gt;
&lt;h3 id=&quot;GRUNT&quot;&gt;&lt;a href=&quot;#GRUNT&quot; class=&quot;headerlink&quot; title=&quot;GRUNT&quot;&gt;&lt;/a&gt;GRUNT&lt;/h3&gt;&lt;p&gt;参考:&lt;a href=&quot;http://www.gruntjs.net/getting-started&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;http://www.gruntjs.net/getting-started&lt;/a&gt;&lt;/p&gt;
</content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;Mocha&quot;&gt;&lt;a href=&quot;#Mocha&quot; class=&quot;headerlink&quot; title=&quot;Mocha&quot;&gt;&lt;/a&gt;Mocha&lt;/h3&gt;&lt;p&gt;Mocha是一个功能丰富的JavaScript测试框架，在Node.js和浏览器上运行，使异步测试简单而有趣.&lt;br&gt;
    
    </summary>
    
      <category term="nodejs" scheme="http://yoursite.com/categories/nodejs/"/>
    
    
      <category term="nodejs" scheme="http://yoursite.com/tags/nodejs/"/>
    
  </entry>
  
  <entry>
    <title>Promise入门</title>
    <link href="http://yoursite.com/2016/12/08/Promise/"/>
    <id>http://yoursite.com/2016/12/08/Promise/</id>
    <published>2016-12-08T09:01:52.000Z</published>
    <updated>2016-12-09T02:02:13.469Z</updated>
    
    <content type="html">&lt;h4 id=&quot;什么是Promise&quot;&gt;&lt;a href=&quot;#什么是Promise&quot; class=&quot;headerlink&quot; title=&quot;什么是Promise&quot;&gt;&lt;/a&gt;什么是Promise&lt;/h4&gt;&lt;p&gt;Promise是抽象异步处理对象以及对其进行各种操作的组件&lt;br&gt;&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;&lt;/p&gt;
&lt;h4 id=&quot;为什么要用Promise&quot;&gt;&lt;a href=&quot;#为什么要用Promise&quot; class=&quot;headerlink&quot; title=&quot;为什么要用Promise&quot;&gt;&lt;/a&gt;为什么要用Promise&lt;/h4&gt;&lt;p&gt;常用的异步处理方式（使用回调函数）：    &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;getAsync(&amp;quot;fileA.txt&amp;quot;, function(error, result){
    if(error){// 取得失败时的处理
        throw error;
    }
    // 取得成功时的处理
});
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;在业务逻辑复杂时，容易出现回调地狱，不易于阅读维护&lt;br&gt;使用Promise进行异步处理：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var promise = getAsyncPromise(&amp;quot;fileA.txt&amp;quot;); 
promise.then(function(result){
    // 获取文件内容成功时的处理
}).catch(function(error){
    // 获取文件内容失败时的处理
});
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;当逻辑复杂时，代码类似如下：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var aPromise = Promise.resolve();
aPromise.then(function taskA(value){
// task A
}).then(function taskB(vaue){
// task B
}).catch(function onRejected(error){
    console.log(error);
});
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;以方法链的形式编写异步代码，便于阅读维护。        &lt;/p&gt;
&lt;h4 id=&quot;编写Promise代码&quot;&gt;&lt;a href=&quot;#编写Promise代码&quot; class=&quot;headerlink&quot; title=&quot;编写Promise代码&quot;&gt;&lt;/a&gt;编写Promise代码&lt;/h4&gt;&lt;p&gt;创建promise对象的流程如下所示：  &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;new Promise(fn) 返回一个promise对象&lt;/li&gt;
&lt;li&gt;&lt;p&gt;在fn 中指定异步等处理&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;处理结果正常的话，调用resolve(处理结果值)&lt;/li&gt;
&lt;li&gt;处理结果错误的话，调用reject(Error对象)&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;例子：  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;function getURL(URL) {
    return new Promise(function (resolve, reject) {
        var req = new XMLHttpRequest();
        req.open(&amp;apos;GET&amp;apos;, URL, true);
        req.onload = function () {
            if (req.status === 200) {
                resolve(req.responseText);
            } else {
                reject(new Error(req.statusText));
            }
        };
        req.onerror = function () {
            reject(new Error(req.statusText));
        };
        req.send();
    });
}
// 运行示例
var URL = &amp;quot;http://httpbin.org/get&amp;quot;;
getURL(URL).then(function onFulfilled(value){
    console.log(value);
}).catch(function onRejected(error){
    console.error(error);
});
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;Promise-resolve-与-Promise-reject&quot;&gt;&lt;a href=&quot;#Promise-resolve-与-Promise-reject&quot; class=&quot;headerlink&quot; title=&quot;Promise.resolve 与 Promise.reject&quot;&gt;&lt;/a&gt;Promise.resolve 与 Promise.reject&lt;/h4&gt;&lt;p&gt;静态方法Promise.resolve(value) 和 Promise.resolve(value)是 new Promise() 方法的快捷方式。&lt;br&gt;Promise.resolve(42);等同于  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;new Promise(function(resolve){
    resolve(42);
});
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Promise.reject(new Error(“出错了”))等同于&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;new Promise(function(resolve,reject){
    reject(new Error(&amp;quot;出错了&amp;quot;));
});
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;Promise-all与Promise-race&quot;&gt;&lt;a href=&quot;#Promise-all与Promise-race&quot; class=&quot;headerlink&quot; title=&quot;Promise.all与Promise.race&quot;&gt;&lt;/a&gt;Promise.all与Promise.race&lt;/h4&gt;&lt;p&gt;Promise.all 接收一个 promise对象的数组作为参数，数组中的方法同时执行，且当这个数组里的所有promise对象全部变为resolve或reject状态的时候，它才会去调用 .then 方法。&lt;br&gt;使用场景：&lt;br&gt;    同时发送两个请求，两个请求完成后，处理结果 &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var request = {
    comment: function getComment() {
        return getURL(url1).then(JSON.parse);
    },
    people: function getPeople() {
        return getURL(url2).then(JSON.parse);
    }
};
Promise.all([request.comment(), request.people()])
.then(function (results) {
    console.log(results); // 按照[comment, people]的顺序
}).catch(function(error){
    console.log(error);
});
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;在上面的代码中，request.comment() 和 request.people() 会同时开始执行，而且每个promise的结果（resolve或reject时传递的参数值），和传递给 Promise.all 的promise数组的顺序是一致的。&lt;/p&gt;
&lt;p&gt;Promise.race 与Promise.all类似，但只要有一个promise对象进入 FulFilled 或者 Rejected 状态的话，就会继续进行后面的处理。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// `delay`毫秒后执行resolve
function timerPromisefy(delay) {
    return new Promise(function (resolve) {
        setTimeout(function () {
            resolve(delay);
        }, delay);
    });
}
// 任何一个promise变为resolve或reject 的话程序就停止运行
Promise.race([
    timerPromisefy(1),
    timerPromisefy(32),
    timerPromisefy(64),
    timerPromisefy(128)
]).then(function (value) {
    console.log(value);    // =&amp;gt; 1
});
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Promise.race 在第一个promise对象变为Fulfilled之后，并不会取消其他promise对象的执行&lt;/p&gt;
&lt;p&gt;参考链接：&lt;a href=&quot;http://liubin.org/promises-book/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;http://liubin.org/promises-book/&lt;/a&gt;&lt;/p&gt;
</content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;什么是Promise&quot;&gt;&lt;a href=&quot;#什么是Promise&quot; class=&quot;headerlink&quot; title=&quot;什么是Promise&quot;&gt;&lt;/a&gt;什么是Promise&lt;/h4&gt;&lt;p&gt;Promise是抽象异步处理对象以及对其进行各种操作的组件&lt;br&gt;
    
    </summary>
    
      <category term="JavaScript" scheme="http://yoursite.com/categories/JavaScript/"/>
    
    
      <category term="nodejs JavaScript" scheme="http://yoursite.com/tags/nodejs-JavaScript/"/>
    
  </entry>
  
  <entry>
    <title>Nodejs命令整理</title>
    <link href="http://yoursite.com/2016/12/08/Nodejs%E5%91%BD%E4%BB%A4%E6%95%B4%E7%90%86/"/>
    <id>http://yoursite.com/2016/12/08/Nodejs命令整理/</id>
    <published>2016-12-08T01:27:44.000Z</published>
    <updated>2016-12-12T06:19:52.089Z</updated>
    
    <content type="html">&lt;h4 id=&quot;开启关闭MongoDB&quot;&gt;&lt;a href=&quot;#开启关闭MongoDB&quot; class=&quot;headerlink&quot; title=&quot;开启关闭MongoDB&quot;&gt;&lt;/a&gt;开启关闭MongoDB&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;net start MongoDB
net stop MongoDB
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;自动重启&quot;&gt;&lt;a href=&quot;#自动重启&quot; class=&quot;headerlink&quot; title=&quot;自动重启&quot;&gt;&lt;/a&gt;自动重启&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;supervisor --harmony index
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;404：请求的网页不存在&lt;br&gt;500：服务器遇到错误,无法完成请求&lt;/p&gt;
</content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;开启关闭MongoDB&quot;&gt;&lt;a href=&quot;#开启关闭MongoDB&quot; class=&quot;headerlink&quot; title=&quot;开启关闭MongoDB&quot;&gt;&lt;/a&gt;开启关闭MongoDB&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;net start MongoDB
net stop
    
    </summary>
    
      <category term="常用" scheme="http://yoursite.com/categories/%E5%B8%B8%E7%94%A8/"/>
    
    
      <category term="nodeJs" scheme="http://yoursite.com/tags/nodeJs/"/>
    
  </entry>
  
  <entry>
    <title>MVP入门</title>
    <link href="http://yoursite.com/2016/11/04/MVP%E5%85%A5%E9%97%A8/"/>
    <id>http://yoursite.com/2016/11/04/MVP入门/</id>
    <published>2016-11-04T09:21:30.000Z</published>
    <updated>2016-11-04T09:24:39.435Z</updated>
    
    <content type="html">&lt;h4 id=&quot;MVP是什么&quot;&gt;&lt;a href=&quot;#MVP是什么&quot; class=&quot;headerlink&quot; title=&quot;MVP是什么&quot;&gt;&lt;/a&gt;MVP是什么&lt;/h4&gt;&lt;p&gt;MVP 是从经典的模式MVC演变而来，它们的基本思想有相通的地方：Controller/Presenter负责逻辑的处理，Model提供数据，View负责显示。作为一种新的模式，MVP与MVC有着一个重大的区别：在MVP中View并不直接使用Model，它们之间的通信是通过Presenter (MVC中的Controller)来进行的，所有的交互都发生在Presenter内部，而在MVC中View会从直接Model中读取数据而不是通过 Controller。&lt;br&gt;&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;&lt;/p&gt;
&lt;h4 id=&quot;MVP的优点&quot;&gt;&lt;a href=&quot;#MVP的优点&quot; class=&quot;headerlink&quot; title=&quot;MVP的优点&quot;&gt;&lt;/a&gt;MVP的优点&lt;/h4&gt;&lt;ol&gt;
&lt;li&gt;模型与视图完全分离，我们可以修改视图而不影响模型&lt;/li&gt;
&lt;li&gt;可以更高效地使用模型，因为所有的交互都发生在一个地方——Presenter内部&lt;/li&gt;
&lt;li&gt;我们可以将一个Presenter用于多个视图，而不需要改变Presenter的逻辑。这个特性非常的有用，因为视图的变化总是比模型的变化频繁。&lt;/li&gt;
&lt;li&gt;如果我们把逻辑放在Presenter中，那么我们就可以脱离用户接口来测试这些逻辑（单元测试）  &lt;/li&gt;
&lt;/ol&gt;
&lt;h4 id=&quot;Demo解析&quot;&gt;&lt;a href=&quot;#Demo解析&quot; class=&quot;headerlink&quot; title=&quot;Demo解析&quot;&gt;&lt;/a&gt;Demo解析&lt;/h4&gt;&lt;p&gt;Demo地址：&lt;a href=&quot;https://github.com/Peter-cao/MvpDemo&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://github.com/Peter-cao/MvpDemo&lt;/a&gt; &lt;/p&gt;
&lt;h5 id=&quot;基类BasePresenter&quot;&gt;&lt;a href=&quot;#基类BasePresenter&quot; class=&quot;headerlink&quot; title=&quot;基类BasePresenter&quot;&gt;&lt;/a&gt;基类BasePresenter&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;public interface BasePresenter {
    void start();
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;BasePresenter中含有方法start(),该方法的作用是presenter开始获取数据并调用view中方法改变界面显示，其调用时机是在activity类的onResume方法中。&lt;/p&gt;
&lt;h5 id=&quot;契约类MainContract&quot;&gt;&lt;a href=&quot;#契约类MainContract&quot; class=&quot;headerlink&quot; title=&quot;契约类MainContract&quot;&gt;&lt;/a&gt;契约类MainContract&lt;/h5&gt;&lt;p&gt;统一管理view与presenter的所有的接口，使得view与presenter中有哪些功能，一目了然，维护起来也方便&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public interface MainContract {
    interface View  {
        void showLoading();//显示loading
        void hideLoading();//隐藏loading
        void showList(List&amp;lt;Topic&amp;gt; list);//显示列表
        void showError(String msg);//显示错误信息

    }
    interface Presenter extends BasePresenter {
        void showList();//发送列表请求
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;MainActivity&quot;&gt;&lt;a href=&quot;#MainActivity&quot; class=&quot;headerlink&quot; title=&quot;MainActivity&quot;&gt;&lt;/a&gt;MainActivity&lt;/h5&gt;&lt;p&gt;实现了MainContract.View接口，MainActivity包含了MainPresenter的引用，当用户执行动作时, View截获事件, 通过Presenter接口传递事件给Presenter处理.&lt;br&gt;在onResume()方法中调用mPresenter的start()方法&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public class MainActivity extends AppCompatActivity implements MainContract.View {
    private SwipeRefreshLayout mSwipeRefreshLayout;
    private ListView mListView;
    private MainContract.Presenter mPresenter;
    private SimpleAdapter mSimpleAdapter;
    private List&amp;lt;Map&amp;lt;String, Object&amp;gt;&amp;gt; listData = new ArrayList&amp;lt;&amp;gt;();
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
        mSwipeRefreshLayout = (SwipeRefreshLayout)findViewById(R.id.activity_main);
        mListView = (ListView)findViewById(R.id.listview);
        mSimpleAdapter = new SimpleAdapter(this,listData,android.R.layout.simple_list_item_2,new String[] { &amp;quot;title&amp;quot; },new int[]{android.R.id.text1});
        mListView.setAdapter(mSimpleAdapter);
        mPresenter = new MainPresenter(this);
    }


    @Override
    protected void onResume() {
        super.onResume();
        mPresenter.start();
    }

    @Override
    public void showLoading() {
        Handler handler = new Handler();
        handler.post(new Runnable() {
            @Override
            public void run() {
                mSwipeRefreshLayout.setRefreshing(true);
            }
        });

    }

    @Override
    public void hideLoading() {
        mSwipeRefreshLayout.setRefreshing(false);
    }

    @Override
    public void showList(List&amp;lt;Topic&amp;gt; list) {
        for(Topic topic : list){
            HashMap map = new HashMap();
            map.put(&amp;quot;title&amp;quot;,topic.getTitle());
            listData.add(map);
        }
        mSimpleAdapter.notifyDataSetChanged();
    }

    @Override
    public void showError(String msg) {
        Toast.makeText(this,msg,Toast.LENGTH_SHORT).show();
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;MainPresenter&quot;&gt;&lt;a href=&quot;#MainPresenter&quot; class=&quot;headerlink&quot; title=&quot;MainPresenter&quot;&gt;&lt;/a&gt;MainPresenter&lt;/h5&gt;&lt;p&gt;实现了MainContract.Presenter接口，MainPresenter包含了MainContract.View的引用mMainActivity，在View需要更新时，调用mMainActivity的对应方法&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public class MainPresenter implements MainContract.Presenter {
    private MainContract.View mMainActivity;

    public MainPresenter(MainContract.View mainActivity) {
        mMainActivity = mainActivity;
    }

    @Override
    public void start() {
        showList();
    }
    @Override
    public void showList(){
        mMainActivity.showLoading();
        Retrofit retrofit = new Retrofit.Builder()
                .baseUrl(&amp;quot;https://cnodejs.org/api/v1/&amp;quot;)
                .addConverterFactory(GsonConverterFactory.create())
                .build();
        ApiService service = retrofit.create(ApiService.class);
        Call&amp;lt;TopicRsp&amp;gt; call = service.listRepos();
        call.enqueue(new Callback&amp;lt;TopicRsp&amp;gt;() {
            @Override
            public void onResponse(Call&amp;lt;TopicRsp&amp;gt; call, Response&amp;lt;TopicRsp&amp;gt; response) {
                mMainActivity.hideLoading();
                if(response.body().isSuccess()){
                    mMainActivity.showList(response.body().getData());
                }else{
                    mMainActivity.showError(&amp;quot;接口异常&amp;quot;);
                }
            }

            @Override
            public void onFailure(Call&amp;lt;TopicRsp&amp;gt; call, Throwable t) {
                mMainActivity.hideLoading();
                mMainActivity.showError(&amp;quot;网络异常&amp;quot;);
            }
        });

    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;参考：&lt;a href=&quot;https://github.com/googlesamples/android-architecture&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://github.com/googlesamples/android-architecture&lt;/a&gt;&lt;/p&gt;
</content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;MVP是什么&quot;&gt;&lt;a href=&quot;#MVP是什么&quot; class=&quot;headerlink&quot; title=&quot;MVP是什么&quot;&gt;&lt;/a&gt;MVP是什么&lt;/h4&gt;&lt;p&gt;MVP 是从经典的模式MVC演变而来，它们的基本思想有相通的地方：Controller/Presenter负责逻辑的处理，Model提供数据，View负责显示。作为一种新的模式，MVP与MVC有着一个重大的区别：在MVP中View并不直接使用Model，它们之间的通信是通过Presenter (MVC中的Controller)来进行的，所有的交互都发生在Presenter内部，而在MVC中View会从直接Model中读取数据而不是通过 Controller。&lt;br&gt;
    
    </summary>
    
    
      <category term="android mvp" scheme="http://yoursite.com/tags/android-mvp/"/>
    
  </entry>
  
  <entry>
    <title>CNode-android笔记</title>
    <link href="http://yoursite.com/2016/10/21/CNode-android%E7%AC%94%E8%AE%B0/"/>
    <id>http://yoursite.com/2016/10/21/CNode-android笔记/</id>
    <published>2016-10-21T01:12:13.000Z</published>
    <updated>2016-11-22T07:01:15.439Z</updated>
    
    <content type="html">&lt;p&gt;本文为开发CNode-android的笔记，供以后开发参考&lt;br&gt;&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;&lt;/p&gt;
&lt;h4 id=&quot;主页列表显示&quot;&gt;&lt;a href=&quot;#主页列表显示&quot; class=&quot;headerlink&quot; title=&quot;主页列表显示&quot;&gt;&lt;/a&gt;主页列表显示&lt;/h4&gt;&lt;p&gt;ListView的使用&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;private List&amp;lt;Map&amp;lt;String, Object&amp;gt;&amp;gt;  dataList = new ArrayList&amp;lt;&amp;gt;();
 topicsList = (ListView) findViewById(R.id.topicsList);
topicsAdapter = new SimpleAdapter(this,dataList,R.layout.topic_item,new String[]{&amp;quot;title&amp;quot;,&amp;quot;loginname&amp;quot;,&amp;quot;reply_count&amp;quot;,&amp;quot;visit_count&amp;quot;,&amp;quot;last_reply_at&amp;quot;,&amp;quot;getCreate_at&amp;quot;}
        ,new int[]{R.id.topicLitle,R.id.author_name,R.id.reply_count,R.id.visit_count,R.id.last_reply,R.id.create_at});
topicsList.setAdapter(topicsAdapter);
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Retrofit的使用  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;compile &amp;apos;com.squareup.retrofit2:retrofit:2.1.0&amp;apos;
compile &amp;apos;com.squareup.retrofit2:converter-gson:2.1.0&amp;apos;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;com.squareup.retrofit2:converter-gson:2.1.0不能漏掉，否则会报错，也可以用其他库，参考&lt;a href=&quot;http://square.github.io/retrofit/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;Retrofit官网&lt;/a&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Retrofit retrofit = new Retrofit.Builder()
.baseUrl(&amp;quot;https://api.github.com&amp;quot;)
.addConverterFactory(GsonConverterFactory.create())
.build();
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;调用的时候不能漏掉&lt;code&gt;.addConverterFactory(GsonConverterFactory.create())&lt;/code&gt;&lt;br&gt;“2016-10-12T05:36:31.914Z”类似字符串转化为Date方法:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;SimpleDateFormat sdf = new SimpleDateFormat(&amp;quot;yyyy-MM-dd&amp;apos;T&amp;apos;HH:mm:ss.SSS&amp;apos;Z&amp;apos;&amp;quot;);
Date last_reply_date = sdf.parse(last_reply_at);
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;列表图片加载&quot;&gt;&lt;a href=&quot;#列表图片加载&quot; class=&quot;headerlink&quot; title=&quot;列表图片加载&quot;&gt;&lt;/a&gt;列表图片加载&lt;/h4&gt;&lt;p&gt;新建TopicListAdapter代替SimpleAdapter，TopicListAdapter继承BaseAdapter，主要重写getView方法，在getView中使用viewHolder，使listview滑动更加顺畅&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;@Override
public View getView(int position, View convertView, ViewGroup parent) {
    ViewHolder viewHolder;
    if (convertView == null) {
        viewHolder = new ViewHolder();
        convertView = mInflater.inflate(R.layout.topic_item, null);
        viewHolder.author_image = (ImageView) convertView.findViewById(R.id.author_image);
        viewHolder.author_name = (TextView) convertView.findViewById(R.id.author_name);
        viewHolder.top = (LinearLayout) convertView.findViewById(R.id.top);
        viewHolder.good = (LinearLayout) convertView.findViewById(R.id.good);
        viewHolder.last_reply = (TextView) convertView.findViewById(R.id.last_reply);
        viewHolder.topicLitle = (TextView) convertView.findViewById(R.id.topicLitle);
        viewHolder.create_at = (TextView) convertView.findViewById(R.id.create_at);
        viewHolder.visit_count = (TextView) convertView.findViewById(R.id.visit_count);
        viewHolder.reply_count = (TextView) convertView.findViewById(R.id.reply_count);
        convertView.setTag(viewHolder);
    } else {
        viewHolder = (ViewHolder) convertView.getTag();
    }
    Topic topic = dataList.get(position);
    viewHolder.author_name.setText(topic.getAuthor().getLoginname());
    viewHolder.top.setVisibility(topic.isTop() ? View.VISIBLE : View.GONE);
    viewHolder.good.setVisibility(topic.isGood() ? View.VISIBLE : View.GONE);
    viewHolder.last_reply.setText(Utils.getDateDiff(topic.getLast_reply_at()));
    viewHolder.topicLitle.setText(topic.getTitle());
    viewHolder.create_at.setText(Utils.formatDate(topic.getCreate_at()));
    viewHolder.visit_count.setText(topic.getVisit_count() + &amp;quot;&amp;quot;);
    viewHolder.reply_count.setText(topic.getReply_count() + &amp;quot;&amp;quot;);

    viewHolder.author_image.setImageResource(R.mipmap.ic_launcher);

    String authorImageUrl = topic.getAuthor().getAvatar_url().contains(&amp;quot;http&amp;quot;) ? topic.getAuthor().getAvatar_url() : &amp;quot;https:&amp;quot; + topic.getAuthor().getAvatar_url();
    viewHolder.author_image.setTag(authorImageUrl);
    showImageByAsyncTask(authorImageUrl, viewHolder.author_image);
    return convertView;
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;使用AsyncTask实现图片的异步加载&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public class AuthorImageLoader extends AsyncTask &amp;lt;String,Void,Bitmap&amp;gt;{
    ...
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;AsyncTask是抽象类.AsyncTask定义了三种泛型类型 Params，Progress和Result。&lt;br&gt;　　1.Params 启动任务执行的输入参数，比如HTTP请求的URL。&lt;br&gt;　　2.Progress 后台任务执行的百分比。&lt;br&gt;　　3.Result 后台执行任务最终返回的结果，比如String。&lt;br&gt;主要重写doInBackground和onPostExecute方法&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;@Override
protected Bitmap doInBackground(String... params) {
    String url = params[0];
    Bitmap bitmap = getBitmapFromUrl(params[0]);
    return bitmap;
}

@Override
protected void onPostExecute(Bitmap bitmap) {
    super.onPostExecute(bitmap);
    if(mImageView.getTag().equals(mUrl)){
        mImageView.setImageBitmap(bitmap);
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;通过url获取bitmap&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public Bitmap getBitmapFromUrl(String url){
    Bitmap bitmap;
    InputStream is = null;
    try{
        URL mUrl = new URL(url);
        HttpURLConnection mHttpURLConnection = (HttpURLConnection) mUrl.openConnection();
        is = new BufferedInputStream(mHttpURLConnection.getInputStream());
        bitmap = BitmapFactory.decodeStream(is);
        mHttpURLConnection.disconnect();
        return  bitmap;
    }catch (Exception e){
        e.printStackTrace();
    }finally {
        try {
            is.close();
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
    return null;
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;参考：&lt;a href=&quot;http://blog.csdn.net/dmk877/article/details/49366421&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;http://blog.csdn.net/dmk877/article/details/49366421&lt;/a&gt;&lt;/p&gt;
&lt;h4 id=&quot;上拉加载-下拉刷新&quot;&gt;&lt;a href=&quot;#上拉加载-下拉刷新&quot; class=&quot;headerlink&quot; title=&quot;上拉加载  下拉刷新&quot;&gt;&lt;/a&gt;上拉加载  下拉刷新&lt;/h4&gt;&lt;p&gt;下拉刷新，使用SwipeRefreshLayout，布局文件如下&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;?xml version=&amp;quot;1.0&amp;quot; encoding=&amp;quot;utf-8&amp;quot;?&amp;gt;
&amp;lt;android.support.v4.widget.SwipeRefreshLayout xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;
    xmlns:app=&amp;quot;http://schemas.android.com/apk/res-auto&amp;quot;
    xmlns:tools=&amp;quot;http://schemas.android.com/tools&amp;quot;
    android:id=&amp;quot;@+id/content_main&amp;quot;
    android:layout_width=&amp;quot;match_parent&amp;quot;
    android:layout_height=&amp;quot;match_parent&amp;quot;
    app:layout_behavior=&amp;quot;@string/appbar_scrolling_view_behavior&amp;quot;
    tools:context=&amp;quot;com.demo.cnode_android.MainActivity&amp;quot;
    tools:showIn=&amp;quot;@layout/app_bar_main&amp;quot;&amp;gt;


    &amp;lt;ListView
        android:id=&amp;quot;@+id/topicsList&amp;quot;
        android:layout_width=&amp;quot;match_parent&amp;quot;
        android:layout_height=&amp;quot;match_parent&amp;quot;
        android:layout_alignParentEnd=&amp;quot;true&amp;quot;
        android:layout_alignParentTop=&amp;quot;true&amp;quot; /&amp;gt;
&amp;lt;/android.support.v4.widget.SwipeRefreshLayout&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;主要方法&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;setOnRefreshListener(OnRefreshListener): 为布局添加一个Listener
setRefreshing(boolean): 显示或隐藏刷新进度条
isRefreshing(): 检查是否处于刷新状态
setColorScheme(): 设置进度条的颜色主题，最多能设置四种
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;上拉加载，为ListView添加setOnScrollListener，判断是否滑倒了底部&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;topicsList.setOnScrollListener(new AbsListView.OnScrollListener() {
    @Override
    public void onScrollStateChanged(AbsListView view, int scrollState) {
       if(scrollState == AbsListView.OnScrollListener.SCROLL_STATE_IDLE){
            if(view.getLastVisiblePosition() == view.getCount()-1){
                page++;
                getList(false);
            }
       }

    }

    @Override
    public void onScroll(AbsListView view, int firstVisibleItem, int visibleItemCount, int totalItemCount) {

    }
});
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;导航抽屉NavigationView&quot;&gt;&lt;a href=&quot;#导航抽屉NavigationView&quot; class=&quot;headerlink&quot; title=&quot;导航抽屉NavigationView&quot;&gt;&lt;/a&gt;导航抽屉NavigationView&lt;/h4&gt;&lt;p&gt;布局：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;?xml version=&amp;quot;1.0&amp;quot; encoding=&amp;quot;utf-8&amp;quot;?&amp;gt;
&amp;lt;android.support.v4.widget.DrawerLayout xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;
    xmlns:app=&amp;quot;http://schemas.android.com/apk/res-auto&amp;quot;
    xmlns:tools=&amp;quot;http://schemas.android.com/tools&amp;quot;
    android:id=&amp;quot;@+id/drawer_layout&amp;quot;
    android:layout_width=&amp;quot;match_parent&amp;quot;
    android:layout_height=&amp;quot;match_parent&amp;quot;
    android:fitsSystemWindows=&amp;quot;true&amp;quot;
    tools:openDrawer=&amp;quot;start&amp;quot;&amp;gt;

    &amp;lt;include
        layout=&amp;quot;@layout/app_bar_main&amp;quot;
        android:layout_width=&amp;quot;match_parent&amp;quot;
        android:layout_height=&amp;quot;match_parent&amp;quot; /&amp;gt;

    &amp;lt;android.support.design.widget.NavigationView
        android:id=&amp;quot;@+id/nav_view&amp;quot;
        android:layout_width=&amp;quot;wrap_content&amp;quot;
        android:layout_height=&amp;quot;match_parent&amp;quot;
        android:layout_gravity=&amp;quot;start&amp;quot;
        android:fitsSystemWindows=&amp;quot;true&amp;quot;
        app:headerLayout=&amp;quot;@layout/nav_header_main&amp;quot;
        app:menu=&amp;quot;@menu/activity_main_drawer&amp;quot; /&amp;gt;

&amp;lt;/android.support.v4.widget.DrawerLayout&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;app:headerLayout接收一个layout，作为导航菜单顶部的Header。&lt;br&gt;app:menu接收一个menu，作为导航菜单的菜单项。activity_main_drawer如下：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;?xml version=&amp;quot;1.0&amp;quot; encoding=&amp;quot;utf-8&amp;quot;?&amp;gt;
&amp;lt;menu xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;&amp;gt;

    &amp;lt;group android:checkableBehavior=&amp;quot;single&amp;quot;&amp;gt;
        &amp;lt;item
            android:id=&amp;quot;@+id/all&amp;quot;
            android:checked=&amp;quot;true&amp;quot;
            android:icon=&amp;quot;@drawable/main_nav_ic_all&amp;quot;
            android:title=&amp;quot;全部&amp;quot; /&amp;gt;
        &amp;lt;item
            android:id=&amp;quot;@+id/good&amp;quot;
            android:icon=&amp;quot;@drawable/main_nav_ic_good&amp;quot;
            android:title=&amp;quot;精华&amp;quot; /&amp;gt;
        &amp;lt;item
            android:id=&amp;quot;@+id/share&amp;quot;
            android:icon=&amp;quot;@drawable/main_nav_ic_share&amp;quot;
            android:title=&amp;quot;分享&amp;quot; /&amp;gt;
        &amp;lt;item
            android:id=&amp;quot;@+id/ask&amp;quot;
            android:icon=&amp;quot;@drawable/main_nav_ic_ask&amp;quot;
            android:title=&amp;quot;问答&amp;quot; /&amp;gt;
        &amp;lt;item
            android:id=&amp;quot;@+id/job&amp;quot;
            android:icon=&amp;quot;@drawable/main_nav_ic_job&amp;quot;
            android:title=&amp;quot;招聘&amp;quot; /&amp;gt;
    &amp;lt;/group&amp;gt;


    &amp;lt;group  android:id=&amp;quot;@+id/group2&amp;quot;&amp;gt;
        &amp;lt;item
            android:id=&amp;quot;@+id/msg&amp;quot;
            android:icon=&amp;quot;@mipmap/ic_notifications_grey600_24dp&amp;quot;
            android:title=&amp;quot;消息&amp;quot; /&amp;gt;
        &amp;lt;item
            android:id=&amp;quot;@+id/setting&amp;quot;
            android:icon=&amp;quot;@mipmap/ic_settings_grey600_24dp&amp;quot;
            android:title=&amp;quot;设置&amp;quot; /&amp;gt;
        &amp;lt;item
            android:id=&amp;quot;@+id/about&amp;quot;
            android:icon=&amp;quot;@mipmap/ic_info_grey600_24dp&amp;quot;
            android:title=&amp;quot;关于&amp;quot; /&amp;gt;
    &amp;lt;/group&amp;gt;


&amp;lt;/menu&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;分为两个group后，会多一条横线分割，checkableBehavior=”single”表示是单选，android:checked=”true”表示已选中。&lt;br&gt;用setNavigationItemSelectedListener方法来设置当导航项被点击时的回调：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;mNavigationView.setNavigationItemSelectedListener(new NavigationView.OnNavigationItemSelectedListener() {
    @Override
    public boolean onNavigationItemSelected(MenuItem menuItem) {
         // Handle navigation view item clicks here.
    int id = item.getItemId();

    if (id == R.id.all) {
        page = 1;
        tab = &amp;quot;&amp;quot;;
        getList(true);
    } else if (id == R.id.ask) {
        page = 1;
        tab = &amp;quot;ask&amp;quot;;
        getList(true);
    } else if (id == R.id.share) {
        page = 1;
        tab = &amp;quot;share&amp;quot;;
        getList(true);
    } else if (id == R.id.job) {
        page = 1;
        tab = &amp;quot;job&amp;quot;;
        getList(true);
    } else if (id == R.id.good) {
        page = 1;
        tab = &amp;quot;good&amp;quot;;
        getList(true);
    } else if (id == R.id.msg) {

    } else if (id == R.id.setting) {

    }else if(id == R.id.about){

    }

    DrawerLayout drawer = (DrawerLayout) findViewById(R.id.drawer_layout);
    drawer.closeDrawer(GravityCompat.START);
    return true;
    }
});
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;使用Picasso实现圆形头像&quot;&gt;&lt;a href=&quot;#使用Picasso实现圆形头像&quot; class=&quot;headerlink&quot; title=&quot;使用Picasso实现圆形头像&quot;&gt;&lt;/a&gt;使用Picasso实现圆形头像&lt;/h4&gt;&lt;p&gt;官网例子（截取矩形图片中间部分为正方形）：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public class CropSquareTransformation implements Transformation {
  @Override public Bitmap transform(Bitmap source) {
    int size = Math.min(source.getWidth(), source.getHeight());
    int x = (source.getWidth() - size) / 2;
    int y = (source.getHeight() - size) / 2;
    Bitmap result = Bitmap.createBitmap(source, x, y, size, size);
    if (result != source) {
      source.recycle();
    }
    return result;
  }

  @Override public String key() { return &amp;quot;square()&amp;quot;; }
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;参考官网例子，定义圆形转换器类&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public class CircleTransform implements Transformation {
    @Override
    public Bitmap transform(Bitmap source) {
         //获取Bitmap的宽度
        int width = source.getWidth();
        //返回一个正方形的Bitmap对象
        Bitmap mBitmap = Bitmap.createBitmap(width,width, Bitmap.Config.ARGB_8888);
        //定义指定宽高的canvas
        Canvas mCanvas = new Canvas(mBitmap);
        //定义画笔
        Paint mPaint = new Paint();
        //设置抗锯齿
        mPaint.setAntiAlias(true);
        //背景：在画布上绘制一个圆
        mCanvas.drawCircle(width/2,width/2,width/2,mPaint);
        //设置图片相交情况下的处理方式
        //setXfermode：设置当绘制的图像出现相交情况时候的处理方式的,它包含的常用模式有哪几种
        //PorterDuff.Mode.SRC_IN 取两层图像交集部门,只显示上层图像,注意这里是指取相交叉的部分,然后显示上层图像
        //PorterDuff.Mode.DST_IN 取两层图像交集部门,只显示下层图像
        mPaint.setXfermode(new PorterDuffXfermode(PorterDuff.Mode.SRC_IN));
        //前景：在画布上绘制一个bitmap
        mCanvas.drawBitmap(source,0,0,mPaint);
        //必须要回收source，否则会报错
        source.recycle();
        return mBitmap;
    }

    @Override
    public String key() {
        return &amp;quot;square()&amp;quot;;
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;调用：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt; private CircleTransform mCircleTransform = new CircleTransform();
Picasso.with(context)
            .load(authorImageUrl)
            .resize(100, 100)
            .centerCrop()
            .transform(mCircleTransform)
            .into(viewHolder.author_image);
&lt;/code&gt;&lt;/pre&gt;</content>
    
    <summary type="html">
    
      &lt;p&gt;本文为开发CNode-android的笔记，供以后开发参考&lt;br&gt;
    
    </summary>
    
    
      <category term="android" scheme="http://yoursite.com/tags/android/"/>
    
  </entry>
  
  <entry>
    <title>RxJava入门</title>
    <link href="http://yoursite.com/2016/10/18/RxJava%E5%85%A5%E9%97%A8/"/>
    <id>http://yoursite.com/2016/10/18/RxJava入门/</id>
    <published>2016-10-18T02:04:32.000Z</published>
    <updated>2016-10-18T15:52:28.799Z</updated>
    
    <content type="html">&lt;h4 id=&quot;RxJava-是什么&quot;&gt;&lt;a href=&quot;#RxJava-是什么&quot; class=&quot;headerlink&quot; title=&quot;RxJava 是什么&quot;&gt;&lt;/a&gt;RxJava 是什么&lt;/h4&gt;&lt;p&gt;Github上的介绍：a library for composing asynchronous and event-based programs by using observable sequences.&lt;br&gt;一个在 Java VM 上使用可观测的序列来组成异步的、基于事件的程序的库&lt;br&gt;&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;&lt;/p&gt;
&lt;h4 id=&quot;观察者模式举例&quot;&gt;&lt;a href=&quot;#观察者模式举例&quot; class=&quot;headerlink&quot; title=&quot;观察者模式举例&quot;&gt;&lt;/a&gt;观察者模式举例&lt;/h4&gt;&lt;p&gt;举例：按下开关，台灯灯亮&lt;br&gt;在这个事件中，台灯作为观察者，开关作为被观察者，台灯透过电线来观察开关的状态来并做出相应的处理&lt;/p&gt;
&lt;h4 id=&quot;创建被观察者（开关）&quot;&gt;&lt;a href=&quot;#创建被观察者（开关）&quot; class=&quot;headerlink&quot; title=&quot;创建被观察者（开关）&quot;&gt;&lt;/a&gt;创建被观察者（开关）&lt;/h4&gt;&lt;p&gt;正常模式：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Observable switcher=Observable.create(new Observable.OnSubscribe&amp;lt;String&amp;gt;(){

    @Override
    public void call(Subscriber&amp;lt;? super String&amp;gt; subscriber) {
        subscriber.onNext(&amp;quot;On&amp;quot;);
        subscriber.onNext(&amp;quot;Off&amp;quot;);
        subscriber.onNext(&amp;quot;On&amp;quot;);
        subscriber.onNext(&amp;quot;On&amp;quot;);
        subscriber.onCompleted();
    }
});
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;偷懒模式1&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Observable switcher=Observable.just(&amp;quot;On&amp;quot;,&amp;quot;Off&amp;quot;,&amp;quot;On&amp;quot;,&amp;quot;On&amp;quot;);
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;偷懒模式2&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;String [] kk={&amp;quot;On&amp;quot;,&amp;quot;Off&amp;quot;,&amp;quot;On&amp;quot;,&amp;quot;On&amp;quot;};
Observable switcher=Observable.from(kk);
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;创建观察者（台灯）&quot;&gt;&lt;a href=&quot;#创建观察者（台灯）&quot; class=&quot;headerlink&quot; title=&quot;创建观察者（台灯）&quot;&gt;&lt;/a&gt;创建观察者（台灯）&lt;/h4&gt;&lt;p&gt;正常模式&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Subscriber light=new Subscriber&amp;lt;String&amp;gt;() {
    @Override
    public void onCompleted() {
        //被观察者的onCompleted()事件会走到这里;
        Log.d(&amp;quot;DDDDDD&amp;quot;,&amp;quot;结束观察...\n&amp;quot;);
    }

    @Override
    public void onError(Throwable e) {
            //出现错误会调用这个方法
    }
    @Override
    public void onNext(String s) {
        //处理传过来的onNext事件
        Log.d(&amp;quot;DDDDD&amp;quot;,&amp;quot;handle this---&amp;quot;+s)
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;偷懒模式&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Action1 light=new Action1&amp;lt;String&amp;gt;() {
       @Override
       public void call(String s) {
           Log.d(&amp;quot;DDDDD&amp;quot;,&amp;quot;handle this---&amp;quot;+s)
       }
   }
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;订阅&quot;&gt;&lt;a href=&quot;#订阅&quot; class=&quot;headerlink&quot; title=&quot;订阅&quot;&gt;&lt;/a&gt;订阅&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;switcher.subscribe(light);
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;基本骨架：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;//创建被观察者，是事件传递的起点
Observable.just(&amp;quot;On&amp;quot;,&amp;quot;Off&amp;quot;,&amp;quot;On&amp;quot;,&amp;quot;On&amp;quot;)
//这就是在传递过程中对事件进行过滤操作
 .filter(new Func1&amp;lt;String, Boolean&amp;gt;() {
            @Override
            public Boolean call(String s) {
                return s！=null;
            }
        })
//实现订阅
.subscribe(
    //创建观察者，作为事件传递的终点处理事件    
      new Subscriber&amp;lt;String&amp;gt;() {
        @Override
        public void onCompleted() {
            Log.d(&amp;quot;DDDDDD&amp;quot;,&amp;quot;结束观察...\n&amp;quot;);
        }

        @Override
        public void onError(Throwable e) {
            //出现错误会调用这个方法
        }
        @Override
        public void onNext(String s) {
            //处理事件
            Log.d(&amp;quot;DDDDD&amp;quot;,&amp;quot;handle this---&amp;quot;+s)
        }
    }
);
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;总结：  &lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;创建被观察者，产生事件&lt;/li&gt;
&lt;li&gt;设置事件传递过程中的过滤，合并，变换等加工操作。&lt;/li&gt;
&lt;li&gt;订阅一个观察者对象，实现事件最终的处理。&lt;/li&gt;
&lt;/ul&gt;
&lt;blockquote&gt;
&lt;p&gt;Tips: 当调用订阅操作（即调用Observer.subscribe()方法）的时候，被观察者才真正开始发出事件。  &lt;/p&gt;
&lt;/blockquote&gt;
&lt;h4 id=&quot;Map操作&quot;&gt;&lt;a href=&quot;#Map操作&quot; class=&quot;headerlink&quot; title=&quot;Map操作&quot;&gt;&lt;/a&gt;Map操作&lt;/h4&gt;&lt;p&gt;例子：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Observable.just(&amp;quot;images/logo.png&amp;quot;) // 输入类型 String
.map(new Func1&amp;lt;String, Bitmap&amp;gt;() {
    @Override
    public Bitmap call(String filePath) { // 参数类型 String
        return getBitmapFromPath(filePath); // 返回类型 Bitmap
    }
})
.subscribe(new Action1&amp;lt;Bitmap&amp;gt;() {
    @Override
    public void call(Bitmap bitmap) { // 参数类型 Bitmap
        showBitmap(bitmap);
    }
});
&lt;/code&gt;&lt;/pre&gt;&lt;blockquote&gt;
&lt;p&gt;实际上在使用map操作时，new Func1() 就对应了类型的转你方向，String是原类型，Bitmap是转换后的类型。在call()方法中，输入的是原类型，返回转换后的类型 &lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;读取文件，创建bitmap可能是一个耗时操作，那么就应该在子线程中执行，主线程应该仅仅做展示。那么线程切换一般就会是比较复杂的事情了。但是在Rxjava中，是非常方便的。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Observable.create(new Observable.just(getFilePath())
   //指定了被观察者执行的线程环境
  .subscribeOn(Schedulers.newThread())
  //将接下来执行的线程环境指定为io线程
  .observeOn(Schedulers.io())
    //使用map操作来完成类型转换
    .map(new Func1&amp;lt;String, Bitmap&amp;gt;() {
      @Override
      public Bitmap call(String s) {
        //显然自定义的createBitmapFromPath(s)方法，是一个极其耗时的操作
          return createBitmapFromPath(s);
      }
  })
    //将后面执行的线程环境切换为主线程
    .observeOn(AndroidSchedulers.mainThread())
    .subscribe(
         //创建观察者，作为事件传递的终点处理事件    
          new Subscriber&amp;lt;Bitmap&amp;gt;() {
                @Override
                public void onCompleted() {
                    Log.d(&amp;quot;DDDDDD&amp;quot;,&amp;quot;结束观察...\n&amp;quot;);
                }

                @Override
                public void onError(Throwable e) {
                    //出现错误会调用这个方法
                }
                @Override
                public void onNext(Bitmap s) {
                    //处理事件
                    showBitmap(s)
                }
            );
    }
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;flatMap-操作&quot;&gt;&lt;a href=&quot;#flatMap-操作&quot; class=&quot;headerlink&quot; title=&quot;flatMap()操作&quot;&gt;&lt;/a&gt;flatMap()操作&lt;/h4&gt;&lt;p&gt;假设这么一种需求：假设有一个数据结构『学生』，现在需要打印出一组学生的名字。实现方式很简单：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Student[] students = ...;
Subscriber&amp;lt;String&amp;gt; subscriber = new Subscriber&amp;lt;String&amp;gt;() {
    @Override
    public void onNext(String name) {
        Log.d(tag, name);
    }
    ...
};
Observable.from(students)
    .map(new Func1&amp;lt;Student, String&amp;gt;() {
        @Override
        public String call(Student student) {
            return student.getName();
        }
    })
.subscribe(subscriber);
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;很简单。那么再假设：如果要打印出每个学生所需要修的所有课程的名称呢？（需求的区别在于，每个学生只有一个名字，但却有多个课程。）首先可以这样实现：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Student[] students = ...;
Subscriber&amp;lt;Student&amp;gt; subscriber = new Subscriber&amp;lt;Student&amp;gt;() {
    @Override
    public void onNext(Student student) {
        List&amp;lt;Course&amp;gt; courses = student.getCourses();
        for (int i = 0; i &amp;lt; courses.size(); i++) {
            Course course = courses.get(i);
            Log.d(tag, course.getName());
        }
    }
    ...
};
Observable.from(students)
.subscribe(subscriber);
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;依然很简单。那么如果我不想在 Subscriber 中使用 for 循环，而是希望 Subscriber 中直接传入单个的 Course 对象呢（这对于代码复用很重要）？用 map() 显然是不行的，因为 map() 是一对一的转化，而我现在的要求是一对多的转化。那怎么才能把一个 Student 转化成多个 Course 呢？&lt;br&gt;这个时候，就需要用 &lt;code&gt;flatMap()&lt;/code&gt; 了：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Student[] students = ...;
Subscriber&amp;lt;Course&amp;gt; subscriber = new Subscriber&amp;lt;Course&amp;gt;() {
    @Override
    public void onNext(Course course) {
        Log.d(tag, course.getName());
    }
    ...
};
Observable.from(students)
    .flatMap(new Func1&amp;lt;Student, Observable&amp;lt;Course&amp;gt;&amp;gt;() {
        @Override
        public Observable&amp;lt;Course&amp;gt; call(Student student) {
            return Observable.from(student.getCourses());
        }
    })
.subscribe(subscriber);
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;异步（线程调度）&quot;&gt;&lt;a href=&quot;#异步（线程调度）&quot; class=&quot;headerlink&quot; title=&quot;异步（线程调度）&quot;&gt;&lt;/a&gt;异步（线程调度）&lt;/h4&gt;&lt;p&gt;在讲解Map操作符时，已经提到了线程调度，在这里我用更加简介的代码代替：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Observable.create(new Observable.just(getFilePath())
      //指定在新线程中创建被观察者
     .subscribeOn(Schedulers.newThread())
     //将接下来执行的线程环境指定为io线程
     .observeOn(Schedulers.io())
       //map就处在io线程
     .map(mMapOperater)
       //将后面执行的线程环境切换为主线程，
       //但是这一句依然执行在io线程
     .observeOn(AndroidSchedulers.mainThread())
     //指定线程无效，但这句代码本身执行在主线程
     .subscribeOn(Schedulers.io())
     //执行在主线程
     .subscribe(mSubscriber);
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;实际上线程调度只有subscribeOn（）和observeOn（）两个方法。对于初学者，只需要掌握两点：  &lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;subscribeOn（）它指示Observable在一个指定的调度器上创建（只作用于被观察者创建阶段）。只能指定一次，如果指定多次则以第一次为准&lt;/li&gt;
&lt;li&gt;observeOn（）指定在事件传递（加工变换）和最终被处理（观察者）的发生在哪一个调度器。可指定多次，每次指定完都在下一步生效。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;RxJava已经为我们提供了一下几个Scheduler&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Schedulers.immediate()：直接在当前线程运行，相当于不指定线程。这是默认的 Scheduler。&lt;/li&gt;
&lt;li&gt;Schedulers.newThread()：总是启用新线程，并在新线程执行操作。&lt;/li&gt;
&lt;li&gt;Schedulers.io()： I/O 操作（读写文件、读写数据库、网络信息交互等）所使用的 Scheduler。行为模式和 newThread() 差不多，区别在于 io() 的内部实现是是用一个无数量上限的线程池，可以重用空闲的线程，因此多数情况下 io() 比 newThread() 更有效率。不要把计算工作放在 io() 中，可以避免创建不必要的线程。&lt;/li&gt;
&lt;li&gt;Schedulers.computation()：计算所使用的 Scheduler。这个计算指的是 CPU 密集型计算，即不会被 I/O 等操作限制性能的操作，例如图形的计算。这个 Scheduler 使用的固定的线程池，大小为 CPU 核数。不要把 I/O 操作放在 computation() 中，否则 I/O 操作的等待时间会浪费 CPU。&lt;/li&gt;
&lt;li&gt;AndroidSchedulers.mainThread()：它指定的操作将在 Android 主线程运行。&lt;/li&gt;
&lt;/ul&gt;
&lt;h4 id=&quot;与-Retrofit-的结合&quot;&gt;&lt;a href=&quot;#与-Retrofit-的结合&quot; class=&quot;headerlink&quot; title=&quot;与 Retrofit 的结合&quot;&gt;&lt;/a&gt;与 Retrofit 的结合&lt;/h4&gt;&lt;p&gt;以获取一个 User 对象的接口作为例子。使用Retrofit 的传统 API，你可以用这样的方式来定义请求：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;@GET(&amp;quot;/user&amp;quot;)
public void getUser(@Query(&amp;quot;userId&amp;quot;) String userId, Callback&amp;lt;User&amp;gt; callback);
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;在程序的构建过程中， Retrofit 会把自动把方法实现并生成代码，然后开发者就可以利用下面的方法来获取特定用户并处理响应：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;getUser(userId, new Callback&amp;lt;User&amp;gt;() {
    @Override
    public void success(User user) {
        userView.setUser(user);
    }

    @Override
    public void failure(RetrofitError error) {
        // Error handling
        ...
    }
};
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;而使用 RxJava 形式的 API，定义同样的请求是这样的：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;@GET(&amp;quot;/user&amp;quot;)
public Observable&amp;lt;User&amp;gt; getUser(@Query(&amp;quot;userId&amp;quot;) String userId);
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;使用的时候是这样的：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;getUser(userId)
    .subscribeOn(Schedulers.newThread())
    .observeOn(AndroidSchedulers.mainThread())
    .subscribe(new Observer&amp;lt;User&amp;gt;() {
        @Override
        public void onNext(User user) {
            userView.setUser(user);
        }

        @Override
        public void onCompleted() {
        }

        @Override
        public void onError(Throwable error) {
            // Error handling
            ...
        }
    });
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;再举一个例子：假设 /user 接口并不能直接访问，而需要填入一个在线获取的 token ，代码应该怎么写？&lt;br&gt;Callback 方式，可以使用嵌套的 Callback：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;@GET(&amp;quot;/token&amp;quot;)
public void getToken(Callback&amp;lt;String&amp;gt; callback);

@GET(&amp;quot;/user&amp;quot;)
public void getUser(@Query(&amp;quot;token&amp;quot;) String token, @Query(&amp;quot;userId&amp;quot;) String userId, Callback&amp;lt;User&amp;gt; callback);

...

getToken(new Callback&amp;lt;String&amp;gt;() {
    @Override
    public void success(String token) {
        getUser(token, userId, new Callback&amp;lt;User&amp;gt;() {
            @Override
            public void success(User user) {
                userView.setUser(user);
            }

            @Override
            public void failure(RetrofitError error) {
                // Error handling
                ...
            }
        };
    }

    @Override
    public void failure(RetrofitError error) {
        // Error handling
        ...
    }
});
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;倒是没有什么性能问题，可是迷之缩进毁一生，你懂我也懂，做过大项目的人应该更懂。&lt;br&gt;而使用 RxJava 的话，代码是这样的：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;@GET(&amp;quot;/token&amp;quot;)
public Observable&amp;lt;String&amp;gt; getToken();

@GET(&amp;quot;/user&amp;quot;)
public Observable&amp;lt;User&amp;gt; getUser(@Query(&amp;quot;token&amp;quot;) String token, @Query(&amp;quot;userId&amp;quot;) String userId);

...

getToken()
    .flatMap(new Func1&amp;lt;String, Observable&amp;lt;User&amp;gt;&amp;gt;() {
        @Override
        public Observable&amp;lt;User&amp;gt; onNext(String token) {
            return getUser(token, userId);
        })
    .observeOn(AndroidSchedulers.mainThread())
    .subscribe(new Observer&amp;lt;User&amp;gt;() {
        @Override
        public void onNext(User user) {
            userView.setUser(user);
        }

        @Override
        public void onCompleted() {
        }

        @Override
        public void onError(Throwable error) {
            // Error handling
            ...
        }
    });  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;参考：&lt;br&gt;&lt;a href=&quot;http://gank.io/post/560e15be2dca930e00da1083&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;http://gank.io/post/560e15be2dca930e00da1083&lt;/a&gt;&lt;br&gt;&lt;a href=&quot;http://gold.xitu.io/post/580103f20e3dd90057fc3e6d&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;http://gold.xitu.io/post/580103f20e3dd90057fc3e6d&lt;/a&gt;  &lt;/p&gt;
&lt;p&gt;Demo:&lt;a href=&quot;/uploads/RxjavaTest.rar&quot;&gt;RxjavaTest.rar&lt;/a&gt;&lt;/p&gt;
</content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;RxJava-是什么&quot;&gt;&lt;a href=&quot;#RxJava-是什么&quot; class=&quot;headerlink&quot; title=&quot;RxJava 是什么&quot;&gt;&lt;/a&gt;RxJava 是什么&lt;/h4&gt;&lt;p&gt;Github上的介绍：a library for composing asynchronous and event-based programs by using observable sequences.&lt;br&gt;一个在 Java VM 上使用可观测的序列来组成异步的、基于事件的程序的库&lt;br&gt;
    
    </summary>
    
    
      <category term="android" scheme="http://yoursite.com/tags/android/"/>
    
  </entry>
  
  <entry>
    <title>React笔记</title>
    <link href="http://yoursite.com/2016/10/13/React%E7%AC%94%E8%AE%B0/"/>
    <id>http://yoursite.com/2016/10/13/React笔记/</id>
    <published>2016-10-13T08:42:35.000Z</published>
    <updated>2016-10-14T03:02:04.910Z</updated>
    
    <content type="html">&lt;h3 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h3&gt;&lt;p&gt;一个用于构建用户界面的JavaScript库&lt;br&gt;&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;&lt;/p&gt;
&lt;h3 id=&quot;Hello-world&quot;&gt;&lt;a href=&quot;#Hello-world&quot; class=&quot;headerlink&quot; title=&quot;Hello world&quot;&gt;&lt;/a&gt;Hello world&lt;/h3&gt;&lt;p&gt;1.下载&lt;br&gt;&lt;a href=&quot;https://facebook.github.io/react/index.html&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://facebook.github.io/react/index.html&lt;/a&gt;  &lt;/p&gt;
&lt;p&gt;2.解压后在根目录下创建 &lt;code&gt;helloworld.html&lt;/code&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;!DOCTYPE html&amp;gt;
&amp;lt;html&amp;gt;
  &amp;lt;head&amp;gt;
    &amp;lt;meta charset=&amp;quot;UTF-8&amp;quot; /&amp;gt;
    &amp;lt;title&amp;gt;Hello React!&amp;lt;/title&amp;gt;
    &amp;lt;script src=&amp;quot;build/react.js&amp;quot;&amp;gt;&amp;lt;/script&amp;gt;
    &amp;lt;script src=&amp;quot;build/react-dom.js&amp;quot;&amp;gt;&amp;lt;/script&amp;gt;
    &amp;lt;script src=&amp;quot;https://unpkg.com/babel-core@5.8.38/browser.min.js&amp;quot;&amp;gt;&amp;lt;/script&amp;gt;
  &amp;lt;/head&amp;gt;
  &amp;lt;body&amp;gt;
    &amp;lt;div id=&amp;quot;example&amp;quot;&amp;gt;&amp;lt;/div&amp;gt;
    &amp;lt;script type=&amp;quot;text/babel&amp;quot;&amp;gt;
      ReactDOM.render(
        &amp;lt;h1&amp;gt;Hello, world!&amp;lt;/h1&amp;gt;,
        document.getElementById(&amp;apos;example&amp;apos;)
      );
    &amp;lt;/script&amp;gt;
  &amp;lt;/body&amp;gt;
&amp;lt;/html&amp;gt;  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;在浏览器打开后  你应该能看到 Hello, world!&lt;/p&gt;
&lt;h3 id=&quot;评论框的实现&quot;&gt;&lt;a href=&quot;#评论框的实现&quot; class=&quot;headerlink&quot; title=&quot;评论框的实现&quot;&gt;&lt;/a&gt;评论框的实现&lt;/h3&gt;&lt;h4 id=&quot;新建一个index-html&quot;&gt;&lt;a href=&quot;#新建一个index-html&quot; class=&quot;headerlink&quot; title=&quot;新建一个index.html&quot;&gt;&lt;/a&gt;新建一个index.html&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;&amp;lt;!-- index.html --&amp;gt;
&amp;lt;!DOCTYPE html&amp;gt;
&amp;lt;html&amp;gt;
  &amp;lt;head&amp;gt;
    &amp;lt;meta charset=&amp;quot;utf-8&amp;quot; /&amp;gt;
    &amp;lt;title&amp;gt;React Tutorial&amp;lt;/title&amp;gt;
    &amp;lt;script src=&amp;quot;https://unpkg.com/react@15.3.2/dist/react.js&amp;quot;&amp;gt;&amp;lt;/script&amp;gt;
    &amp;lt;script src=&amp;quot;https://unpkg.com/react-dom@15.3.2/dist/react-dom.js&amp;quot;&amp;gt;&amp;lt;/script&amp;gt;
    &amp;lt;script src=&amp;quot;https://unpkg.com/babel-standalone@6.15.0/babel.min.js&amp;quot;&amp;gt;&amp;lt;/script&amp;gt;
    &amp;lt;script src=&amp;quot;https://unpkg.com/jquery@3.1.0/dist/jquery.min.js&amp;quot;&amp;gt;&amp;lt;/script&amp;gt;
    &amp;lt;script src=&amp;quot;https://unpkg.com/remarkable@1.7.1/dist/remarkable.min.js&amp;quot;&amp;gt;&amp;lt;/script&amp;gt;
  &amp;lt;/head&amp;gt;
  &amp;lt;body&amp;gt;
    &amp;lt;div id=&amp;quot;content&amp;quot;&amp;gt;&amp;lt;/div&amp;gt;

    &amp;lt;script type=&amp;quot;text/babel&amp;quot;&amp;gt;

    &amp;lt;/script&amp;gt;
  &amp;lt;/body&amp;gt;
&amp;lt;/html&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;构建CommentBox&quot;&gt;&lt;a href=&quot;#构建CommentBox&quot; class=&quot;headerlink&quot; title=&quot;构建CommentBox&quot;&gt;&lt;/a&gt;构建CommentBox&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;var CommentBox = React.createClass({
  render: function() {
    return (
      &amp;lt;div className=&amp;quot;commentBox&amp;quot;&amp;gt;
        Hello, world! I am a CommentBox.
      &amp;lt;/div&amp;gt;
    );
  }
});
ReactDOM.render(
  &amp;lt;CommentBox /&amp;gt;,
  document.getElementById(&amp;apos;content&amp;apos;)
);
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;构建CommentList、CommentForm，并组装&quot;&gt;&lt;a href=&quot;#构建CommentList、CommentForm，并组装&quot; class=&quot;headerlink&quot; title=&quot;构建CommentList、CommentForm，并组装&quot;&gt;&lt;/a&gt;构建CommentList、CommentForm，并组装&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;var CommentList = React.createClass({
  render: function() {
    return (
      &amp;lt;div className=&amp;quot;commentList&amp;quot;&amp;gt;
        Hello, world! I am a CommentList.
      &amp;lt;/div&amp;gt;
    );
  }
});

var CommentForm = React.createClass({
  render: function() {
    return (
      &amp;lt;div className=&amp;quot;commentForm&amp;quot;&amp;gt;
        Hello, world! I am a CommentForm.
      &amp;lt;/div&amp;gt;
    );
  }
});  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;更新CommentBox，使用CommentList、CommentForm  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var CommentBox = React.createClass({
  render: function() {
    return (
      &amp;lt;div className=&amp;quot;commentBox&amp;quot;&amp;gt;
        &amp;lt;h1&amp;gt;Comments&amp;lt;/h1&amp;gt;
        &amp;lt;CommentList /&amp;gt;
        &amp;lt;CommentForm /&amp;gt;
      &amp;lt;/div&amp;gt;
    );
  }
}); 
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;使用-props-构建Comment&quot;&gt;&lt;a href=&quot;#使用-props-构建Comment&quot; class=&quot;headerlink&quot; title=&quot;使用 props 构建Comment&quot;&gt;&lt;/a&gt;使用 props 构建Comment&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;var Comment = React.createClass({
  render: function() {
    return (
      &amp;lt;div className=&amp;quot;comment&amp;quot;&amp;gt;
        &amp;lt;h2 className=&amp;quot;commentAuthor&amp;quot;&amp;gt;
          {this.props.author}
        &amp;lt;/h2&amp;gt;
        {this.props.children}
      &amp;lt;/div&amp;gt;
    );
  }
});  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;通过this.props.author获取Comment节点的author属性，通过this.props.children获取Comment下的子节点&lt;br&gt;更新CommentList   &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var CommentList = React.createClass({
  render: function() {
    return (
      &amp;lt;div className=&amp;quot;commentList&amp;quot;&amp;gt;
        &amp;lt;Comment author=&amp;quot;Pete Hunt&amp;quot;&amp;gt;This is one comment&amp;lt;/Comment&amp;gt;
        &amp;lt;Comment author=&amp;quot;Jordan Walke&amp;quot;&amp;gt;This is *another* comment&amp;lt;/Comment&amp;gt;
      &amp;lt;/div&amp;gt;
    );
  }
});
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;####添加Markdown ####&lt;br&gt;使用第三方库remarkable来使评论支持Markdown，代码如下&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var Comment = React.createClass({
  render: function() {
    var md = new Remarkable();
    return (
      &amp;lt;div className=&amp;quot;comment&amp;quot;&amp;gt;
        &amp;lt;h2 className=&amp;quot;commentAuthor&amp;quot;&amp;gt;
          {this.props.author}
        &amp;lt;/h2&amp;gt;
        {md.render(this.props.children.toString())}
      &amp;lt;/div&amp;gt;
    );
  }
});  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;这样有个问题，评论会显示成&lt;code&gt;&amp;lt;p&amp;gt;This is &amp;lt;em&amp;gt;another&amp;lt;/em&amp;gt; comment&amp;lt;/p&amp;gt;&lt;/code&gt;,我们希望转化成HTML，方法如下  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var Comment = React.createClass({
  rawMarkup: function() {
    var md = new Remarkable();
    var rawMarkup = md.render(this.props.children.toString());
    return { __html: rawMarkup };
  },

  render: function() {
    return (
      &amp;lt;div className=&amp;quot;comment&amp;quot;&amp;gt;
        &amp;lt;h2 className=&amp;quot;commentAuthor&amp;quot;&amp;gt;
          {this.props.author}
        &amp;lt;/h2&amp;gt;
        &amp;lt;span dangerouslySetInnerHTML={this.rawMarkup()} /&amp;gt;
      &amp;lt;/div&amp;gt;
    );
  }
});
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;数据注入&quot;&gt;&lt;a href=&quot;#数据注入&quot; class=&quot;headerlink&quot; title=&quot;数据注入&quot;&gt;&lt;/a&gt;数据注入&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;var data = [
  {id: 1, author: &amp;quot;Pete Hunt&amp;quot;, text: &amp;quot;This is one comment&amp;quot;},
  {id: 2, author: &amp;quot;Jordan Walke&amp;quot;, text: &amp;quot;This is *another* comment&amp;quot;}
];  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;修改CommentBox和ReactDOM.render（）来传递数据到CommentList&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var CommentBox = React.createClass({
  render: function() {
    return (
      &amp;lt;div className=&amp;quot;commentBox&amp;quot;&amp;gt;
        &amp;lt;h1&amp;gt;Comments&amp;lt;/h1&amp;gt;
        &amp;lt;CommentList data={this.props.data} /&amp;gt;
        &amp;lt;CommentForm /&amp;gt;
      &amp;lt;/div&amp;gt;
    );
  }
});

ReactDOM.render(
  &amp;lt;CommentBox data={data} /&amp;gt;,
  document.getElementById(&amp;apos;content&amp;apos;)
);  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;动态显示CommentList数据  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var CommentList = React.createClass({
  render: function() {
    var commentNodes = this.props.data.map(function(comment) {
      return (
        &amp;lt;Comment author={comment.author} key={comment.id}&amp;gt;
          {comment.text}
        &amp;lt;/Comment&amp;gt;
      );
    });
    return (
      &amp;lt;div className=&amp;quot;commentList&amp;quot;&amp;gt;
        {commentNodes}
      &amp;lt;/div&amp;gt;
    );
  }
});  
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;从服务器上获取数据&quot;&gt;&lt;a href=&quot;#从服务器上获取数据&quot; class=&quot;headerlink&quot; title=&quot;从服务器上获取数据&quot;&gt;&lt;/a&gt;从服务器上获取数据&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;ReactDOM.render(
  &amp;lt;CommentBox url=&amp;quot;/api/comments&amp;quot; /&amp;gt;,
  document.getElementById(&amp;apos;content&amp;apos;)
);
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;使用-state&quot;&gt;&lt;a href=&quot;#使用-state&quot; class=&quot;headerlink&quot; title=&quot;使用 state&quot;&gt;&lt;/a&gt;使用 state&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;var CommentBox = React.createClass({
  getInitialState: function() {//组件初始化函数
    return {data: []};
  },
  render: function() {
    return (
      &amp;lt;div className=&amp;quot;commentBox&amp;quot;&amp;gt;
        &amp;lt;h1&amp;gt;Comments&amp;lt;/h1&amp;gt;
        &amp;lt;CommentList data={this.state.data} /&amp;gt;
        &amp;lt;CommentForm /&amp;gt;
      &amp;lt;/div&amp;gt;
    );
  }
});  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;更新组件&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var CommentBox = React.createClass({
  getInitialState: function() {
    return {data: []};
  },
  componentDidMount: function() {//当组件第一次render完成之后自动调用
    $.ajax({
      url: this.props.url,
      dataType: &amp;apos;json&amp;apos;,
      cache: false,
      success: function(data) {
        this.setState({data: data});
      }.bind(this),
      error: function(xhr, status, err) {
        console.error(this.props.url, status, err.toString());
      }.bind(this)
    });
  },
  render: function() {
    return (
      &amp;lt;div className=&amp;quot;commentBox&amp;quot;&amp;gt;
        &amp;lt;h1&amp;gt;Comments&amp;lt;/h1&amp;gt;
        &amp;lt;CommentList data={this.state.data} /&amp;gt;
        &amp;lt;CommentForm /&amp;gt;
      &amp;lt;/div&amp;gt;
    );
  }
});
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;通过调用this.setState();方法改变state，React会自动更新UI&lt;/p&gt;
&lt;h4 id=&quot;提交评论&quot;&gt;&lt;a href=&quot;#提交评论&quot; class=&quot;headerlink&quot; title=&quot;提交评论&quot;&gt;&lt;/a&gt;提交评论&lt;/h4&gt;&lt;p&gt;更新CommentForm组件  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var CommentForm = React.createClass({
  render: function() {
    return (
      &amp;lt;form className=&amp;quot;commentForm&amp;quot;&amp;gt;
        &amp;lt;input type=&amp;quot;text&amp;quot; placeholder=&amp;quot;Your name&amp;quot; /&amp;gt;
        &amp;lt;input type=&amp;quot;text&amp;quot; placeholder=&amp;quot;Say something...&amp;quot; /&amp;gt;
        &amp;lt;input type=&amp;quot;submit&amp;quot; value=&amp;quot;Post&amp;quot; /&amp;gt;
      &amp;lt;/form&amp;gt;
    );
  }
});  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;处理用户输入，将author和text存入state,将input的value设置为&lt;code&gt;this.state.author&lt;/code&gt;，在onChange中调用this.setState（）修改author和text的值，这样input中的值将自动更新，更新代码如下&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var CommentForm = React.createClass({
  getInitialState: function() {//初始化author和text
    return {author: &amp;apos;&amp;apos;, text: &amp;apos;&amp;apos;};
  },
  handleAuthorChange: function(e) {
    this.setState({author: e.target.value});
  },
  handleTextChange: function(e) {
    this.setState({text: e.target.value});
  },
  render: function() {
    return (
      &amp;lt;form className=&amp;quot;commentForm&amp;quot;&amp;gt;
        &amp;lt;input
          type=&amp;quot;text&amp;quot;
          placeholder=&amp;quot;Your name&amp;quot;
          value={this.state.author}
          onChange={this.handleAuthorChange}
        /&amp;gt;
        &amp;lt;input
          type=&amp;quot;text&amp;quot;
          placeholder=&amp;quot;Say something...&amp;quot;
          value={this.state.text}
          onChange={this.handleTextChange}
        /&amp;gt;
        &amp;lt;input type=&amp;quot;submit&amp;quot; value=&amp;quot;Post&amp;quot; /&amp;gt;
      &amp;lt;/form&amp;gt;
    );
  }
});  
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;提交评论-1&quot;&gt;&lt;a href=&quot;#提交评论-1&quot; class=&quot;headerlink&quot; title=&quot;提交评论&quot;&gt;&lt;/a&gt;提交评论&lt;/h4&gt;&lt;p&gt;将form的 &lt;code&gt;onSubmit={this.handleSubmit}&lt;/code&gt;,点提交按钮触发handleSubmit方法，提交数据，并将数据清空&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var CommentForm = React.createClass({
  getInitialState: function() {
    return {author: &amp;apos;&amp;apos;, text: &amp;apos;&amp;apos;};
  },
  handleAuthorChange: function(e) {
    this.setState({author: e.target.value});
  },
  handleTextChange: function(e) {
    this.setState({text: e.target.value});
  },
  handleSubmit: function(e) {
    e.preventDefault();//阻止浏览器默认的提交表单事件
    var author = this.state.author.trim();
    var text = this.state.text.trim();
    if (!text || !author) {
      return;
    }
    // TODO: 提交数据
    this.setState({author: &amp;apos;&amp;apos;, text: &amp;apos;&amp;apos;});
  },
  render: function() {
    return (
      &amp;lt;form className=&amp;quot;commentForm&amp;quot; onSubmit={this.handleSubmit}&amp;gt;
        &amp;lt;input
          type=&amp;quot;text&amp;quot;
          placeholder=&amp;quot;Your name&amp;quot;
          value={this.state.author}
          onChange={this.handleAuthorChange}
        /&amp;gt;
        &amp;lt;input
          type=&amp;quot;text&amp;quot;
          placeholder=&amp;quot;Say something...&amp;quot;
          value={this.state.text}
          onChange={this.handleTextChange}
        /&amp;gt;
        &amp;lt;input type=&amp;quot;submit&amp;quot; value=&amp;quot;Post&amp;quot; /&amp;gt;
      &amp;lt;/form&amp;gt;
    );
  }
});
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;使用props调用组件的方法&quot;&gt;&lt;a href=&quot;#使用props调用组件的方法&quot; class=&quot;headerlink&quot; title=&quot;使用props调用组件的方法&quot;&gt;&lt;/a&gt;使用props调用组件的方法&lt;/h4&gt;&lt;p&gt;当用户提交一条评论后，需要刷新列表。这个逻辑应该在CommentBox中做完，因为CommentBox组件的state中含有列表的data.&lt;br&gt;我们需要将子组件的数据传递到父组件。方法如下：在CommentBox的render方法中为CommentForm添加&lt;code&gt;onCommentSubmit={this.handleCommentSubmit}&lt;/code&gt;方法，将handleCommentSubmit和onCommentSubmit方法绑定&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var CommentBox = React.createClass({
  loadCommentsFromServer: function() {
    $.ajax({
      url: this.props.url,
      dataType: &amp;apos;json&amp;apos;,
      cache: false,
      success: function(data) {
        this.setState({data: data});
      }.bind(this),
      error: function(xhr, status, err) {
        console.error(this.props.url, status, err.toString());
      }.bind(this)
    });
  },
  handleCommentSubmit: function(comment) {//添加新方法
    // TODO: 提交数据
  },
  getInitialState: function() {
    return {data: []};
  },
  componentDidMount: function() {
    this.loadCommentsFromServer();
    setInterval(this.loadCommentsFromServer, this.props.pollInterval);
  },
  render: function() {
    return (
      &amp;lt;div className=&amp;quot;commentBox&amp;quot;&amp;gt;
        &amp;lt;h1&amp;gt;Comments&amp;lt;/h1&amp;gt;
        &amp;lt;CommentList data={this.state.data} /&amp;gt;
        &amp;lt;CommentForm onCommentSubmit={this.handleCommentSubmit} /&amp;gt;//添加onCommentSubmit触发handleCommentSubmit
      &amp;lt;/div&amp;gt;
    );
  }
});  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;在CommentForm 的handleSubmit中调用&lt;code&gt;this.props.onCommentSubmit({author: author, text: text});&lt;/code&gt;触发handleCommentSubmit方法&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;var CommentForm = React.createClass({
  getInitialState: function() {
    return {author: &amp;apos;&amp;apos;, text: &amp;apos;&amp;apos;};
  },
  handleAuthorChange: function(e) {
    this.setState({author: e.target.value});
  },
  handleTextChange: function(e) {
    this.setState({text: e.target.value});
  },
  handleSubmit: function(e) {
    e.preventDefault();
    var author = this.state.author.trim();
    var text = this.state.text.trim();
    if (!text || !author) {
      return;
    }
    this.props.onCommentSubmit({author: author, text: text});//调用onCommentSubmit({author: author, text: text})
    this.setState({author: &amp;apos;&amp;apos;, text: &amp;apos;&amp;apos;});
  },
  render: function() {
    return (
      &amp;lt;form className=&amp;quot;commentForm&amp;quot; onSubmit={this.handleSubmit}&amp;gt;
        &amp;lt;input
          type=&amp;quot;text&amp;quot;
          placeholder=&amp;quot;Your name&amp;quot;
          value={this.state.author}
          onChange={this.handleAuthorChange}
        /&amp;gt;
        &amp;lt;input
          type=&amp;quot;text&amp;quot;
          placeholder=&amp;quot;Say something...&amp;quot;
          value={this.state.text}
          onChange={this.handleTextChange}
        /&amp;gt;
        &amp;lt;input type=&amp;quot;submit&amp;quot; value=&amp;quot;Post&amp;quot; /&amp;gt;
      &amp;lt;/form&amp;gt;
    );
  }
});  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;然后在CommentBox 的handleCommentSubmit方法中发送请求&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;handleCommentSubmit: function(comment) {
   $.ajax({
     url: this.props.url,
     dataType: &amp;apos;json&amp;apos;,
     type: &amp;apos;POST&amp;apos;,
     data: comment,
     success: function(data) {
       this.setState({data: data});
     }.bind(this),
     error: function(xhr, status, err) {
       console.error(this.props.url, status, err.toString());
     }.bind(this)
   });
 },    
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;提前更新&quot;&gt;&lt;a href=&quot;#提前更新&quot; class=&quot;headerlink&quot; title=&quot;提前更新&quot;&gt;&lt;/a&gt;提前更新&lt;/h4&gt;&lt;p&gt;到目前为止功能已经完成，但是当提交评论时，总会先发送请求，等请求成功后才显示数据，显示比较慢。&lt;br&gt;更改方法：在发送请求之前，将提交的评论添加到this.state.data中，然后调用 this.setState(),先将数据显示出来，在请求成功的回调用再次调用this.setState({data: data})，确保数据和服务器同步&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;handleCommentSubmit: function(comment) {
    var comments = this.state.data;
    // Optimistically set an id on the new comment. It will be replaced by an
    // id generated by the server. In a production application you would likely
    // not use Date.now() for this and would have a more robust system in place.
    comment.id = Date.now();
    var newComments = comments.concat([comment]);
    this.setState({data: newComments});
    $.ajax({
      url: this.props.url,
      dataType: &amp;apos;json&amp;apos;,
      type: &amp;apos;POST&amp;apos;,
      data: comment,
      success: function(data) {
        this.setState({data: data});
      }.bind(this),
      error: function(xhr, status, err) {
        this.setState({data: comments});
        console.error(this.props.url, status, err.toString());
      }.bind(this)
    });
  },
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&quot;参考&quot;&gt;&lt;a href=&quot;#参考&quot; class=&quot;headerlink&quot; title=&quot;参考&quot;&gt;&lt;/a&gt;参考&lt;/h3&gt;&lt;p&gt;&lt;a href=&quot;https://facebook.github.io/react/docs/tutorial.html&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://facebook.github.io/react/docs/tutorial.html&lt;/a&gt;&lt;/p&gt;
</content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h3&gt;&lt;p&gt;一个用于构建用户界面的JavaScript库&lt;br&gt;
    
    </summary>
    
    
      <category term="React" scheme="http://yoursite.com/tags/React/"/>
    
  </entry>
  
  <entry>
    <title>android_AppWidget</title>
    <link href="http://yoursite.com/2016/07/15/android-AppWidget/"/>
    <id>http://yoursite.com/2016/07/15/android-AppWidget/</id>
    <published>2016-07-15T06:02:10.000Z</published>
    <updated>2016-07-15T10:18:29.969Z</updated>
    
    <content type="html">&lt;p&gt;AppWidget就是我们平常在桌面上见到的那种一个个的小窗口，利用这个小窗口可以给用户提供一些方便快捷的操作。&lt;br&gt;&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;&lt;/p&gt;
&lt;h4 id=&quot;创建一个简单的AppWidget&quot;&gt;&lt;a href=&quot;#创建一个简单的AppWidget&quot; class=&quot;headerlink&quot; title=&quot;创建一个简单的AppWidget&quot;&gt;&lt;/a&gt;创建一个简单的AppWidget&lt;/h4&gt;&lt;ol&gt;
&lt;li&gt;&lt;p&gt;在res文件夹下新建一个名字为xml的文件夹，然后在xml目录下创建一个appwidget_info_info.xml文件&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;?xml version=&amp;quot;1.0&amp;quot; encoding=&amp;quot;utf-8&amp;quot;?&amp;gt;
&amp;lt;appwidget-provider xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;
    android:initialKeyguardLayout=&amp;quot;@layout/appwidget_info&amp;quot;
    android:initialLayout=&amp;quot;@layout/appwidget_info&amp;quot;
    android:minHeight=&amp;quot;40dp&amp;quot;
    android:minWidth=&amp;quot;250dp&amp;quot;
    android:previewImage=&amp;quot;@drawable/example_appwidget_preview&amp;quot;
    android:resizeMode=&amp;quot;horizontal|vertical&amp;quot;
    android:updatePeriodMillis=&amp;quot;86400000&amp;quot;
    android:widgetCategory=&amp;quot;home_screen&amp;quot;&amp;gt;&amp;lt;/appwidget-provider&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;
&lt;li&gt;&lt;p&gt;在layout文件夹下面新建一个appwidgetlayout.xml文件&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;RelativeLayout xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;
android:layout_width=&amp;quot;match_parent&amp;quot;
android:layout_height=&amp;quot;match_parent&amp;quot;
android:padding=&amp;quot;@dimen/widget_margin&amp;quot;&amp;gt;

    &amp;lt;TextView
        android:id=&amp;quot;@+id/appwidget_text&amp;quot;
        android:layout_width=&amp;quot;wrap_content&amp;quot;
        android:layout_height=&amp;quot;wrap_content&amp;quot;
        android:contentDescription=&amp;quot;@string/appwidget_text&amp;quot;
        android:text=&amp;quot;@string/appwidget_text&amp;quot;
        android:textSize=&amp;quot;24sp&amp;quot;
        android:textStyle=&amp;quot;bold|italic&amp;quot;
        android:layout_marginLeft=&amp;quot;8dp&amp;quot;
        android:layout_marginRight=&amp;quot;8dp&amp;quot;
        android:layout_alignParentTop=&amp;quot;true&amp;quot;
        android:layout_alignParentStart=&amp;quot;true&amp;quot; /&amp;gt;

    &amp;lt;Button
        android:layout_width=&amp;quot;wrap_content&amp;quot;
        android:layout_height=&amp;quot;wrap_content&amp;quot;
        android:text=&amp;quot;@string/btn_name&amp;quot;
        android:id=&amp;quot;@+id/button&amp;quot;
        android:layout_below=&amp;quot;@+id/appwidget_text&amp;quot;
        android:layout_alignStart=&amp;quot;@+id/appwidget_text&amp;quot; /&amp;gt;

&amp;lt;/RelativeLayout&amp;gt;  
&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;
&lt;li&gt;&lt;p&gt;继承AppWidgetProvider类&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public class appwidgetInfo extends AppWidgetProvider {

    private  static  String broadCastString = &amp;quot;com.caokai.broadCastString&amp;quot;;
    static void updateAppWidget(Context context, AppWidgetManager appWidgetManager,
                                int appWidgetId) {
        //发送广播的PendingIntent
        Intent intent = new Intent();
        intent.setAction(broadCastString);
        PendingIntent pendingIntent = PendingIntent.getBroadcast(context,0,intent,0);

        //启动activity的PendingIntent
        Intent activityIntent =  new Intent(context,MainActivity.class);
        PendingIntent pendingIntent1 = PendingIntent.getActivity(context,0,activityIntent,0);

        CharSequence widgetText = &amp;quot;test&amp;quot;;
        // 获取RemoteViews
        RemoteViews views = new RemoteViews(context.getPackageName(), R.layout.appwidget_info);
        views.setTextViewText(R.id.appwidget_text, widgetText);

        //设置点击事件
        views.setOnClickPendingIntent(R.id.button,pendingIntent);
        //设置点击事件
        views.setOnClickPendingIntent(R.id.appwidget_text,pendingIntent1);

        // Instruct the widget manager to update the widget
        appWidgetManager.updateAppWidget(appWidgetId, views);
    }

    @Override
    public void onUpdate(Context context, AppWidgetManager appWidgetManager, int[] appWidgetIds) {
        // There may be multiple widgets active, so update all of them
        for (int appWidgetId : appWidgetIds) {
            updateAppWidget(context, appWidgetManager, appWidgetId);
        }
    }

    @Override
    public void onEnabled(Context context) {
        // Enter relevant functionality for when the first widget is created
    }

    @Override
    public void onDisabled(Context context) {
        // Enter relevant functionality for when the last widget is disabled
    }

    @Override
    public void onReceive(Context context, Intent intent) {
        if(intent.getAction().equals(broadCastString)){
            RemoteViews remoteViews = new RemoteViews(context.getPackageName(),R.layout.appwidget_info);
            remoteViews.setTextViewText(R.id.appwidget_text,&amp;quot;点击到了&amp;quot;);

            AppWidgetManager appWidgetManager =  AppWidgetManager.getInstance(context);

            ComponentName componentName = new ComponentName(context,appwidgetInfo.class);
            appWidgetManager.updateAppWidget(componentName,remoteViews);
        }else{
            super.onReceive(context, intent);
        }

    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;
&lt;li&gt;&lt;p&gt;AndroidManifest.xml中申明&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;?xml version=&amp;quot;1.0&amp;quot; encoding=&amp;quot;utf-8&amp;quot;?&amp;gt;
&amp;lt;manifest xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;
    package=&amp;quot;caokai.com.appwidgettest&amp;quot;&amp;gt;

    &amp;lt;application
        android:allowBackup=&amp;quot;true&amp;quot;
        android:icon=&amp;quot;@mipmap/ic_launcher&amp;quot;
        android:label=&amp;quot;@string/app_name&amp;quot;
        android:supportsRtl=&amp;quot;true&amp;quot;
        android:theme=&amp;quot;@style/AppTheme&amp;quot;&amp;gt;
        &amp;lt;activity android:name=&amp;quot;.MainActivity&amp;quot;&amp;gt;
            &amp;lt;intent-filter&amp;gt;
                &amp;lt;action android:name=&amp;quot;android.intent.action.MAIN&amp;quot; /&amp;gt;

                &amp;lt;category android:name=&amp;quot;android.intent.category.LAUNCHER&amp;quot; /&amp;gt;
            &amp;lt;/intent-filter&amp;gt;
        &amp;lt;/activity&amp;gt;

        &amp;lt;receiver android:name=&amp;quot;.appwidgetInfo&amp;quot;&amp;gt;
            &amp;lt;intent-filter&amp;gt;
                &amp;lt;action android:name=&amp;quot;android.appwidget.action.APPWIDGET_UPDATE&amp;quot; /&amp;gt;
            &amp;lt;/intent-filter&amp;gt;
            &amp;lt;intent-filter&amp;gt;
                &amp;lt;action android:name=&amp;quot;com.caokai.broadCastString&amp;quot; /&amp;gt;
            &amp;lt;/intent-filter&amp;gt;

            &amp;lt;meta-data
                android:name=&amp;quot;android.appwidget.provider&amp;quot;
                android:resource=&amp;quot;@xml/appwidget_info_info&amp;quot; /&amp;gt;
        &amp;lt;/receiver&amp;gt;
        &amp;lt;receiver android:name=&amp;quot;.NewAppWidget&amp;quot;&amp;gt;
            &amp;lt;intent-filter&amp;gt;
                &amp;lt;action android:name=&amp;quot;android.appwidget.action.APPWIDGET_UPDATE&amp;quot; /&amp;gt;
            &amp;lt;/intent-filter&amp;gt;

            &amp;lt;meta-data
                android:name=&amp;quot;android.appwidget.provider&amp;quot;
                android:resource=&amp;quot;@xml/new_app_widget_info&amp;quot; /&amp;gt;
        &amp;lt;/receiver&amp;gt;
    &amp;lt;/application&amp;gt;

&amp;lt;/manifest&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;以上文件可在android studio中通过  New-&amp;gt;Widget-&amp;gt;AppWidget来自动生成&lt;/p&gt;
&lt;h4 id=&quot;AppWidget与客户端程序交互&quot;&gt;&lt;a href=&quot;#AppWidget与客户端程序交互&quot; class=&quot;headerlink&quot; title=&quot;AppWidget与客户端程序交互&quot;&gt;&lt;/a&gt;AppWidget与客户端程序交互&lt;/h4&gt;&lt;ol&gt;
&lt;li&gt;&lt;p&gt;RemoteViews&lt;br&gt;因为appwidget运行的进程和我们创建的应用不在一个进程中，所以我们也就不能像平常引用控件那样来获得控件的实例。这个时候RemoteViews出场了。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;RemoteViews remoteViews  = new RemoteViews(context.getPackageName(),R.layout.appwidgetlayout);

remoteViews.setOnClickPendingIntent(R.id.btnSend, pendingIntent);  //为小工具上的按钮绑定事件
&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;
&lt;li&gt;&lt;p&gt;pendingIntent&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;//创建一个Intent对象
Intent intent = new Intent();
intent.setAction(broadCastString);

//这一步相当于写信，说明这个信的作用到底是什么，在这里表示将发送一个广播
PendingIntent pendingIntent = PendingIntent.getBroadcast(context, 0, intent, 0);
&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;
&lt;li&gt;&lt;p&gt;在onUpdate()方法中绑定事件&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;/**
* 到达指定的更新时间或者当用户向桌面添加AppWidget时被调用
* */
@Override
public void onUpdate(Context context, AppWidgetManager appWidgetManager,
        int[] appWidgetIds)
        {

                //创建一个Intent对象
                Intent intent = new Intent();
                intent.setAction(broadCastString);

                //设置pendingIntent的作用
                PendingIntent pendingIntent = PendingIntent.getBroadcast(context, 0, intent, 0);
                RemoteViews remoteViews  = new RemoteViews(context.getPackageName(),R.layout.appwidgetlayout);

                //绑定事件
                remoteViews.setOnClickPendingIntent(R.id.btnSend, pendingIntent);

                //更新Appwidget
                appWidgetManager.updateAppWidget(appWidgetIds, remoteViews);
        }  
&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;
&lt;li&gt;&lt;p&gt;在onReceive()方法中更新appwidget&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;/**
 * 接受广播事件
 * */
@Override
public void onReceive(Context context, Intent intent)
{
    if (intent.getAction().equals(broadCastString))
    {               
        //只能通过远程对象来设置appwidget中的控件状态
        RemoteViews remoteViews  = new RemoteViews(context.getPackageName(),R.layout.appwidgetlayout);

       //通过远程对象将按钮的文字设置为”hihi”
        remoteViews.setTextViewText(R.id.btnSend, &amp;quot;hihi&amp;quot;);   

        //获得appwidget管理实例，用于管理appwidget以便进行更新操作
        AppWidgetManager appWidgetManager = AppWidgetManager.getInstance(context);

        //相当于获得所有本程序创建的appwidget
        ComponentName componentName = new ComponentName(context,AppWidget.class);

        //更新appwidget
        appWidgetManager.updateAppWidget(componentName, remoteViews);
    }else{
        super.onReceive(context, intent);
    }

}
&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;代码：&lt;a href=&quot;/uploads/AppWidgetTest.zip&quot;&gt;AppWidgetTest.zip&lt;/a&gt;&lt;/p&gt;
</content>
    
    <summary type="html">
    
      &lt;p&gt;AppWidget就是我们平常在桌面上见到的那种一个个的小窗口，利用这个小窗口可以给用户提供一些方便快捷的操作。&lt;br&gt;
    
    </summary>
    
    
      <category term="android" scheme="http://yoursite.com/tags/android/"/>
    
  </entry>
  
  <entry>
    <title>android_拍照</title>
    <link href="http://yoursite.com/2016/07/14/android-%E6%8B%8D%E7%85%A7/"/>
    <id>http://yoursite.com/2016/07/14/android-拍照/</id>
    <published>2016-07-14T07:51:54.000Z</published>
    <updated>2016-07-18T14:37:59.769Z</updated>
    
    <content type="html">&lt;h4 id=&quot;拍照&quot;&gt;&lt;a href=&quot;#拍照&quot; class=&quot;headerlink&quot; title=&quot;拍照&quot;&gt;&lt;/a&gt;拍照&lt;/h4&gt;&lt;h5 id=&quot;请求相机权限&quot;&gt;&lt;a href=&quot;#请求相机权限&quot; class=&quot;headerlink&quot; title=&quot;请求相机权限&quot;&gt;&lt;/a&gt;请求相机权限&lt;/h5&gt;&lt;p&gt;如果拍照是app的一个必要的功能，可以在Google Play上设置为只对有相机的设备可见。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;manifest ... &amp;gt;
    &amp;lt;uses-feature android:name=&amp;quot;android.hardware.camera&amp;quot;
                  android:required=&amp;quot;true&amp;quot; /&amp;gt;
    ...
&amp;lt;/manifest&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;如果相机功能不是必须的，则android:required=”false”,然后调用&lt;code&gt;hasSystemFeature(PackageManager.FEATURE_CAMERA)&lt;/code&gt;来判断相机是否可用&lt;/p&gt;
&lt;h5 id=&quot;获得照片通过相机app&quot;&gt;&lt;a href=&quot;#获得照片通过相机app&quot; class=&quot;headerlink&quot; title=&quot;获得照片通过相机app&quot;&gt;&lt;/a&gt;获得照片通过相机app&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;static final int REQUEST_IMAGE_CAPTURE = 1;

private void dispatchTakePictureIntent() {
    Intent takePictureIntent = new Intent(MediaStore.ACTION_IMAGE_CAPTURE);
    if (takePictureIntent.resolveActivity(getPackageManager()) != null) {
        startActivityForResult(takePictureIntent, REQUEST_IMAGE_CAPTURE);
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;startActivityForResult()方法被resolveActivity()（该方法返回能处理Intent的第一个Activity组件）方法保护了起来&lt;/p&gt;
&lt;h5 id=&quot;获得缩略图&quot;&gt;&lt;a href=&quot;#获得缩略图&quot; class=&quot;headerlink&quot; title=&quot;获得缩略图&quot;&gt;&lt;/a&gt;获得缩略图&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;@Override
protected void onActivityResult(int requestCode, int resultCode, Intent data) {
    if (requestCode == REQUEST_IMAGE_CAPTURE &amp;amp;&amp;amp; resultCode == RESULT_OK) {
        Bundle extras = data.getExtras();
        Bitmap imageBitmap = (Bitmap) extras.get(&amp;quot;data&amp;quot;);
        mImageView.setImageBitmap(imageBitmap);
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;保存全尺寸照片&quot;&gt;&lt;a href=&quot;#保存全尺寸照片&quot; class=&quot;headerlink&quot; title=&quot;保存全尺寸照片&quot;&gt;&lt;/a&gt;保存全尺寸照片&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;&amp;lt;manifest ...&amp;gt;
    &amp;lt;uses-permission android:name=&amp;quot;android.permission.WRITE_EXTERNAL_STORAGE&amp;quot; /&amp;gt;
    ...
&amp;lt;/manifest&amp;gt;  
&lt;/code&gt;&lt;/pre&gt;&lt;hr&gt;
&lt;pre&gt;&lt;code&gt;String mCurrentPhotoPath;

private File createImageFile() throws IOException {
    // Create an image file name
    String timeStamp = new SimpleDateFormat(&amp;quot;yyyyMMdd_HHmmss&amp;quot;).format(new Date());
    String imageFileName = &amp;quot;JPEG_&amp;quot; + timeStamp + &amp;quot;_&amp;quot;;
    File storageDir = getExternalFilesDir(Environment.DIRECTORY_PICTURES);
    File image = File.createTempFile(
        imageFileName,  /* prefix */
        &amp;quot;.jpg&amp;quot;,         /* suffix */
        storageDir      /* directory */
    );

    // Save a file: path for use with ACTION_VIEW intents
    mCurrentPhotoPath = &amp;quot;file:&amp;quot; + image.getAbsolutePath();
    return image;
}
&lt;/code&gt;&lt;/pre&gt;&lt;hr&gt;
&lt;pre&gt;&lt;code&gt;static final int REQUEST_TAKE_PHOTO = 1;

private void dispatchTakePictureIntent() {
    Intent takePictureIntent = new Intent(MediaStore.ACTION_IMAGE_CAPTURE);
    // Ensure that there&amp;apos;s a camera activity to handle the intent
    if (takePictureIntent.resolveActivity(getPackageManager()) != null) {
        // Create the File where the photo should go
        File photoFile = null;
        try {
            photoFile = createImageFile();
        } catch (IOException ex) {
            // Error occurred while creating the File
            ...
        }
        // Continue only if the File was successfully created
        if (photoFile != null) {
            Uri photoURI = FileProvider.getUriForFile(this,
                                                  &amp;quot;com.example.android.fileprovider&amp;quot;,
                                                  photoFile);
            takePictureIntent.putExtra(MediaStore.EXTRA_OUTPUT, photoURI);
            startActivityForResult(takePictureIntent, REQUEST_TAKE_PHOTO);
        }
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;将图片添加到画廊&quot;&gt;&lt;a href=&quot;#将图片添加到画廊&quot; class=&quot;headerlink&quot; title=&quot;将图片添加到画廊&quot;&gt;&lt;/a&gt;将图片添加到画廊&lt;/h5&gt;&lt;h5 id=&quot;解码缩放的图片&quot;&gt;&lt;a href=&quot;#解码缩放的图片&quot; class=&quot;headerlink&quot; title=&quot;解码缩放的图片&quot;&gt;&lt;/a&gt;解码缩放的图片&lt;/h5&gt;&lt;h4 id=&quot;录像&quot;&gt;&lt;a href=&quot;#录像&quot; class=&quot;headerlink&quot; title=&quot;录像&quot;&gt;&lt;/a&gt;录像&lt;/h4&gt;&lt;h4 id=&quot;控制摄像头&quot;&gt;&lt;a href=&quot;#控制摄像头&quot; class=&quot;headerlink&quot; title=&quot;控制摄像头&quot;&gt;&lt;/a&gt;控制摄像头&lt;/h4&gt;</content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;拍照&quot;&gt;&lt;a href=&quot;#拍照&quot; class=&quot;headerlink&quot; title=&quot;拍照&quot;&gt;&lt;/a&gt;拍照&lt;/h4&gt;&lt;h5 id=&quot;请求相机权限&quot;&gt;&lt;a href=&quot;#请求相机权限&quot; class=&quot;headerlink&quot; title=&quot;请求相机权限&quot;&gt;&lt;/a&gt;请
    
    </summary>
    
    
      <category term="android" scheme="http://yoursite.com/tags/android/"/>
    
  </entry>
  
  <entry>
    <title>SAX解析xml</title>
    <link href="http://yoursite.com/2016/07/06/SAX%E8%A7%A3%E6%9E%90xml/"/>
    <id>http://yoursite.com/2016/07/06/SAX解析xml/</id>
    <published>2016-07-06T14:24:26.000Z</published>
    <updated>2016-07-14T11:52:40.978Z</updated>
    
    <content type="html">&lt;p&gt;常见的xml解析方式有ＤＯＭ解析和ＳＡＸ解析&lt;br&gt;&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;&lt;br&gt;DOM（文件对象模型）解析：解析器读入整个文档，然后构建一个驻留内存的树结构，然后代码就可以根据DOM接口来操作这个树结构了。  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;优点：整个文档读入内存，方便操作：支持修改、删除和重现排列等多种功能。
缺点：将整个文档读入内存中，保留了过多的不需要的节点，浪费内存和空间。
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;为了解决DOM解析存在的问题，就出现了SAX解析&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;优点：不用实现调入整个文档，占用资源少。尤其在嵌入式环境中，如android，极力推荐使用SAX解析。
缺点：不像DOM解析一样将文档长期驻留在内存中，数据不是持久的。如果事件过后没有保存数据，数据就会丢失。
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;使用方法：  &lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;先由SAXParserFactory这个工厂的实例生产一个SAXParser解析器；&lt;/li&gt;
&lt;li&gt;然后根据读取的xml路径，传递给SAXParser这个解析器，再调用parse()方法；&lt;/li&gt;
&lt;li&gt;在parse()方法中需要传递DefaultHandler这个类的扩展类的实例，因为它才会真正去一步步去解析xml文档的；&lt;/li&gt;
&lt;li&gt;在DefaultHandler扩展类中需要重写startDocument()，endDocument()，startElement（），endElement（），characters（）等等方法，因为他们方法内部有返回的具体文档的结果。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;具体代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;File file = new File(Environment.getExternalStorageDirectory()+&amp;quot;/channel.xml&amp;quot;);
InputStream inputStream = new FileInputStream(file) ;
SAXParserFactory factory = SAXParserFactory.newInstance();
SAXParser parser = factory.newSAXParser();
SAXHeaper handler = new SAXHeaper();
parser.parse(inputStream,handler);
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;SAXHeaper类（继承DefaultHandler类）：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import org.xml.sax.helpers.DefaultHandler;

import java.util.ArrayList;

/**
 * Created by ck on 2016/7/6.
 */
public class SAXHeaper extends DefaultHandler {
    private String TagName;
    private channel channel;
    private ArrayList&amp;lt;channel&amp;gt; channels;
    //解析开始
    @Override
    public void startDocument() throws SAXException {
        channels = new ArrayList&amp;lt;channel&amp;gt;();
    }
    //解析结束
    @Override
    public void endDocument() throws SAXException {
        Log.i(&amp;quot;SAXHeaper&amp;quot;,channels.toString());
    }
    //标签开始
    @Override
    public void startElement(String uri, String localName, String qName, Attributes attributes) throws SAXException {
        TagName = localName;
        if(localName.equals(&amp;quot;item&amp;quot;)){
            channel = new channel();
        }
    }
    //标签结束
    @Override
    public void endElement(String uri, String localName, String qName) throws SAXException {
        TagName = &amp;quot;&amp;quot;;
        if(localName.equals(&amp;quot;item&amp;quot;)){
            channels.add(channel);
            channel = null;
        }

    }
    //标签中的内容
    @Override
    public void characters(char[] ch, int start, int length) throws SAXException {
        String chars = new String(ch,start,length);
        if(TagName.equals(&amp;quot;title&amp;quot;)){
            channel.setTitle(chars);
        }else if(TagName.equals(&amp;quot;link&amp;quot;)){
            channel.setLink(chars);
        }else if(TagName.equals(&amp;quot;category&amp;quot;)){
            channel.setCategory(chars);
        }else if(TagName.equals(&amp;quot;description&amp;quot;)){
            channel.setDescription(chars);
        }else if(TagName.equals(&amp;quot;pubDate&amp;quot;)){
            channel.setPubDate(chars);
        }
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;代码：&lt;a href=&quot;/uploads/SAXTest.zip&quot;&gt;SAXTest.zip&lt;/a&gt;&lt;/p&gt;
</content>
    
    <summary type="html">
    
      &lt;p&gt;常见的xml解析方式有ＤＯＭ解析和ＳＡＸ解析&lt;br&gt;
    
    </summary>
    
    
      <category term="xml" scheme="http://yoursite.com/tags/xml/"/>
    
  </entry>
  
  <entry>
    <title>android_支持各种屏幕尺寸</title>
    <link href="http://yoursite.com/2016/07/02/android-%E6%94%AF%E6%8C%81%E5%90%84%E7%A7%8D%E5%B1%8F%E5%B9%95%E5%B0%BA%E5%AF%B8/"/>
    <id>http://yoursite.com/2016/07/02/android-支持各种屏幕尺寸/</id>
    <published>2016-07-02T01:44:09.000Z</published>
    <updated>2016-07-05T08:40:53.485Z</updated>
    
    <content type="html">&lt;h4 id=&quot;使用”wrap-content”和”match-parent”&quot;&gt;&lt;a href=&quot;#使用”wrap-content”和”match-parent”&quot; class=&quot;headerlink&quot; title=&quot;使用”wrap_content”和”match_parent”&quot;&gt;&lt;/a&gt;使用”wrap_content”和”match_parent”&lt;/h4&gt;&lt;p&gt;如果使用 “wrap_content” 和 “match_parent” 尺寸值而不是硬编码的尺寸，您的视图就会相应地仅使用自身所需的空间或展开以填满可用空间。&lt;/p&gt;
&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;
&lt;p&gt;例如：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;LinearLayout xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;
android:orientation=&amp;quot;vertical&amp;quot;
android:layout_width=&amp;quot;match_parent&amp;quot;
android:layout_height=&amp;quot;match_parent&amp;quot;&amp;gt;
    &amp;lt;LinearLayout android:layout_width=&amp;quot;match_parent&amp;quot; 
                  android:id=&amp;quot;@+id/linearLayout1&amp;quot;  
                  android:gravity=&amp;quot;center&amp;quot;
                  android:layout_height=&amp;quot;50dp&amp;quot;&amp;gt;
        &amp;lt;ImageView android:id=&amp;quot;@+id/imageView1&amp;quot; 
                   android:layout_height=&amp;quot;wrap_content&amp;quot;
                   android:layout_width=&amp;quot;wrap_content&amp;quot;
                   android:src=&amp;quot;@drawable/logo&amp;quot;
                   android:paddingRight=&amp;quot;30dp&amp;quot;
                   android:layout_gravity=&amp;quot;left&amp;quot;
                   android:layout_weight=&amp;quot;0&amp;quot; /&amp;gt;
        &amp;lt;View android:layout_height=&amp;quot;wrap_content&amp;quot; 
              android:id=&amp;quot;@+id/view1&amp;quot;
              android:layout_width=&amp;quot;wrap_content&amp;quot;
              android:layout_weight=&amp;quot;1&amp;quot; /&amp;gt;
        &amp;lt;Button android:id=&amp;quot;@+id/categorybutton&amp;quot;
                android:background=&amp;quot;@drawable/button_bg&amp;quot;
                android:layout_height=&amp;quot;match_parent&amp;quot;
                android:layout_weight=&amp;quot;0&amp;quot;
                android:layout_width=&amp;quot;120dp&amp;quot;
                style=&amp;quot;@style/CategoryButtonStyle&amp;quot;/&amp;gt;
    &amp;lt;/LinearLayout&amp;gt;

    &amp;lt;fragment android:id=&amp;quot;@+id/headlines&amp;quot; 
              android:layout_height=&amp;quot;fill_parent&amp;quot;
              android:name=&amp;quot;com.example.android.newsreader.HeadlinesFragment&amp;quot;
              android:layout_width=&amp;quot;match_parent&amp;quot; /&amp;gt;
&amp;lt;/LinearLayout&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;使用相对布局&quot;&gt;&lt;a href=&quot;#使用相对布局&quot; class=&quot;headerlink&quot; title=&quot;使用相对布局&quot;&gt;&lt;/a&gt;使用相对布局&lt;/h4&gt;&lt;p&gt;RelativeLayout        &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;?xml version=&amp;quot;1.0&amp;quot; encoding=&amp;quot;utf-8&amp;quot;?&amp;gt;
&amp;lt;RelativeLayout xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;
    android:layout_width=&amp;quot;match_parent&amp;quot;
    android:layout_height=&amp;quot;match_parent&amp;quot;&amp;gt;
    &amp;lt;TextView
        android:id=&amp;quot;@+id/label&amp;quot;
        android:layout_width=&amp;quot;match_parent&amp;quot;
        android:layout_height=&amp;quot;wrap_content&amp;quot;
        android:text=&amp;quot;Type here:&amp;quot;/&amp;gt;
    &amp;lt;EditText
        android:id=&amp;quot;@+id/entry&amp;quot;
        android:layout_width=&amp;quot;match_parent&amp;quot;
        android:layout_height=&amp;quot;wrap_content&amp;quot;
        android:layout_below=&amp;quot;@id/label&amp;quot;/&amp;gt;
    &amp;lt;Button
        android:id=&amp;quot;@+id/ok&amp;quot;
        android:layout_width=&amp;quot;wrap_content&amp;quot;
        android:layout_height=&amp;quot;wrap_content&amp;quot;
        android:layout_below=&amp;quot;@id/entry&amp;quot;
        android:layout_alignParentRight=&amp;quot;true&amp;quot;
        android:layout_marginLeft=&amp;quot;10dp&amp;quot;
        android:text=&amp;quot;OK&amp;quot; /&amp;gt;
    &amp;lt;Button
        android:layout_width=&amp;quot;wrap_content&amp;quot;
        android:layout_height=&amp;quot;wrap_content&amp;quot;
        android:layout_toLeftOf=&amp;quot;@id/ok&amp;quot;
        android:layout_alignTop=&amp;quot;@id/ok&amp;quot;
        android:text=&amp;quot;Cancel&amp;quot; /&amp;gt;
&amp;lt;/RelativeLayout&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;使用尺寸限定符&quot;&gt;&lt;a href=&quot;#使用尺寸限定符&quot; class=&quot;headerlink&quot; title=&quot;使用尺寸限定符&quot;&gt;&lt;/a&gt;使用尺寸限定符&lt;/h4&gt;&lt;p&gt;在较大的屏幕上实施’双面板’模式,小屏幕上显示’单面板’模式&lt;br&gt;res/layout/main.xml，单面板（默认）布局：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;LinearLayout xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;
android:orientation=&amp;quot;vertical&amp;quot;
android:layout_width=&amp;quot;match_parent&amp;quot;
android:layout_height=&amp;quot;match_parent&amp;quot;&amp;gt;

    &amp;lt;fragment android:id=&amp;quot;@+id/headlines&amp;quot;
              android:layout_height=&amp;quot;fill_parent&amp;quot;
              android:name=&amp;quot;com.example.android.newsreader.HeadlinesFragment&amp;quot;
              android:layout_width=&amp;quot;match_parent&amp;quot; /&amp;gt;
&amp;lt;/LinearLayout&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;res/layout-large/main.xml，双面板布局：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;LinearLayout xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;
android:layout_width=&amp;quot;fill_parent&amp;quot;
android:layout_height=&amp;quot;fill_parent&amp;quot;
android:orientation=&amp;quot;horizontal&amp;quot;&amp;gt;
    &amp;lt;fragment android:id=&amp;quot;@+id/headlines&amp;quot;
              android:layout_height=&amp;quot;fill_parent&amp;quot;
              android:name=&amp;quot;com.example.android.newsreader.HeadlinesFragment&amp;quot;
              android:layout_width=&amp;quot;400dp&amp;quot;
              android:layout_marginRight=&amp;quot;10dp&amp;quot;/&amp;gt;
    &amp;lt;fragment android:id=&amp;quot;@+id/article&amp;quot;
              android:layout_height=&amp;quot;fill_parent&amp;quot;
              android:name=&amp;quot;com.example.android.newsreader.ArticleFragment&amp;quot;
              android:layout_width=&amp;quot;fill_parent&amp;quot; /&amp;gt;
&amp;lt;/LinearLayout&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;使用最小宽度限定符&quot;&gt;&lt;a href=&quot;#使用最小宽度限定符&quot; class=&quot;headerlink&quot; title=&quot;使用最小宽度限定符&quot;&gt;&lt;/a&gt;使用最小宽度限定符&lt;/h4&gt;&lt;p&gt;对于最小宽度大于等于 600 dp 的设备，系统会选择 layout-sw600dp/main.xml（双面板）布局，否则系统就会选择 layout/main.xml（单面板）布局&lt;br&gt;res/layout/main.xml，单面板（默认）布局：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;LinearLayout xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;
android:orientation=&amp;quot;vertical&amp;quot;
android:layout_width=&amp;quot;match_parent&amp;quot;
android:layout_height=&amp;quot;match_parent&amp;quot;&amp;gt;

    &amp;lt;fragment android:id=&amp;quot;@+id/headlines&amp;quot;
              android:layout_height=&amp;quot;fill_parent&amp;quot;
              android:name=&amp;quot;com.example.android.newsreader.HeadlinesFragment&amp;quot;
              android:layout_width=&amp;quot;match_parent&amp;quot; /&amp;gt;
&amp;lt;/LinearLayout&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;res/layout-sw600dp/main.xml，双面板布局：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;LinearLayout xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;
android:layout_width=&amp;quot;fill_parent&amp;quot;
android:layout_height=&amp;quot;fill_parent&amp;quot;
android:orientation=&amp;quot;horizontal&amp;quot;&amp;gt;
    &amp;lt;fragment android:id=&amp;quot;@+id/headlines&amp;quot;
              android:layout_height=&amp;quot;fill_parent&amp;quot;
              android:name=&amp;quot;com.example.android.newsreader.HeadlinesFragment&amp;quot;
              android:layout_width=&amp;quot;400dp&amp;quot;
              android:layout_marginRight=&amp;quot;10dp&amp;quot;/&amp;gt;
    &amp;lt;fragment android:id=&amp;quot;@+id/article&amp;quot;
              android:layout_height=&amp;quot;fill_parent&amp;quot;
              android:name=&amp;quot;com.example.android.newsreader.ArticleFragment&amp;quot;
              android:layout_width=&amp;quot;fill_parent&amp;quot; /&amp;gt;
&amp;lt;/LinearLayout&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;但 Android 版本低于 3.2 的设备不支持此技术，原因是这些设备无法将 sw600dp 识别为尺寸限定符，因此您仍需使用 large 限定符。这样一来，就会有一个名称为 res/layout-large/main.xml 的文件（与 res/layout-sw600dp/main.xml 一样）。下一教程中了解到避免此类布局文件出现重复的技术&lt;/p&gt;
&lt;h4 id=&quot;使用布局别名&quot;&gt;&lt;a href=&quot;#使用布局别名&quot; class=&quot;headerlink&quot; title=&quot;使用布局别名&quot;&gt;&lt;/a&gt;使用布局别名&lt;/h4&gt;&lt;ul&gt;
&lt;li&gt;res/layout/main.xml: 单面板布局&lt;/li&gt;
&lt;li&gt;res/layout-large: 多面板布局&lt;/li&gt;
&lt;li&gt;res/layout-sw600dp: 多面板布局&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;后两个文件是相同的，因为其中一个用于和 Android 3.2 设备匹配，而另一个则是为使用较低版本 Android 的平板电脑和电视准备的。&lt;br&gt;为避免文件重复出现，可以使用别名。例如：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;res/layout/main.xml，单面板布局&lt;/li&gt;
&lt;li&gt;res/layout/main_twopanes.xml，双面板布局&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;然后添加两个文件&lt;br&gt;res/values-large/layout.xml:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;resources&amp;gt;
    &amp;lt;item name=&amp;quot;main&amp;quot; type=&amp;quot;layout&amp;quot;&amp;gt;@layout/main_twopanes&amp;lt;/item&amp;gt;
&amp;lt;/resources&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;res/values-sw600dp/layout.xml:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;resources&amp;gt;
    &amp;lt;item name=&amp;quot;main&amp;quot; type=&amp;quot;layout&amp;quot;&amp;gt;@layout/main_twopanes&amp;lt;/item&amp;gt;
&amp;lt;/resources&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;使用屏幕方向限定符&quot;&gt;&lt;a href=&quot;#使用屏幕方向限定符&quot; class=&quot;headerlink&quot; title=&quot;使用屏幕方向限定符&quot;&gt;&lt;/a&gt;使用屏幕方向限定符&lt;/h4&gt;&lt;p&gt;某些布局会同时支持横向模式和纵向模式，可以通过调整优化其中大部分布局的效果。    &lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;小屏幕，纵向：单面板，带徽标&lt;/li&gt;
&lt;li&gt;小屏幕，横向：单面板，带徽标&lt;/li&gt;
&lt;li&gt;7 英寸平板电脑，纵向：单面板，带操作栏&lt;/li&gt;
&lt;li&gt;7 英寸平板电脑，横向：双面板，宽，带操作栏&lt;/li&gt;
&lt;li&gt;10 英寸平板电脑，纵向：双面板，窄，带操作栏&lt;/li&gt;
&lt;li&gt;10 英寸平板电脑，横向：双面板，宽，带操作栏&lt;/li&gt;
&lt;li&gt;电视，横向：双面板，宽，带操作栏&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;res/layout/onepane.xml:&lt;br&gt;    &lt;linearlayout xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot; android:orientation=&quot;vertical&quot; android:layout_width=&quot;match_parent&quot; android:layout_height=&quot;match_parent&quot;&gt;&lt;/linearlayout&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;    &amp;lt;fragment android:id=&amp;quot;@+id/headlines&amp;quot;
              android:layout_height=&amp;quot;fill_parent&amp;quot;
              android:name=&amp;quot;com.example.android.newsreader.HeadlinesFragment&amp;quot;
              android:layout_width=&amp;quot;match_parent&amp;quot; /&amp;gt;
&amp;lt;/LinearLayout&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;res/layout/onepane_with_bar.xml:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;LinearLayout xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;
android:orientation=&amp;quot;vertical&amp;quot;
android:layout_width=&amp;quot;match_parent&amp;quot;
android:layout_height=&amp;quot;match_parent&amp;quot;&amp;gt;
    &amp;lt;LinearLayout android:layout_width=&amp;quot;match_parent&amp;quot; 
                  android:id=&amp;quot;@+id/linearLayout1&amp;quot;  
                  android:gravity=&amp;quot;center&amp;quot;
                  android:layout_height=&amp;quot;50dp&amp;quot;&amp;gt;
        &amp;lt;ImageView android:id=&amp;quot;@+id/imageView1&amp;quot; 
                   android:layout_height=&amp;quot;wrap_content&amp;quot;
                   android:layout_width=&amp;quot;wrap_content&amp;quot;
                   android:src=&amp;quot;@drawable/logo&amp;quot;
                   android:paddingRight=&amp;quot;30dp&amp;quot;
                   android:layout_gravity=&amp;quot;left&amp;quot;
                   android:layout_weight=&amp;quot;0&amp;quot; /&amp;gt;
        &amp;lt;View android:layout_height=&amp;quot;wrap_content&amp;quot; 
              android:id=&amp;quot;@+id/view1&amp;quot;
              android:layout_width=&amp;quot;wrap_content&amp;quot;
              android:layout_weight=&amp;quot;1&amp;quot; /&amp;gt;
        &amp;lt;Button android:id=&amp;quot;@+id/categorybutton&amp;quot;
                android:background=&amp;quot;@drawable/button_bg&amp;quot;
                android:layout_height=&amp;quot;match_parent&amp;quot;
                android:layout_weight=&amp;quot;0&amp;quot;
                android:layout_width=&amp;quot;120dp&amp;quot;
                style=&amp;quot;@style/CategoryButtonStyle&amp;quot;/&amp;gt;
    &amp;lt;/LinearLayout&amp;gt;

    &amp;lt;fragment android:id=&amp;quot;@+id/headlines&amp;quot; 
              android:layout_height=&amp;quot;fill_parent&amp;quot;
              android:name=&amp;quot;com.example.android.newsreader.HeadlinesFragment&amp;quot;
              android:layout_width=&amp;quot;match_parent&amp;quot; /&amp;gt;
&amp;lt;/LinearLayout&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;res/layout/twopanes.xml:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;LinearLayout xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;
android:layout_width=&amp;quot;fill_parent&amp;quot;
android:layout_height=&amp;quot;fill_parent&amp;quot;
android:orientation=&amp;quot;horizontal&amp;quot;&amp;gt;
    &amp;lt;fragment android:id=&amp;quot;@+id/headlines&amp;quot;
              android:layout_height=&amp;quot;fill_parent&amp;quot;
              android:name=&amp;quot;com.example.android.newsreader.HeadlinesFragment&amp;quot;
              android:layout_width=&amp;quot;400dp&amp;quot;
              android:layout_marginRight=&amp;quot;10dp&amp;quot;/&amp;gt;
    &amp;lt;fragment android:id=&amp;quot;@+id/article&amp;quot;
              android:layout_height=&amp;quot;fill_parent&amp;quot;
              android:name=&amp;quot;com.example.android.newsreader.ArticleFragment&amp;quot;
              android:layout_width=&amp;quot;fill_parent&amp;quot; /&amp;gt;
&amp;lt;/LinearLayout&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;res/layout/twopanes_narrow.xml:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;LinearLayout xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;
android:layout_width=&amp;quot;fill_parent&amp;quot;
android:layout_height=&amp;quot;fill_parent&amp;quot;
android:orientation=&amp;quot;horizontal&amp;quot;&amp;gt;
    &amp;lt;fragment android:id=&amp;quot;@+id/headlines&amp;quot;
              android:layout_height=&amp;quot;fill_parent&amp;quot;
              android:name=&amp;quot;com.example.android.newsreader.HeadlinesFragment&amp;quot;
              android:layout_width=&amp;quot;200dp&amp;quot;
              android:layout_marginRight=&amp;quot;10dp&amp;quot;/&amp;gt;
    &amp;lt;fragment android:id=&amp;quot;@+id/article&amp;quot;
              android:layout_height=&amp;quot;fill_parent&amp;quot;
              android:name=&amp;quot;com.example.android.newsreader.ArticleFragment&amp;quot;
              android:layout_width=&amp;quot;fill_parent&amp;quot; /&amp;gt;
&amp;lt;/LinearLayout&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;定义好所有可能的布局，那就只需使用配置限定符将正确的布局映射到各种配置即可&lt;br&gt;res/values/layouts.xml:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;resources&amp;gt;
    &amp;lt;item name=&amp;quot;main_layout&amp;quot; type=&amp;quot;layout&amp;quot;&amp;gt;@layout/onepane_with_bar&amp;lt;/item&amp;gt;
    &amp;lt;bool name=&amp;quot;has_two_panes&amp;quot;&amp;gt;false&amp;lt;/bool&amp;gt;
&amp;lt;/resources&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;res/values-sw600dp-land/layouts.xml:&lt;br&gt;    &lt;resources&gt;&lt;br&gt;        &lt;item name=&quot;main_layout&quot; type=&quot;layout&quot;&gt;@layout/twopanes&lt;/item&gt;&lt;br&gt;        &lt;bool name=&quot;has_two_panes&quot;&gt;true&lt;/bool&gt;&lt;br&gt;    &lt;/resources&gt;&lt;br&gt;res/values-sw600dp-port/layouts.xml:&lt;br&gt;    &lt;resources&gt;&lt;br&gt;        &lt;item name=&quot;main_layout&quot; type=&quot;layout&quot;&gt;@layout/twopanes&lt;/item&gt;&lt;br&gt;        &lt;bool name=&quot;has_two_panes&quot;&gt;true&lt;/bool&gt;&lt;br&gt;    &lt;/resources&gt;&lt;br&gt;res/values-large-port/layouts.xml:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;resources&amp;gt;
    &amp;lt;item name=&amp;quot;main_layout&amp;quot; type=&amp;quot;layout&amp;quot;&amp;gt;@layout/twopanes_narrow&amp;lt;/item&amp;gt;
    &amp;lt;bool name=&amp;quot;has_two_panes&amp;quot;&amp;gt;true&amp;lt;/bool&amp;gt;
&amp;lt;/resources&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;使用自动拉伸位图&quot;&gt;&lt;a href=&quot;#使用自动拉伸位图&quot; class=&quot;headerlink&quot; title=&quot;使用自动拉伸位图&quot;&gt;&lt;/a&gt;使用自动拉伸位图&lt;/h4&gt;&lt;p&gt;通过 SDK 的 draw9patch 实用工具（位于 tools/ 目录中）处理图片&lt;/p&gt;
&lt;p&gt;文档参考：&lt;a href=&quot;https://developer.android.com/training/multiscreen/screensizes.html#TaskUse9Patch&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://developer.android.com/training/multiscreen/screensizes.html#TaskUse9Patch&lt;/a&gt;&lt;/p&gt;
</content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;使用”wrap-content”和”match-parent”&quot;&gt;&lt;a href=&quot;#使用”wrap-content”和”match-parent”&quot; class=&quot;headerlink&quot; title=&quot;使用”wrap_content”和”match_parent”&quot;&gt;&lt;/a&gt;使用”wrap_content”和”match_parent”&lt;/h4&gt;&lt;p&gt;如果使用 “wrap_content” 和 “match_parent” 尺寸值而不是硬编码的尺寸，您的视图就会相应地仅使用自身所需的空间或展开以填满可用空间。&lt;/p&gt;
    
    </summary>
    
      <category term="android" scheme="http://yoursite.com/categories/android/"/>
    
    
      <category term="android" scheme="http://yoursite.com/tags/android/"/>
    
  </entry>
  
  <entry>
    <title>android_文件共享</title>
    <link href="http://yoursite.com/2016/06/26/android-%E6%96%87%E4%BB%B6%E5%85%B1%E4%BA%AB/"/>
    <id>http://yoursite.com/2016/06/26/android-文件共享/</id>
    <published>2016-06-26T07:12:59.000Z</published>
    <updated>2016-07-05T08:46:27.155Z</updated>
    
    <content type="html">&lt;h4 id=&quot;配置文件共享&quot;&gt;&lt;a href=&quot;#配置文件共享&quot; class=&quot;headerlink&quot; title=&quot;配置文件共享&quot;&gt;&lt;/a&gt;配置文件共享&lt;/h4&gt;&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;
&lt;h5 id=&quot;定义FileProvider&quot;&gt;&lt;a href=&quot;#定义FileProvider&quot; class=&quot;headerlink&quot; title=&quot;定义FileProvider&quot;&gt;&lt;/a&gt;定义FileProvider&lt;/h5&gt;&lt;!--more--&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;manifest xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot; package=&amp;quot;com.example.myapp&amp;quot;&amp;gt;
    &amp;lt;application
        ...&amp;gt;
        &amp;lt;provider
            android:name=&amp;quot;android.support.v4.content.FileProvider&amp;quot;
            android:authorities=&amp;quot;com.example.myapp.fileprovider&amp;quot;
            android:grantUriPermissions=&amp;quot;true&amp;quot;
            android:exported=&amp;quot;false&amp;quot;&amp;gt;
            &amp;lt;meta-data
                android:name=&amp;quot;android.support.FILE_PROVIDER_PATHS&amp;quot;
                android:resource=&amp;quot;@xml/filepaths&amp;quot; /&amp;gt;
        &amp;lt;/provider&amp;gt;
        ...
    &amp;lt;/application&amp;gt;
&amp;lt;/manifest&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;指定共享目录&quot;&gt;&lt;a href=&quot;#指定共享目录&quot; class=&quot;headerlink&quot; title=&quot;指定共享目录&quot;&gt;&lt;/a&gt;指定共享目录&lt;/h5&gt;&lt;p&gt;在res/xml/filepaths.xml中定义&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;paths&amp;gt;
    &amp;lt;files-path path=&amp;quot;images/&amp;quot; name=&amp;quot;myimages&amp;quot; /&amp;gt;
&amp;lt;/paths&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;如果你请求default_image.jpg&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;content://com.example.myapp.fileprovider/myimages/default_image.jpg
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;共享文件&quot;&gt;&lt;a href=&quot;#共享文件&quot; class=&quot;headerlink&quot; title=&quot;共享文件&quot;&gt;&lt;/a&gt;共享文件&lt;/h4&gt;&lt;h5 id=&quot;在配置中定义文件选择activity&quot;&gt;&lt;a href=&quot;#在配置中定义文件选择activity&quot; class=&quot;headerlink&quot; title=&quot;在配置中定义文件选择activity&quot;&gt;&lt;/a&gt;在配置中定义文件选择activity&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;&amp;lt;manifest xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;&amp;gt;
    ...
        &amp;lt;application&amp;gt;
        ...
            &amp;lt;activity
                android:name=&amp;quot;.FileSelectActivity&amp;quot;
                android:label=&amp;quot;@File Selector&amp;quot; &amp;gt;
                &amp;lt;intent-filter&amp;gt;
                    &amp;lt;action
                        android:name=&amp;quot;android.intent.action.PICK&amp;quot;/&amp;gt;
                    &amp;lt;category
                        android:name=&amp;quot;android.intent.category.DEFAULT&amp;quot;/&amp;gt;
                    &amp;lt;category
                        android:name=&amp;quot;android.intent.category.OPENABLE&amp;quot;/&amp;gt;
                    &amp;lt;data android:mimeType=&amp;quot;text/plain&amp;quot;/&amp;gt;
                    &amp;lt;data android:mimeType=&amp;quot;image/*&amp;quot;/&amp;gt;
                &amp;lt;/intent-filter&amp;gt;
            &amp;lt;/activity&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;在代码中定义文件选择activity&quot;&gt;&lt;a href=&quot;#在代码中定义文件选择activity&quot; class=&quot;headerlink&quot; title=&quot;在代码中定义文件选择activity&quot;&gt;&lt;/a&gt;在代码中定义文件选择activity&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;public class MainActivity extends Activity {
    // The path to the root of this app&amp;apos;s internal storage
    private File mPrivateRootDir;
    // The path to the &amp;quot;images&amp;quot; subdirectory
    private File mImagesDir;
    // Array of files in the images subdirectory
    File[] mImageFiles;
    // Array of filenames corresponding to mImageFiles
    String[] mImageFilenames;
    // Initialize the Activity
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        ...
        // Set up an Intent to send back to apps that request a file
        mResultIntent =
                new Intent(&amp;quot;com.example.myapp.ACTION_RETURN_FILE&amp;quot;);
        // Get the files/ subdirectory of internal storage
        mPrivateRootDir = getFilesDir();
        // Get the files/images subdirectory;
        mImagesDir = new File(mPrivateRootDir, &amp;quot;images&amp;quot;);
        // Get the files in the images subdirectory
        mImageFiles = mImagesDir.listFiles();
        // Set the Activity&amp;apos;s result to null to begin with
        setResult(Activity.RESULT_CANCELED, null);
        /*
         * Display the file names in the ListView mFileListView.
         * Back the ListView with the array mImageFilenames, which
         * you can create by iterating through mImageFiles and
         * calling File.getAbsolutePath() for each File
         */
         ...
    }
    ...
}
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;指定文件选择后的响应&quot;&gt;&lt;a href=&quot;#指定文件选择后的响应&quot; class=&quot;headerlink&quot; title=&quot;指定文件选择后的响应&quot;&gt;&lt;/a&gt;指定文件选择后的响应&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;protected void onCreate(Bundle savedInstanceState) {
    ...
    // Define a listener that responds to clicks on a file in the ListView
    mFileListView.setOnItemClickListener(
            new AdapterView.OnItemClickListener() {
        @Override
        /*
         * When a filename in the ListView is clicked, get its
         * content URI and send it to the requesting app
         */
        public void onItemClick(AdapterView&amp;lt;?&amp;gt; adapterView,
                View view,
                int position,
                long rowId) {
            /*
             * Get a File for the selected file name.
             * Assume that the file names are in the
             * mImageFilename array.
             */
            File requestFile = new File(mImageFilename[position]);
            /*
             * Most file-related method calls need to be in
             * try-catch blocks.
             */
            // Use the FileProvider to get a content URI
            try {
                fileUri = FileProvider.getUriForFile(
                        MainActivity.this,
                        &amp;quot;com.example.myapp.fileprovider&amp;quot;,
                        requestFile);
            } catch (IllegalArgumentException e) {
                Log.e(&amp;quot;File Selector&amp;quot;,
                      &amp;quot;The selected file can&amp;apos;t be shared: &amp;quot; +
                      clickedFilename);
            }
            ...
        }
    });
    ...
}
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;授权&quot;&gt;&lt;a href=&quot;#授权&quot; class=&quot;headerlink&quot; title=&quot;授权&quot;&gt;&lt;/a&gt;授权&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;protected void onCreate(Bundle savedInstanceState) {
    ...
    // Define a listener that responds to clicks in the ListView
    mFileListView.setOnItemClickListener(
            new AdapterView.OnItemClickListener() {
        @Override
        public void onItemClick(AdapterView&amp;lt;?&amp;gt; adapterView,
                View view,
                int position,
                long rowId) {
            ...
            if (fileUri != null) {
                // Grant temporary read permission to the content URI
                mResultIntent.addFlags(
                    Intent.FLAG_GRANT_READ_URI_PERMISSION);
            }
            ...
         }
         ...
    });
...
}
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;共享文件给请求的app&quot;&gt;&lt;a href=&quot;#共享文件给请求的app&quot; class=&quot;headerlink&quot; title=&quot;共享文件给请求的app&quot;&gt;&lt;/a&gt;共享文件给请求的app&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;protected void onCreate(Bundle savedInstanceState) {
    ...
    // Define a listener that responds to clicks on a file in the ListView
    mFileListView.setOnItemClickListener(
            new AdapterView.OnItemClickListener() {
        @Override
        public void onItemClick(AdapterView&amp;lt;?&amp;gt; adapterView,
                View view,
                int position,
                long rowId) {
            ...
            if (fileUri != null) {
                ...
                // Put the Uri and MIME type in the result Intent
                mResultIntent.setDataAndType(
                        fileUri,
                        getContentResolver().getType(fileUri));
                // Set the result
                MainActivity.this.setResult(Activity.RESULT_OK,
                        mResultIntent);
                } else {
                    mResultIntent.setDataAndType(null, &amp;quot;&amp;quot;);
                    MainActivity.this.setResult(RESULT_CANCELED,
                            mResultIntent);
                }
            }
    });
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;一旦用户选择好文件以后，提供一个按钮返回app,点击调用finish（）&lt;br&gt;    public void onDoneClick(View v) {&lt;br&gt;        // Associate a method with the Done button&lt;br&gt;        finish();&lt;br&gt;    }&lt;/p&gt;
&lt;h4 id=&quot;请求共享的文件&quot;&gt;&lt;a href=&quot;#请求共享的文件&quot; class=&quot;headerlink&quot; title=&quot;请求共享的文件&quot;&gt;&lt;/a&gt;请求共享的文件&lt;/h4&gt;&lt;h5 id=&quot;发送请求&quot;&gt;&lt;a href=&quot;#发送请求&quot; class=&quot;headerlink&quot; title=&quot;发送请求&quot;&gt;&lt;/a&gt;发送请求&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;public class MainActivity extends Activity {
    private Intent mRequestFileIntent;
    private ParcelFileDescriptor mInputPFD;
    ...
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
        mRequestFileIntent = new Intent(Intent.ACTION_PICK);
        mRequestFileIntent.setType(&amp;quot;image/jpg&amp;quot;);
        ...
    }
    ...
    protected void requestFile() {
        /**
         * When the user requests a file, send an Intent to the
         * server app.
         * files.
         */
            startActivityForResult(mRequestFileIntent, 0);
        ...
    }
    ...
}
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;接受文件&quot;&gt;&lt;a href=&quot;#接受文件&quot; class=&quot;headerlink&quot; title=&quot;接受文件&quot;&gt;&lt;/a&gt;接受文件&lt;/h5&gt;&lt;pre&gt;&lt;code&gt; /*
 * When the Activity of the app that hosts files sets a result and calls
 * finish(), this method is invoked. The returned Intent contains the
 * content URI of a selected file. The result code indicates if the
 * selection worked or not.
 */
@Override
public void onActivityResult(int requestCode, int resultCode,
        Intent returnIntent) {
    // If the selection didn&amp;apos;t work
    if (resultCode != RESULT_OK) {
        // Exit without doing anything else
        return;
    } else {
        // Get the file&amp;apos;s content URI from the incoming Intent
        Uri returnUri = returnIntent.getData();
        /*
         * Try to open the file for &amp;quot;read&amp;quot; access using the
         * returned URI. If the file isn&amp;apos;t found, write to the
         * error log and return.
         */
        try {
            /*
             * Get the content resolver instance for this context, and use it
             * to get a ParcelFileDescriptor for the file.
             */
            mInputPFD = getContentResolver().openFileDescriptor(returnUri, &amp;quot;r&amp;quot;);
        } catch (FileNotFoundException e) {
            e.printStackTrace();
            Log.e(&amp;quot;MainActivity&amp;quot;, &amp;quot;File not found.&amp;quot;);
            return;
        }
        // Get a regular file descriptor for the file
        FileDescriptor fd = mInputPFD.getFileDescriptor();
        ...
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;取出文件信息&quot;&gt;&lt;a href=&quot;#取出文件信息&quot; class=&quot;headerlink&quot; title=&quot;取出文件信息&quot;&gt;&lt;/a&gt;取出文件信息&lt;/h4&gt;&lt;h5 id=&quot;读取文件的MIME-Type&quot;&gt;&lt;a href=&quot;#读取文件的MIME-Type&quot; class=&quot;headerlink&quot; title=&quot;读取文件的MIME Type&quot;&gt;&lt;/a&gt;读取文件的MIME Type&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;...
/*
 * Get the file&amp;apos;s content URI from the incoming Intent, then
 * get the file&amp;apos;s MIME type
 */
Uri returnUri = returnIntent.getData();
String mimeType = getContentResolver().getType(returnUri);
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;读取文件的名字和大小&quot;&gt;&lt;a href=&quot;#读取文件的名字和大小&quot; class=&quot;headerlink&quot; title=&quot;读取文件的名字和大小&quot;&gt;&lt;/a&gt;读取文件的名字和大小&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;...
/*
 * Get the file&amp;apos;s content URI from the incoming Intent,
 * then query the server app to get the file&amp;apos;s display name
 * and size.
 */
Uri returnUri = returnIntent.getData();
Cursor returnCursor =
        getContentResolver().query(returnUri, null, null, null, null);
/*
 * Get the column indexes of the data in the Cursor,
 * move to the first row in the Cursor, get the data,
 * and display it.
 */
int nameIndex = returnCursor.getColumnIndex(OpenableColumns.DISPLAY_NAME);
int sizeIndex = returnCursor.getColumnIndex(OpenableColumns.SIZE);
returnCursor.moveToFirst();
TextView nameView = (TextView) findViewById(R.id.filename_text);
TextView sizeView = (TextView) findViewById(R.id.filesize_text);
nameView.setText(returnCursor.getString(nameIndex));
sizeView.setText(Long.toString(returnCursor.getLong(sizeIndex)));
...
&lt;/code&gt;&lt;/pre&gt;</content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;配置文件共享&quot;&gt;&lt;a href=&quot;#配置文件共享&quot; class=&quot;headerlink&quot; title=&quot;配置文件共享&quot;&gt;&lt;/a&gt;配置文件共享&lt;/h4&gt;
    
    </summary>
    
      <category term="android" scheme="http://yoursite.com/categories/android/"/>
    
    
      <category term="android" scheme="http://yoursite.com/tags/android/"/>
    
  </entry>
  
  <entry>
    <title>android_简单数据的分享</title>
    <link href="http://yoursite.com/2016/06/23/android-%E7%AE%80%E5%8D%95%E6%95%B0%E6%8D%AE%E7%9A%84%E5%88%86%E4%BA%AB/"/>
    <id>http://yoursite.com/2016/06/23/android-简单数据的分享/</id>
    <published>2016-06-23T14:25:22.000Z</published>
    <updated>2016-07-05T08:44:01.446Z</updated>
    
    <content type="html">&lt;h4 id=&quot;向其他app发送简单数据&quot;&gt;&lt;a href=&quot;#向其他app发送简单数据&quot; class=&quot;headerlink&quot; title=&quot;向其他app发送简单数据&quot;&gt;&lt;/a&gt;向其他app发送简单数据&lt;/h4&gt;&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;
&lt;h5 id=&quot;发送文本内容&quot;&gt;&lt;a href=&quot;#发送文本内容&quot; class=&quot;headerlink&quot; title=&quot;发送文本内容&quot;&gt;&lt;/a&gt;发送文本内容&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;Intent sendIntent = new Intent();
sendIntent.setAction(Intent.ACTION_SEND);
sendIntent.putExtra(Intent.EXTRA_TEXT, &amp;quot;This is my text to send.&amp;quot;);
sendIntent.setType(&amp;quot;text/plain&amp;quot;);
startActivity(sendIntent);
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Intent.createChooser()&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Intent sendIntent = new Intent();
sendIntent.setAction(Intent.ACTION_SEND);
sendIntent.putExtra(Intent.EXTRA_TEXT, &amp;quot;This is my text to send.&amp;quot;);
sendIntent.setType(&amp;quot;text/plain&amp;quot;);
startActivity(Intent.createChooser(sendIntent, getResources().getText(R.string.send_to)));
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;发送二进制内容&quot;&gt;&lt;a href=&quot;#发送二进制内容&quot; class=&quot;headerlink&quot; title=&quot;发送二进制内容&quot;&gt;&lt;/a&gt;发送二进制内容&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;Intent shareIntent = new Intent();
shareIntent.setAction(Intent.ACTION_SEND);
shareIntent.putExtra(Intent.EXTRA_STREAM, uriToImage);
shareIntent.setType(&amp;quot;image/jpeg&amp;quot;);
startActivity(Intent.createChooser(shareIntent, getResources().getText(R.string.send_to)));
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;发送多个内容&quot;&gt;&lt;a href=&quot;#发送多个内容&quot; class=&quot;headerlink&quot; title=&quot;发送多个内容&quot;&gt;&lt;/a&gt;发送多个内容&lt;/h5&gt;&lt;p&gt;ACTION_SEND_MULTIPLE  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;ArrayList&amp;lt;Uri&amp;gt; imageUris = new ArrayList&amp;lt;Uri&amp;gt;();
imageUris.add(imageUri1); // Add your image URIs here
imageUris.add(imageUri2);

Intent shareIntent = new Intent();
shareIntent.setAction(Intent.ACTION_SEND_MULTIPLE);
shareIntent.putParcelableArrayListExtra(Intent.EXTRA_STREAM, imageUris);
shareIntent.setType(&amp;quot;image/*&amp;quot;);
startActivity(Intent.createChooser(shareIntent, &amp;quot;Share images to..&amp;quot;));
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;从其他app接收简单数据&quot;&gt;&lt;a href=&quot;#从其他app接收简单数据&quot; class=&quot;headerlink&quot; title=&quot;从其他app接收简单数据&quot;&gt;&lt;/a&gt;从其他app接收简单数据&lt;/h4&gt;&lt;h5 id=&quot;更新Manifest&quot;&gt;&lt;a href=&quot;#更新Manifest&quot; class=&quot;headerlink&quot; title=&quot;更新Manifest&quot;&gt;&lt;/a&gt;更新Manifest&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;&amp;lt;activity android:name=&amp;quot;.ui.MyActivity&amp;quot; &amp;gt;
    &amp;lt;intent-filter&amp;gt;
        &amp;lt;action android:name=&amp;quot;android.intent.action.SEND&amp;quot; /&amp;gt;
        &amp;lt;category android:name=&amp;quot;android.intent.category.DEFAULT&amp;quot; /&amp;gt;
        &amp;lt;data android:mimeType=&amp;quot;image/*&amp;quot; /&amp;gt;
    &amp;lt;/intent-filter&amp;gt;
    &amp;lt;intent-filter&amp;gt;
        &amp;lt;action android:name=&amp;quot;android.intent.action.SEND&amp;quot; /&amp;gt;
        &amp;lt;category android:name=&amp;quot;android.intent.category.DEFAULT&amp;quot; /&amp;gt;
        &amp;lt;data android:mimeType=&amp;quot;text/plain&amp;quot; /&amp;gt;
    &amp;lt;/intent-filter&amp;gt;
    &amp;lt;intent-filter&amp;gt;
        &amp;lt;action android:name=&amp;quot;android.intent.action.SEND_MULTIPLE&amp;quot; /&amp;gt;
        &amp;lt;category android:name=&amp;quot;android.intent.category.DEFAULT&amp;quot; /&amp;gt;
        &amp;lt;data android:mimeType=&amp;quot;image/*&amp;quot; /&amp;gt;
    &amp;lt;/intent-filter&amp;gt;
&amp;lt;/activity&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;处理接收到的内容&quot;&gt;&lt;a href=&quot;#处理接收到的内容&quot; class=&quot;headerlink&quot; title=&quot;处理接收到的内容&quot;&gt;&lt;/a&gt;处理接收到的内容&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;void onCreate (Bundle savedInstanceState) {
    ...
    // Get intent, action and MIME type
    Intent intent = getIntent();
    String action = intent.getAction();
    String type = intent.getType();

    if (Intent.ACTION_SEND.equals(action) &amp;amp;&amp;amp; type != null) {
        if (&amp;quot;text/plain&amp;quot;.equals(type)) {
            handleSendText(intent); // Handle text being sent
        } else if (type.startsWith(&amp;quot;image/&amp;quot;)) {
            handleSendImage(intent); // Handle single image being sent
        }
    } else if (Intent.ACTION_SEND_MULTIPLE.equals(action) &amp;amp;&amp;amp; type != null) {
        if (type.startsWith(&amp;quot;image/&amp;quot;)) {
            handleSendMultipleImages(intent); // Handle multiple images being sent
        }
    } else {
        // Handle other intents, such as being started from the home screen
    }
    ...
}

void handleSendText(Intent intent) {
    String sharedText = intent.getStringExtra(Intent.EXTRA_TEXT);
    if (sharedText != null) {
        // Update UI to reflect text being shared
    }
}

void handleSendImage(Intent intent) {
    Uri imageUri = (Uri) intent.getParcelableExtra(Intent.EXTRA_STREAM);
    if (imageUri != null) {
        // Update UI to reflect image being shared
    }
}

void handleSendMultipleImages(Intent intent) {
    ArrayList&amp;lt;Uri&amp;gt; imageUris = intent.getParcelableArrayListExtra(Intent.EXTRA_STREAM);
    if (imageUris != null) {
        // Update UI to reflect multiple images being shared
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;添加一个简单的分享action&quot;&gt;&lt;a href=&quot;#添加一个简单的分享action&quot; class=&quot;headerlink&quot; title=&quot;添加一个简单的分享action&quot;&gt;&lt;/a&gt;添加一个简单的分享action&lt;/h4&gt;&lt;h5 id=&quot;更新菜单声明&quot;&gt;&lt;a href=&quot;#更新菜单声明&quot; class=&quot;headerlink&quot; title=&quot;更新菜单声明&quot;&gt;&lt;/a&gt;更新菜单声明&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;&amp;lt;menu xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;&amp;gt;
    &amp;lt;item
            android:id=&amp;quot;@+id/menu_item_share&amp;quot;
            android:showAsAction=&amp;quot;ifRoom&amp;quot;
            android:title=&amp;quot;Share&amp;quot;
            android:actionProviderClass=
                &amp;quot;android.widget.ShareActionProvider&amp;quot; /&amp;gt;
    ...
&amp;lt;/menu&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;Set-the-Share-Intent&quot;&gt;&lt;a href=&quot;#Set-the-Share-Intent&quot; class=&quot;headerlink&quot; title=&quot;Set the Share Intent&quot;&gt;&lt;/a&gt;Set the Share Intent&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;private ShareActionProvider mShareActionProvider;
...

@Override
public boolean onCreateOptionsMenu(Menu menu) {
    // Inflate menu resource file.
    getMenuInflater().inflate(R.menu.share_menu, menu);

    // Locate MenuItem with ShareActionProvider
    MenuItem item = menu.findItem(R.id.menu_item_share);

    // Fetch and store ShareActionProvider
    mShareActionProvider = (ShareActionProvider) item.getActionProvider();

    // Return true to display menu
    return true;
}

// Call to update the share intent
private void setShareIntent(Intent shareIntent) {
    if (mShareActionProvider != null) {
        mShareActionProvider.setShareIntent(shareIntent);
    }
}
&lt;/code&gt;&lt;/pre&gt;</content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;向其他app发送简单数据&quot;&gt;&lt;a href=&quot;#向其他app发送简单数据&quot; class=&quot;headerlink&quot; title=&quot;向其他app发送简单数据&quot;&gt;&lt;/a&gt;向其他app发送简单数据&lt;/h4&gt;
    
    </summary>
    
      <category term="android" scheme="http://yoursite.com/categories/android/"/>
    
    
      <category term="android" scheme="http://yoursite.com/tags/android/"/>
    
  </entry>
  
  <entry>
    <title>shadowsocks搭建VPN</title>
    <link href="http://yoursite.com/2016/06/23/%E7%BF%BB%E5%A2%99%E6%96%B9%E6%B3%95/"/>
    <id>http://yoursite.com/2016/06/23/翻墙方法/</id>
    <published>2016-06-23T05:03:22.000Z</published>
    <updated>2016-10-14T08:14:05.128Z</updated>
    
    <content type="html">&lt;h4 id=&quot;购买服务器&quot;&gt;&lt;a href=&quot;#购买服务器&quot; class=&quot;headerlink&quot; title=&quot;购买服务器&quot;&gt;&lt;/a&gt;购买服务器&lt;/h4&gt;&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;
&lt;pre&gt;&lt;code&gt;https://www.openvzvps.cc/aff.php?aff=259
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;修改安装的系统并安装服务&quot;&gt;&lt;a href=&quot;#修改安装的系统并安装服务&quot; class=&quot;headerlink&quot; title=&quot;修改安装的系统并安装服务&quot;&gt;&lt;/a&gt;修改安装的系统并安装服务&lt;/h4&gt;&lt;p&gt;在 客户中心-&amp;gt;我的产品与服务 将预装的centos重装为ubunt&lt;br&gt;用xshell进入后台  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;apt-get update
apt-get install python-gevent python-pip
apt-get install python-m2crypto
pip install shadowsocks
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;添加配置文件并启动服务&quot;&gt;&lt;a href=&quot;#添加配置文件并启动服务&quot; class=&quot;headerlink&quot; title=&quot;添加配置文件并启动服务&quot;&gt;&lt;/a&gt;添加配置文件并启动服务&lt;/h4&gt;&lt;p&gt;下载&lt;a href=&quot;/uploads/shadowsocks.rar&quot;&gt;shadowsocks.rar&lt;/a&gt;&lt;br&gt;修改里面的shadowsocks.json  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;{
    &amp;quot;server&amp;quot;:&amp;quot;0.0.0.0&amp;quot;,
    &amp;quot;server_port&amp;quot;:xxxx,  //端口
    &amp;quot;password&amp;quot;:&amp;quot;xxxxxx&amp;quot;, //密码
    &amp;quot;timeout&amp;quot;:600,
    &amp;quot;method&amp;quot;:&amp;quot;aes-256-cfb&amp;quot;
} 
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;将文件夹上传到root目录，然后&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;bash restart.sh
bash check.sh
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;下载客户端&quot;&gt;&lt;a href=&quot;#下载客户端&quot; class=&quot;headerlink&quot; title=&quot;下载客户端&quot;&gt;&lt;/a&gt;下载客户端&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;windows: https://shadowsocks.org/en/download/clients.html
android: https://github.com/shadowsocks/shadowsocks-android/releases
&lt;/code&gt;&lt;/pre&gt;</content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;购买服务器&quot;&gt;&lt;a href=&quot;#购买服务器&quot; class=&quot;headerlink&quot; title=&quot;购买服务器&quot;&gt;&lt;/a&gt;购买服务器&lt;/h4&gt;
    
    </summary>
    
    
      <category term="shadowsocks" scheme="http://yoursite.com/tags/shadowsocks/"/>
    
  </entry>
  
  <entry>
    <title>android_系统权限</title>
    <link href="http://yoursite.com/2016/06/23/android-%E7%B3%BB%E7%BB%9F%E6%9D%83%E9%99%90/"/>
    <id>http://yoursite.com/2016/06/23/android-系统权限/</id>
    <published>2016-06-22T16:07:25.000Z</published>
    <updated>2016-07-05T08:46:09.591Z</updated>
    
    <content type="html">&lt;h4 id=&quot;声明权限&quot;&gt;&lt;a href=&quot;#声明权限&quot; class=&quot;headerlink&quot; title=&quot;声明权限&quot;&gt;&lt;/a&gt;声明权限&lt;/h4&gt;&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;
&lt;h5 id=&quot;确定app需要哪些权限&quot;&gt;&lt;a href=&quot;#确定app需要哪些权限&quot; class=&quot;headerlink&quot; title=&quot;确定app需要哪些权限&quot;&gt;&lt;/a&gt;确定app需要哪些权限&lt;/h5&gt;&lt;h5 id=&quot;在Manifest中添加权限&quot;&gt;&lt;a href=&quot;#在Manifest中添加权限&quot; class=&quot;headerlink&quot; title=&quot;在Manifest中添加权限&quot;&gt;&lt;/a&gt;在Manifest中添加权限&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;&amp;lt;manifest xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;
        package=&amp;quot;com.example.snazzyapp&amp;quot;&amp;gt;

    &amp;lt;uses-permission android:name=&amp;quot;android.permission.SEND_SMS&amp;quot;/&amp;gt;


    &amp;lt;application ...&amp;gt;
        ...
    &amp;lt;/application&amp;gt;

&amp;lt;/manifest&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;在运行时请求权限&quot;&gt;&lt;a href=&quot;#在运行时请求权限&quot; class=&quot;headerlink&quot; title=&quot;在运行时请求权限&quot;&gt;&lt;/a&gt;在运行时请求权限&lt;/h4&gt;&lt;p&gt;在android6.0之后，在app运行时用户可以授予权限，而不是在app安装时确认&lt;/p&gt;
&lt;h5 id=&quot;检查权限&quot;&gt;&lt;a href=&quot;#检查权限&quot; class=&quot;headerlink&quot; title=&quot;检查权限&quot;&gt;&lt;/a&gt;检查权限&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;// Assume thisActivity is the current activity
int permissionCheck = ContextCompat.checkSelfPermission(thisActivity,
        Manifest.permission.WRITE_CALENDAR);
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;请求权限&quot;&gt;&lt;a href=&quot;#请求权限&quot; class=&quot;headerlink&quot; title=&quot;请求权限&quot;&gt;&lt;/a&gt;请求权限&lt;/h5&gt;&lt;h6 id=&quot;解释为什么要权限&quot;&gt;&lt;a href=&quot;#解释为什么要权限&quot; class=&quot;headerlink&quot; title=&quot;解释为什么要权限&quot;&gt;&lt;/a&gt;解释为什么要权限&lt;/h6&gt;&lt;pre&gt;&lt;code&gt;shouldShowRequestPermissionRationale()    
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;当app请求了对应权限，而用户拒绝了，此方法返回true&lt;/p&gt;
&lt;h6 id=&quot;请求需要的权限&quot;&gt;&lt;a href=&quot;#请求需要的权限&quot; class=&quot;headerlink&quot; title=&quot;请求需要的权限&quot;&gt;&lt;/a&gt;请求需要的权限&lt;/h6&gt;&lt;p&gt;requestPermissions()&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// Here, thisActivity is the current activity
if (ContextCompat.checkSelfPermission(thisActivity,
                Manifest.permission.READ_CONTACTS)
        != PackageManager.PERMISSION_GRANTED) {

    // Should we show an explanation?
    if (ActivityCompat.shouldShowRequestPermissionRationale(thisActivity,
            Manifest.permission.READ_CONTACTS)) {

        // Show an expanation to the user *asynchronously* -- don&amp;apos;t block
        // this thread waiting for the user&amp;apos;s response! After the user
        // sees the explanation, try again to request the permission.

    } else {

        // No explanation needed, we can request the permission.

        ActivityCompat.requestPermissions(thisActivity,
                new String[]{Manifest.permission.READ_CONTACTS},
                MY_PERMISSIONS_REQUEST_READ_CONTACTS);

        // MY_PERMISSIONS_REQUEST_READ_CONTACTS is an
        // app-defined int constant. The callback method gets the
        // result of the request.
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;h6 id=&quot;处理权限请求响应&quot;&gt;&lt;a href=&quot;#处理权限请求响应&quot; class=&quot;headerlink&quot; title=&quot;处理权限请求响应&quot;&gt;&lt;/a&gt;处理权限请求响应&lt;/h6&gt;&lt;pre&gt;&lt;code&gt;@Override
public void onRequestPermissionsResult(int requestCode,
        String permissions[], int[] grantResults) {
    switch (requestCode) {
        case MY_PERMISSIONS_REQUEST_READ_CONTACTS: {
            // If request is cancelled, the result arrays are empty.
            if (grantResults.length &amp;gt; 0
                &amp;amp;&amp;amp; grantResults[0] == PackageManager.PERMISSION_GRANTED) {

                // permission was granted, yay! Do the
                // contacts-related task you need to do.

            } else {

                // permission denied, boo! Disable the
                // functionality that depends on this permission.
            }
            return;
        }

        // other &amp;apos;case&amp;apos; lines to check for other
        // permissions this app might request
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;权限的最佳实践&quot;&gt;&lt;a href=&quot;#权限的最佳实践&quot; class=&quot;headerlink&quot; title=&quot;权限的最佳实践&quot;&gt;&lt;/a&gt;权限的最佳实践&lt;/h4&gt;</content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;声明权限&quot;&gt;&lt;a href=&quot;#声明权限&quot; class=&quot;headerlink&quot; title=&quot;声明权限&quot;&gt;&lt;/a&gt;声明权限&lt;/h4&gt;
    
    </summary>
    
      <category term="android" scheme="http://yoursite.com/categories/android/"/>
    
    
      <category term="android" scheme="http://yoursite.com/tags/android/"/>
    
  </entry>
  
  <entry>
    <title>android_与其他APP互动</title>
    <link href="http://yoursite.com/2016/06/22/android-%E4%B8%8E%E5%85%B6%E4%BB%96APP%E4%BA%92%E5%8A%A8/"/>
    <id>http://yoursite.com/2016/06/22/android-与其他APP互动/</id>
    <published>2016-06-22T15:26:17.000Z</published>
    <updated>2016-07-05T08:46:03.398Z</updated>
    
    <content type="html">&lt;h4 id=&quot;如何打开其他app&quot;&gt;&lt;a href=&quot;#如何打开其他app&quot; class=&quot;headerlink&quot; title=&quot;如何打开其他app&quot;&gt;&lt;/a&gt;如何打开其他app&lt;/h4&gt;&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;
&lt;h5 id=&quot;创建隐含的Intent&quot;&gt;&lt;a href=&quot;#创建隐含的Intent&quot; class=&quot;headerlink&quot; title=&quot;创建隐含的Intent&quot;&gt;&lt;/a&gt;创建隐含的Intent&lt;/h5&gt;&lt;h6 id=&quot;通过Uri创建Intent&quot;&gt;&lt;a href=&quot;#通过Uri创建Intent&quot; class=&quot;headerlink&quot; title=&quot;通过Uri创建Intent&quot;&gt;&lt;/a&gt;通过Uri创建Intent&lt;/h6&gt;&lt;p&gt;打电话  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Uri number = Uri.parse(&amp;quot;tel:5551234&amp;quot;);
Intent callIntent = new Intent(Intent.ACTION_DIAL, number);
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;打开地图  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// Map point based on address
Uri location = Uri.parse(&amp;quot;geo:0,0?q=1600+Amphitheatre+Parkway,+Mountain+View,+California&amp;quot;);
// Or map point based on latitude/longitude
// Uri location = Uri.parse(&amp;quot;geo:37.422219,-122.08364?z=14&amp;quot;); // z param is zoom level
Intent mapIntent = new Intent(Intent.ACTION_VIEW, location);
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;打开网页&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Uri webpage = Uri.parse(&amp;quot;http://www.android.com&amp;quot;);
Intent webIntent = new Intent(Intent.ACTION_VIEW, webpage);
&lt;/code&gt;&lt;/pre&gt;&lt;h6 id=&quot;通过添加-extra-data-来制定action&quot;&gt;&lt;a href=&quot;#通过添加-extra-data-来制定action&quot; class=&quot;headerlink&quot; title=&quot;通过添加 extra data 来制定action&quot;&gt;&lt;/a&gt;通过添加 extra data 来制定action&lt;/h6&gt;&lt;p&gt;发送邮件&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Intent emailIntent = new Intent(Intent.ACTION_SEND);
// The intent does not have a URI, so declare the &amp;quot;text/plain&amp;quot; MIME type
emailIntent.setType(HTTP.PLAIN_TEXT_TYPE);
emailIntent.putExtra(Intent.EXTRA_EMAIL, new String[] {&amp;quot;jon@example.com&amp;quot;}); // recipients
emailIntent.putExtra(Intent.EXTRA_SUBJECT, &amp;quot;Email subject&amp;quot;);
emailIntent.putExtra(Intent.EXTRA_TEXT, &amp;quot;Email message text&amp;quot;);
emailIntent.putExtra(Intent.EXTRA_STREAM, Uri.parse(&amp;quot;content://path/to/email/attachment&amp;quot;));
// You can also attach multiple items by passing an ArrayList of Uris  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;创建一个日历标签&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Intent calendarIntent = new Intent(Intent.ACTION_INSERT, Events.CONTENT_URI);
Calendar beginTime = Calendar.getInstance().set(2012, 0, 19, 7, 30);
Calendar endTime = Calendar.getInstance().set(2012, 0, 19, 10, 30);
calendarIntent.putExtra(CalendarContract.EXTRA_EVENT_BEGIN_TIME, beginTime.getTimeInMillis());
calendarIntent.putExtra(CalendarContract.EXTRA_EVENT_END_TIME, endTime.getTimeInMillis());
calendarIntent.putExtra(Events.TITLE, &amp;quot;Ninja class&amp;quot;);
calendarIntent.putExtra(Events.EVENT_LOCATION, &amp;quot;Secret dojo&amp;quot;);
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;校验是否有app可以接收Intent&quot;&gt;&lt;a href=&quot;#校验是否有app可以接收Intent&quot; class=&quot;headerlink&quot; title=&quot;校验是否有app可以接收Intent&quot;&gt;&lt;/a&gt;校验是否有app可以接收Intent&lt;/h5&gt;&lt;p&gt;queryIntentActivities()&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;PackageManager packageManager = getPackageManager();
List activities = packageManager.queryIntentActivities(intent,
        PackageManager.MATCH_DEFAULT_ONLY);
boolean isIntentSafe = activities.size() &amp;gt; 0;
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;用Intent-启动activity&quot;&gt;&lt;a href=&quot;#用Intent-启动activity&quot; class=&quot;headerlink&quot; title=&quot;用Intent 启动activity&quot;&gt;&lt;/a&gt;用Intent 启动activity&lt;/h5&gt;&lt;p&gt;startActivity(intent);&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// Build the intent
Uri location = Uri.parse(&amp;quot;geo:0,0?q=1600+Amphitheatre+Parkway,+Mountain+View,+California&amp;quot;);
Intent mapIntent = new Intent(Intent.ACTION_VIEW, location);

// Verify it resolves
PackageManager packageManager = getPackageManager();
List&amp;lt;ResolveInfo&amp;gt; activities = packageManager.queryIntentActivities(mapIntent, 0);
boolean isIntentSafe = activities.size() &amp;gt; 0;

// Start an activity if it&amp;apos;s safe
if (isIntentSafe) {
    startActivity(mapIntent);
}
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;创建一个app选择器&quot;&gt;&lt;a href=&quot;#创建一个app选择器&quot; class=&quot;headerlink&quot; title=&quot;创建一个app选择器&quot;&gt;&lt;/a&gt;创建一个app选择器&lt;/h5&gt;&lt;p&gt;通过用createChooser()创建Intent 然后调用 startActivity&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Intent intent = new Intent(Intent.ACTION_SEND);
...

// Always use string resources for UI text.
// This says something like &amp;quot;Share this photo with&amp;quot;
String title = getResources().getString(R.string.chooser_title);
// Create intent to show chooser
Intent chooser = Intent.createChooser(intent, title);

// Verify the intent will resolve to at least one activity
if (intent.resolveActivity(getPackageManager()) != null) {
    startActivity(chooser);
}
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;从另外一个activity获得一个结果&quot;&gt;&lt;a href=&quot;#从另外一个activity获得一个结果&quot; class=&quot;headerlink&quot; title=&quot;从另外一个activity获得一个结果&quot;&gt;&lt;/a&gt;从另外一个activity获得一个结果&lt;/h4&gt;&lt;h5 id=&quot;启动一个activity&quot;&gt;&lt;a href=&quot;#启动一个activity&quot; class=&quot;headerlink&quot; title=&quot;启动一个activity&quot;&gt;&lt;/a&gt;启动一个activity&lt;/h5&gt;&lt;p&gt; startActivityForResult() &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;static final int PICK_CONTACT_REQUEST = 1;  // The request code
...
private void pickContact() {
    Intent pickContactIntent = new Intent(Intent.ACTION_PICK, Uri.parse(&amp;quot;content://contacts&amp;quot;));
    pickContactIntent.setType(Phone.CONTENT_TYPE); // Show user only contacts w/ phone numbers
    startActivityForResult(pickContactIntent, PICK_CONTACT_REQUEST);
}
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;接收结果&quot;&gt;&lt;a href=&quot;#接收结果&quot; class=&quot;headerlink&quot; title=&quot;接收结果&quot;&gt;&lt;/a&gt;接收结果&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;@Override
protected void onActivityResult(int requestCode, int resultCode, Intent data) {
    // Check which request we&amp;apos;re responding to
    if (requestCode == PICK_CONTACT_REQUEST) {
        // Make sure the request was successful
        if (resultCode == RESULT_OK) {
            // The user picked a contact.
            // The Intent&amp;apos;s data Uri identifies which contact was selected.

            // Do something with the contact here (bigger example below)
        }
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;获取联系人数据&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;@Override
protected void onActivityResult(int requestCode, int resultCode, Intent data) {
    // Check which request it is that we&amp;apos;re responding to
    if (requestCode == PICK_CONTACT_REQUEST) {
        // Make sure the request was successful
        if (resultCode == RESULT_OK) {
            // Get the URI that points to the selected contact
            Uri contactUri = data.getData();
            // We only need the NUMBER column, because there will be only one row in the result
            String[] projection = {Phone.NUMBER};

            // Perform the query on the contact to get the NUMBER column
            // We don&amp;apos;t need a selection or sort order (there&amp;apos;s only one result for the given URI)
            // CAUTION: The query() method should be called from a separate thread to avoid blocking
            // your app&amp;apos;s UI thread. (For simplicity of the sample, this code doesn&amp;apos;t do that.)
            // Consider using CursorLoader to perform the query.
            Cursor cursor = getContentResolver()
                    .query(contactUri, projection, null, null, null);
            cursor.moveToFirst();

            // Retrieve the phone number from the NUMBER column
            int column = cursor.getColumnIndex(Phone.NUMBER);
            String number = cursor.getString(column);

            // Do something with the phone number...
        }
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;允许其他app启动你的activity&quot;&gt;&lt;a href=&quot;#允许其他app启动你的activity&quot; class=&quot;headerlink&quot; title=&quot;允许其他app启动你的activity&quot;&gt;&lt;/a&gt;允许其他app启动你的activity&lt;/h4&gt;&lt;h5 id=&quot;添加Intent过滤器&quot;&gt;&lt;a href=&quot;#添加Intent过滤器&quot; class=&quot;headerlink&quot; title=&quot;添加Intent过滤器&quot;&gt;&lt;/a&gt;添加Intent过滤器&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;&amp;lt;activity android:name=&amp;quot;ShareActivity&amp;quot;&amp;gt;
    &amp;lt;intent-filter&amp;gt;
        &amp;lt;action android:name=&amp;quot;android.intent.action.SEND&amp;quot;/&amp;gt;
        &amp;lt;category android:name=&amp;quot;android.intent.category.DEFAULT&amp;quot;/&amp;gt;
        &amp;lt;data android:mimeType=&amp;quot;text/plain&amp;quot;/&amp;gt;
        &amp;lt;data android:mimeType=&amp;quot;image/*&amp;quot;/&amp;gt;
    &amp;lt;/intent-filter&amp;gt;
&amp;lt;/activity&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;  or&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;activity android:name=&amp;quot;ShareActivity&amp;quot;&amp;gt;
    &amp;lt;!-- filter for sending text; accepts SENDTO action with sms URI schemes --&amp;gt;
    &amp;lt;intent-filter&amp;gt;
        &amp;lt;action android:name=&amp;quot;android.intent.action.SENDTO&amp;quot;/&amp;gt;
        &amp;lt;category android:name=&amp;quot;android.intent.category.DEFAULT&amp;quot;/&amp;gt;
        &amp;lt;data android:scheme=&amp;quot;sms&amp;quot; /&amp;gt;
        &amp;lt;data android:scheme=&amp;quot;smsto&amp;quot; /&amp;gt;
    &amp;lt;/intent-filter&amp;gt;
    &amp;lt;!-- filter for sending text or images; accepts SEND action and text or image data --&amp;gt;
    &amp;lt;intent-filter&amp;gt;
        &amp;lt;action android:name=&amp;quot;android.intent.action.SEND&amp;quot;/&amp;gt;
        &amp;lt;category android:name=&amp;quot;android.intent.category.DEFAULT&amp;quot;/&amp;gt;
        &amp;lt;data android:mimeType=&amp;quot;image/*&amp;quot;/&amp;gt;
        &amp;lt;data android:mimeType=&amp;quot;text/plain&amp;quot;/&amp;gt;
    &amp;lt;/intent-filter&amp;gt;
&amp;lt;/activity&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;在activity中处理Intent&quot;&gt;&lt;a href=&quot;#在activity中处理Intent&quot; class=&quot;headerlink&quot; title=&quot;在activity中处理Intent&quot;&gt;&lt;/a&gt;在activity中处理Intent&lt;/h5&gt;&lt;p&gt; getIntent()&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;@Override
protected void onCreate(Bundle savedInstanceState) {
    super.onCreate(savedInstanceState);

    setContentView(R.layout.main);

    // Get the intent that started this activity
    Intent intent = getIntent();
    Uri data = intent.getData();

    // Figure out what to do based on the intent type
    if (intent.getType().indexOf(&amp;quot;image/&amp;quot;) != -1) {
        // Handle intents with image data ...
    } else if (intent.getType().equals(&amp;quot;text/plain&amp;quot;)) {
        // Handle intents with text ...
    }
} 
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;返回结果&quot;&gt;&lt;a href=&quot;#返回结果&quot; class=&quot;headerlink&quot; title=&quot;返回结果&quot;&gt;&lt;/a&gt;返回结果&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;// Create intent to deliver some kind of result data
Intent result = new Intent(&amp;quot;com.example.RESULT_ACTION&amp;quot;, Uri.parse(&amp;quot;content://result_uri&amp;quot;));
setResult(Activity.RESULT_OK, result);
finish();
&lt;/code&gt;&lt;/pre&gt;</content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;如何打开其他app&quot;&gt;&lt;a href=&quot;#如何打开其他app&quot; class=&quot;headerlink&quot; title=&quot;如何打开其他app&quot;&gt;&lt;/a&gt;如何打开其他app&lt;/h4&gt;
    
    </summary>
    
      <category term="android" scheme="http://yoursite.com/categories/android/"/>
    
    
      <category term="android" scheme="http://yoursite.com/tags/android/"/>
    
  </entry>
  
  <entry>
    <title>快捷键整理</title>
    <link href="http://yoursite.com/2016/06/19/%E5%BF%AB%E6%8D%B7%E9%94%AE%E6%95%B4%E7%90%86/"/>
    <id>http://yoursite.com/2016/06/19/快捷键整理/</id>
    <published>2016-06-19T10:25:35.000Z</published>
    <updated>2016-12-10T09:45:53.151Z</updated>
    
    <content type="html">&lt;h4 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h4&gt;&lt;p&gt;记录各种工具下常用的快捷键，提升效率&lt;br&gt;&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;&lt;/p&gt;
&lt;h4 id=&quot;android-studio&quot;&gt;&lt;a href=&quot;#android-studio&quot; class=&quot;headerlink&quot; title=&quot;android studio&quot;&gt;&lt;/a&gt;android studio&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;generate = Alt + insert  
格式化代码 = Ctrl+Alt+L 
大小写切换 = Ctrl+Shift+U  
代码提示 = shift+ space(自己设置)  
设置方法 = File-&amp;gt;setting-&amp;gt;Keymap-&amp;gt;Main menu-&amp;gt;Code-&amp;gt;Completion  
导入包 = Alt + Enter
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;sublime-Text3&quot;&gt;&lt;a href=&quot;#sublime-Text3&quot; class=&quot;headerlink&quot; title=&quot;sublime Text3&quot;&gt;&lt;/a&gt;sublime Text3&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;Package Control = ctrl+Shift+P  
格式化 = Ctrl+Alt+F  
Jump to definition = Alt+Click  
Jump to definition = Control+Windows+Alt+Up  
Go back = Control+Windows+Alt+Left  
Manual Code Intelligence = Control+Shift+space  
Open Terminal at File = ctrl+shift+t   
Open Terminal at Project Folder  =   ctrl+alt+shift+t
替换  Ctrl+h
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;Chrome&quot;&gt;&lt;a href=&quot;#Chrome&quot; class=&quot;headerlink&quot; title=&quot;Chrome&quot;&gt;&lt;/a&gt;Chrome&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;vpn开关 = alt+shift+o（switchyomega插件）
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;eclipse&quot;&gt;&lt;a href=&quot;#eclipse&quot; class=&quot;headerlink&quot; title=&quot;eclipse&quot;&gt;&lt;/a&gt;eclipse&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;ctrl+shift+x   转为大写  
ctrl+shift+y   转为小写
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;win10&quot;&gt;&lt;a href=&quot;#win10&quot; class=&quot;headerlink&quot; title=&quot;win10&quot;&gt;&lt;/a&gt;win10&lt;/h4&gt;&lt;pre&gt;&lt;code&gt;shift+win+c   打开小娜
ctrl +win+d   打开虚拟桌面
ctrl +win+f4  关闭虚拟桌面
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;在 Explorer 的地址栏中执行 cmd 或 powershell 可以快速打开并跳转至该目录&lt;/p&gt;
</content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h4&gt;&lt;p&gt;记录各种工具下常用的快捷键，提升效率&lt;br&gt;
    
    </summary>
    
      <category term="常用" scheme="http://yoursite.com/categories/%E5%B8%B8%E7%94%A8/"/>
    
    
      <category term="常用" scheme="http://yoursite.com/tags/%E5%B8%B8%E7%94%A8/"/>
    
  </entry>
  
  <entry>
    <title>android学习记录</title>
    <link href="http://yoursite.com/2016/06/19/android%E5%85%A5%E9%97%A8/"/>
    <id>http://yoursite.com/2016/06/19/android入门/</id>
    <published>2016-06-19T10:09:27.000Z</published>
    <updated>2016-07-05T08:44:22.960Z</updated>
    
    <content type="html">&lt;h4 id=&quot;activity之间的跳转和数据传递&quot;&gt;&lt;a href=&quot;#activity之间的跳转和数据传递&quot; class=&quot;headerlink&quot; title=&quot;activity之间的跳转和数据传递&quot;&gt;&lt;/a&gt;activity之间的跳转和数据传递&lt;/h4&gt;&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;
&lt;p&gt;MainActivity-&amp;gt;Main2Activity&lt;br&gt;MainActivity代码:  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Intent intent = new Intent();
intent.putExtra(&amp;quot;name&amp;quot;,&amp;quot;哈哈哈&amp;quot;);
intent.setClass(MainActivity.this,Main2Activity.class);
MainActivity.this.startActivity(intent);  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Main2Activity代码:  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;tv1 = (TextView) findViewById(R.id.main2text);
String value = getIntent().getStringExtra(&amp;quot;name&amp;quot;);
tv1.setText(value);  
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;activity生命周期&quot;&gt;&lt;a href=&quot;#activity生命周期&quot; class=&quot;headerlink&quot; title=&quot;activity生命周期&quot;&gt;&lt;/a&gt;activity生命周期&lt;/h4&gt;&lt;p&gt;&lt;img src=&quot;/uploads/activityLifecycle.PNG&quot; alt=&quot;Activity Lifecycle&quot;&gt;  &lt;/p&gt;
&lt;h5 id=&quot;onCreate&quot;&gt;&lt;a href=&quot;#onCreate&quot; class=&quot;headerlink&quot; title=&quot;onCreate()&quot;&gt;&lt;/a&gt;onCreate()&lt;/h5&gt;&lt;p&gt;Called when the activity is first created&lt;br&gt;当activity第一次被创建的时候调用&lt;/p&gt;
&lt;h5 id=&quot;onStart&quot;&gt;&lt;a href=&quot;#onStart&quot; class=&quot;headerlink&quot; title=&quot;onStart()&quot;&gt;&lt;/a&gt;onStart()&lt;/h5&gt;&lt;p&gt;Called when the activity is becoming visible to the user&lt;br&gt;当activity对用户可见时调用&lt;/p&gt;
&lt;h4 id=&quot;onRestart&quot;&gt;&lt;a href=&quot;#onRestart&quot; class=&quot;headerlink&quot; title=&quot;onRestart()&quot;&gt;&lt;/a&gt;onRestart()&lt;/h4&gt;&lt;p&gt;Called after your activity has been stopped&lt;br&gt;当activity已经stoped过，恢复的时候调用&lt;/p&gt;
&lt;h5 id=&quot;onResume&quot;&gt;&lt;a href=&quot;#onResume&quot; class=&quot;headerlink&quot; title=&quot;onResume()&quot;&gt;&lt;/a&gt;onResume()&lt;/h5&gt;&lt;p&gt;Called when the activity will start interacting with the user&lt;br&gt;当用户能获取activity焦点时调用&lt;/p&gt;
&lt;h5 id=&quot;onPause&quot;&gt;&lt;a href=&quot;#onPause&quot; class=&quot;headerlink&quot; title=&quot;onPause()&quot;&gt;&lt;/a&gt;onPause()&lt;/h5&gt;&lt;p&gt;Called when the system is about to start resuming a previous activity&lt;br&gt;当系统恢复之前的activity时调用（失去焦点），通常用来在此方法里面持久化当前activity的数据等&lt;/p&gt;
&lt;h5 id=&quot;onStop&quot;&gt;&lt;a href=&quot;#onStop&quot; class=&quot;headerlink&quot; title=&quot;onStop()&quot;&gt;&lt;/a&gt;onStop()&lt;/h5&gt;&lt;p&gt;Called when the activity is no longer visible to the use&lt;br&gt;当activity完全不可见时调用&lt;/p&gt;
&lt;h5 id=&quot;onDestroy&quot;&gt;&lt;a href=&quot;#onDestroy&quot; class=&quot;headerlink&quot; title=&quot;onDestroy()&quot;&gt;&lt;/a&gt;onDestroy()&lt;/h5&gt;&lt;p&gt;The final call you receive before your activity is destroyed  &lt;/p&gt;
&lt;h4 id=&quot;将activity设置为窗口模式&quot;&gt;&lt;a href=&quot;#将activity设置为窗口模式&quot; class=&quot;headerlink&quot; title=&quot;将activity设置为窗口模式&quot;&gt;&lt;/a&gt;将activity设置为窗口模式&lt;/h4&gt;&lt;p&gt;在AndroidManifest.xml中设置theme&lt;/p&gt;
&lt;h4 id=&quot;设备兼容&quot;&gt;&lt;a href=&quot;#设备兼容&quot; class=&quot;headerlink&quot; title=&quot;设备兼容&quot;&gt;&lt;/a&gt;设备兼容&lt;/h4&gt;&lt;h5 id=&quot;语言兼容&quot;&gt;&lt;a href=&quot;#语言兼容&quot; class=&quot;headerlink&quot; title=&quot;语言兼容&quot;&gt;&lt;/a&gt;语言兼容&lt;/h5&gt;&lt;pre&gt;&lt;code&gt;MyProject/
    res/
       values/
           strings.xml
       values-es/
           strings.xml
       values-fr/
           strings.xml  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Android系统会基于当前系统的语言环境设置来选择对应的设置&lt;/p&gt;
&lt;h5 id=&quot;屏幕兼容&quot;&gt;&lt;a href=&quot;#屏幕兼容&quot; class=&quot;headerlink&quot; title=&quot;屏幕兼容&quot;&gt;&lt;/a&gt;屏幕兼容&lt;/h5&gt;&lt;p&gt;创建不同的布局  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;MyProject/
    res/
        layout/              # default (portrait)
            main.xml
        layout-land/         # landscape
            main.xml
        layout-large/        # large (portrait)
            main.xml
        layout-large-land/   # large landscape
            main.xml
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;创建不同的位图  &lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;xhdpi: 2.0&lt;/li&gt;
&lt;li&gt;hdpi: 1.5&lt;/li&gt;
&lt;li&gt;mdpi: 1.0 (baseline)&lt;/li&gt;
&lt;li&gt;ldpi: 0.75&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果xhdpi的图像为200*200，则hdpi为150*150，mdpi为100*100,ldpi为75*75  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;MyProject/
    res/
        drawable-xhdpi/
            awesomeimage.png
        drawable-hdpi/
            awesomeimage.png
        drawable-mdpi/
            awesomeimage.png
        drawable-ldpi/
            awesomeimage.png  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;当引用 @drawable/awesomeimage时，系统会根据屏幕的分辨率选择对应的图片&lt;/p&gt;
&lt;h5 id=&quot;版本兼容&quot;&gt;&lt;a href=&quot;#版本兼容&quot; class=&quot;headerlink&quot; title=&quot;版本兼容&quot;&gt;&lt;/a&gt;版本兼容&lt;/h5&gt;&lt;p&gt;指定最小和目标API级别  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;manifest xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot; ... &amp;gt;
    &amp;lt;uses-sdk android:minSdkVersion=&amp;quot;4&amp;quot; android:targetSdkVersion=&amp;quot;15&amp;quot; /&amp;gt;
    ...
&amp;lt;/manifest&amp;gt;  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;在运行时检查系统版本  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;private void setUpActionBar() {
    // Make sure we&amp;apos;re running on Honeycomb or higher to use ActionBar APIs
    if (Build.VERSION.SDK_INT &amp;gt;= Build.VERSION_CODES.HONEYCOMB) {
        ActionBar actionBar = getActionBar();
        actionBar.setDisplayHomeAsUpEnabled(true);
    }
}  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;使用平台的主题和样式&lt;br&gt;To make your activity look like a dialog box:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;activity android:theme=&amp;quot;@android:style/Theme.Dialog&amp;quot;&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;To make your activity have a transparent background:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;activity android:theme=&amp;quot;@android:style/Theme.Translucent&amp;quot;&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;To apply your own custom theme defined in /res/values/styles.xml:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;activity android:theme=&amp;quot;@style/CustomTheme&amp;quot;&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;To apply a theme to your entire app (all activities), add the android:theme attribute to the &lt;application&gt; element:&lt;/application&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;application android:theme=&amp;quot;@style/CustomTheme&amp;quot;&amp;gt;  
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&quot;Fragment的创建&quot;&gt;&lt;a href=&quot;#Fragment的创建&quot; class=&quot;headerlink&quot; title=&quot;Fragment的创建&quot;&gt;&lt;/a&gt;Fragment的创建&lt;/h4&gt;&lt;p&gt;1.Create a Fragment Class  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import android.os.Bundle;
import android.support.v4.app.Fragment;
import android.view.LayoutInflater;
import android.view.ViewGroup;

public class ArticleFragment extends Fragment {
    @Override
    public View onCreateView(LayoutInflater inflater, ViewGroup container,
        Bundle savedInstanceState) {
        // Inflate the layout for this fragment
        return inflater.inflate(R.layout.article_view, container, false);
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;2.Add a Fragment to an Activity using XML  &lt;/p&gt;
&lt;p&gt;res/layout-large/news_articles.xml  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;LinearLayout xmlns:android=&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;
    android:orientation=&amp;quot;horizontal&amp;quot;
    android:layout_width=&amp;quot;fill_parent&amp;quot;
    android:layout_height=&amp;quot;fill_parent&amp;quot;&amp;gt;

    &amp;lt;fragment android:name=&amp;quot;com.example.android.fragments.HeadlinesFragment&amp;quot;
              android:id=&amp;quot;@+id/headlines_fragment&amp;quot;
              android:layout_weight=&amp;quot;1&amp;quot;
              android:layout_width=&amp;quot;0dp&amp;quot;
              android:layout_height=&amp;quot;match_parent&amp;quot; /&amp;gt;

    &amp;lt;fragment android:name=&amp;quot;com.example.android.fragments.ArticleFragment&amp;quot;
              android:id=&amp;quot;@+id/article_fragment&amp;quot;
              android:layout_weight=&amp;quot;2&amp;quot;
              android:layout_width=&amp;quot;0dp&amp;quot;
              android:layout_height=&amp;quot;match_parent&amp;quot; /&amp;gt;

&amp;lt;/LinearLayout&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;3.Then apply the layout to your activity  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import android.os.Bundle;
import android.support.v4.app.FragmentActivity;

public class MainActivity extends FragmentActivity {
    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.news_articles);
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;更多内容参考&lt;a href=&quot;http://blog.csdn.net/guolin_blog/article/details/8881711&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;Android Fragment完全解析，关于碎片你所需知道的一切&lt;/a&gt;&lt;/p&gt;
&lt;h4 id=&quot;保存数据&quot;&gt;&lt;a href=&quot;#保存数据&quot; class=&quot;headerlink&quot; title=&quot;保存数据&quot;&gt;&lt;/a&gt;保存数据&lt;/h4&gt;&lt;h5 id=&quot;保存键值对&quot;&gt;&lt;a href=&quot;#保存键值对&quot; class=&quot;headerlink&quot; title=&quot;保存键值对&quot;&gt;&lt;/a&gt;保存键值对&lt;/h5&gt;&lt;p&gt;获取SharedPreferences     &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;1.通过R.string.preference_file_key获取对应的SharedPreferences
    Context context = getActivity();
    SharedPreferences sharedPref = context.getSharedPreferences(
        getString(R.string.preference_file_key), Context.MODE_PRIVATE);  
2.获取Activity的公共的SharedPreferences
    SharedPreferences sharedPref = getActivity().getPreferences(Context.MODE_PRIVATE);  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;写入SharedPreferences &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;SharedPreferences sharedPref = getActivity().getPreferences(Context.MODE_PRIVATE);
SharedPreferences.Editor editor = sharedPref.edit();
editor.putInt(getString(R.string.saved_high_score), newHighScore);
editor.commit();
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;读取SharedPreferences &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;SharedPreferences sharedPref = getActivity().getPreferences(Context.MODE_PRIVATE);
int defaultValue = getResources().getInteger(R.string.saved_high_score_default);
long highScore = sharedPref.getInt(getString(R.string.saved_high_score), defaultValue);
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;保存文件&quot;&gt;&lt;a href=&quot;#保存文件&quot; class=&quot;headerlink&quot; title=&quot;保存文件&quot;&gt;&lt;/a&gt;保存文件&lt;/h5&gt;&lt;h6 id=&quot;内部存储&quot;&gt;&lt;a href=&quot;#内部存储&quot; class=&quot;headerlink&quot; title=&quot;内部存储&quot;&gt;&lt;/a&gt;内部存储&lt;/h6&gt;&lt;ul&gt;
&lt;li&gt;它始终可用  &lt;/li&gt;
&lt;li&gt;默认情况下只有您的应用可以访问此处保存的文件&lt;/li&gt;
&lt;li&gt;当用户卸载您的应用时，系统会从内部存储中删除您的应用的所有文件。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;获取应用的内部目录的 File  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;getFilesDir()
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;获取应用临时缓存文件的内部目录的 File  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;getCacheDir()
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;用法：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;File file = new File(context.getFilesDir(), filename);
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;或者 调用 openFileOutput() 获取写入到内部目录中的文件的 FileOutputStream&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;String filename = &amp;quot;myfile&amp;quot;;
String string = &amp;quot;Hello world!&amp;quot;;
FileOutputStream outputStream;

try {
  outputStream = openFileOutput(filename, Context.MODE_PRIVATE);
  outputStream.write(string.getBytes());
  outputStream.close();
} catch (Exception e) {
  e.printStackTrace();
}  
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;或者 缓存某些文件，您应改用 createTempFile()&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public File getTempFile(Context context, String url) {
    File file;
    try {
        String fileName = Uri.parse(url).getLastPathSegment();
        file = File.createTempFile(fileName, null, context.getCacheDir());
    catch (IOException e) {
        // Error while creating file
    }
    return file;
}
&lt;/code&gt;&lt;/pre&gt;&lt;h6 id=&quot;外部存储&quot;&gt;&lt;a href=&quot;#外部存储&quot; class=&quot;headerlink&quot; title=&quot;外部存储&quot;&gt;&lt;/a&gt;外部存储&lt;/h6&gt;&lt;ul&gt;
&lt;li&gt;它并非始终可用，因为用户可采用 USB 存储的形式装载外部存储，并在某些情况下会从设备中将其删除&lt;/li&gt;
&lt;li&gt;它是全局可读的，因此此处保存的文件可能不受您控制地被读取&lt;/li&gt;
&lt;li&gt;当用户卸载您的应用时，只有在您通过 getExternalFilesDir() 将您的应用的文件保存在目录中时，系统才会从此处删除您的应用的文件&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;1.获取权限  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;manifest ...&amp;gt;  
    &amp;lt;uses-permission android:name=&amp;quot;android.permission.WRITE_EXTERNAL_STORAGE&amp;quot; /&amp;gt;
     &amp;lt;uses-permission android:name=&amp;quot;android.permission.READ_EXTERNAL_STORAGE&amp;quot; /&amp;gt;
    ...
&amp;lt;/manifest&amp;gt;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;通过调用 getExternalStorageState() 查询外部存储状态。 如果返回的状态为 MEDIA_MOUNTED，那么您可以对您的文件进行读写    &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;/* Checks if external storage is available for read and write */
public boolean isExternalStorageWritable() {
    String state = Environment.getExternalStorageState();
    if (Environment.MEDIA_MOUNTED.equals(state)) {
        return true;
    }
    return false;
}

/* Checks if external storage is available to at least read */
public boolean isExternalStorageReadable() {
    String state = Environment.getExternalStorageState();
    if (Environment.MEDIA_MOUNTED.equals(state) ||
        Environment.MEDIA_MOUNTED_READ_ONLY.equals(state)) {
        return true;
    }
    return false;
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;如果要使用外部存储上的公共文件，使用 getExternalStoragePublicDirectory() 方法获取表示外部存储上相应目录的 File 。该方法使用指定 您想要保存以便它们可以与其他公共文件在逻辑上组织在一起的文件类型的参数，比如 DIRECTORY_MUSIC 或 DIRECTORY_PICTURES  &lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public File getAlbumStorageDir(String albumName) {
    // Get the directory for the user&amp;apos;s public pictures directory. 
    File file = new File(Environment.getExternalStoragePublicDirectory(
            Environment.DIRECTORY_PICTURES), albumName);
    if (!file.mkdirs()) {
        Log.e(LOG_TAG, &amp;quot;Directory not created&amp;quot;);
    }
    return file;
} 
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;通过调用 getExternalFilesDir() 获取相应的目录并向其传递指示您想要的目录类型的名称。当用户卸载应用时，系统会删除这些文件&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public File getAlbumStorageDir(Context context, String albumName) {
    // Get the directory for the app&amp;apos;s private pictures directory. 
    File file = new File(context.getExternalFilesDir(
            Environment.DIRECTORY_PICTURES), albumName);
    if (!file.mkdirs()) {
        Log.e(LOG_TAG, &amp;quot;Directory not created&amp;quot;);
    }
    return file;
}
&lt;/code&gt;&lt;/pre&gt;&lt;h6 id=&quot;删除文件&quot;&gt;&lt;a href=&quot;#删除文件&quot; class=&quot;headerlink&quot; title=&quot;删除文件&quot;&gt;&lt;/a&gt;删除文件&lt;/h6&gt;&lt;pre&gt;&lt;code&gt;myFile.delete();
or
myContext.deleteFile(fileName);
&lt;/code&gt;&lt;/pre&gt;&lt;h5 id=&quot;使用-SQLite-保存数据&quot;&gt;&lt;a href=&quot;#使用-SQLite-保存数据&quot; class=&quot;headerlink&quot; title=&quot;使用 SQLite 保存数据&quot;&gt;&lt;/a&gt;使用 SQLite 保存数据&lt;/h5&gt;&lt;h6 id=&quot;Define-a-Schema-and-Contract&quot;&gt;&lt;a href=&quot;#Define-a-Schema-and-Contract&quot; class=&quot;headerlink&quot; title=&quot;Define a Schema and Contract&quot;&gt;&lt;/a&gt;Define a Schema and Contract&lt;/h6&gt;&lt;p&gt;通过实现 BaseColumns接口，能自动生成_ID 字段&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public final class FeedReaderContract {
    // To prevent someone from accidentally instantiating the contract class,
    // give it an empty constructor.
    public FeedReaderContract() {}

    /* Inner class that defines the table contents */
    public static abstract class FeedEntry implements BaseColumns {
        public static final String TABLE_NAME = &amp;quot;entry&amp;quot;;
        public static final String COLUMN_NAME_ENTRY_ID = &amp;quot;entryid&amp;quot;;
        public static final String COLUMN_NAME_TITLE = &amp;quot;title&amp;quot;;
        public static final String COLUMN_NAME_SUBTITLE = &amp;quot;subtitle&amp;quot;;
        ...
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;h6 id=&quot;Create-a-Database-Using-a-SQL-Helper&quot;&gt;&lt;a href=&quot;#Create-a-Database-Using-a-SQL-Helper&quot; class=&quot;headerlink&quot; title=&quot;Create a Database Using a SQL Helper&quot;&gt;&lt;/a&gt;Create a Database Using a SQL Helper&lt;/h6&gt;&lt;pre&gt;&lt;code&gt;private static final String TEXT_TYPE = &amp;quot; TEXT&amp;quot;;
private static final String COMMA_SEP = &amp;quot;,&amp;quot;;
private static final String SQL_CREATE_ENTRIES =
    &amp;quot;CREATE TABLE &amp;quot; + FeedEntry.TABLE_NAME + &amp;quot; (&amp;quot; +
    FeedEntry._ID + &amp;quot; INTEGER PRIMARY KEY,&amp;quot; +
    FeedEntry.COLUMN_NAME_ENTRY_ID + TEXT_TYPE + COMMA_SEP +
    FeedEntry.COLUMN_NAME_TITLE + TEXT_TYPE + COMMA_SEP +
    ... // Any other options for the CREATE command
    &amp;quot; )&amp;quot;;

private static final String SQL_DELETE_ENTRIES =
    &amp;quot;DROP TABLE IF EXISTS &amp;quot; + FeedEntry.TABLE_NAME; 
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Note: Because they can be long-running, be sure that you call getWritableDatabase() or getReadableDatabase() in a background thread, such as with AsyncTask or IntentService.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;public class FeedReaderDbHelper extends SQLiteOpenHelper {
    // If you change the database schema, you must increment the database version.
    public static final int DATABASE_VERSION = 1;
    public static final String DATABASE_NAME = &amp;quot;FeedReader.db&amp;quot;;

    public FeedReaderDbHelper(Context context) {
        super(context, DATABASE_NAME, null, DATABASE_VERSION);
    }
    public void onCreate(SQLiteDatabase db) {
        db.execSQL(SQL_CREATE_ENTRIES);
    }
    public void onUpgrade(SQLiteDatabase db, int oldVersion, int newVersion) {
        // This database is only a cache for online data, so its upgrade policy is
        // to simply to discard the data and start over
        db.execSQL(SQL_DELETE_ENTRIES);
        onCreate(db);
    }
    public void onDowngrade(SQLiteDatabase db, int oldVersion, int newVersion) {
        onUpgrade(db, oldVersion, newVersion);
    }
}
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;然后&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;FeedReaderDbHelper mDbHelper = new FeedReaderDbHelper(getContext())
&lt;/code&gt;&lt;/pre&gt;&lt;h6 id=&quot;Put-Information-into-a-Database&quot;&gt;&lt;a href=&quot;#Put-Information-into-a-Database&quot; class=&quot;headerlink&quot; title=&quot;Put Information into a Database&quot;&gt;&lt;/a&gt;Put Information into a Database&lt;/h6&gt;&lt;pre&gt;&lt;code&gt;// Gets the data repository in write mode
SQLiteDatabase db = mDbHelper.getWritableDatabase();

// Create a new map of values, where column names are the keys
ContentValues values = new ContentValues();
values.put(FeedEntry.COLUMN_NAME_ENTRY_ID, id);
values.put(FeedEntry.COLUMN_NAME_TITLE, title);
values.put(FeedEntry.COLUMN_NAME_CONTENT, content);

// Insert the new row, returning the primary key value of the new row
long newRowId;
newRowId = db.insert(
         FeedEntry.TABLE_NAME,
         FeedEntry.COLUMN_NAME_NULLABLE,
         values);
&lt;/code&gt;&lt;/pre&gt;&lt;h6 id=&quot;Read-Information-from-a-Database&quot;&gt;&lt;a href=&quot;#Read-Information-from-a-Database&quot; class=&quot;headerlink&quot; title=&quot;Read Information from a Database&quot;&gt;&lt;/a&gt;Read Information from a Database&lt;/h6&gt;&lt;pre&gt;&lt;code&gt;SQLiteDatabase db = mDbHelper.getReadableDatabase();

// Define a projection that specifies which columns from the database
// you will actually use after this query.
String[] projection = {
    FeedEntry._ID,
    FeedEntry.COLUMN_NAME_TITLE,
    FeedEntry.COLUMN_NAME_UPDATED,
    ...
    };

// How you want the results sorted in the resulting Cursor
String sortOrder =
    FeedEntry.COLUMN_NAME_UPDATED + &amp;quot; DESC&amp;quot;;

Cursor c = db.query(
    FeedEntry.TABLE_NAME,  // The table to query
    projection,                               // The columns to return
    selection,                                // The columns for the WHERE clause
    selectionArgs,                            // The values for the WHERE clause
    null,                                     // don&amp;apos;t group the rows
    null,                                     // don&amp;apos;t filter by row groups
    sortOrder                                 // The sort order
    );
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;then&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;cursor.moveToFirst();
long itemId = cursor.getLong(
    cursor.getColumnIndexOrThrow(FeedEntry._ID)
)
&lt;/code&gt;&lt;/pre&gt;&lt;h6 id=&quot;Delete-Information-from-a-Database&quot;&gt;&lt;a href=&quot;#Delete-Information-from-a-Database&quot; class=&quot;headerlink&quot; title=&quot;Delete Information from a Database&quot;&gt;&lt;/a&gt;Delete Information from a Database&lt;/h6&gt;&lt;pre&gt;&lt;code&gt;// Define &amp;apos;where&amp;apos; part of query.
String selection = FeedEntry.COLUMN_NAME_ENTRY_ID + &amp;quot; LIKE ?&amp;quot;;
// Specify arguments in placeholder order.
String[] selectionArgs = { String.valueOf(rowId) };
// Issue SQL statement.
db.delete(table_name, selection, selectionArgs)
&lt;/code&gt;&lt;/pre&gt;&lt;h6 id=&quot;Update-a-Database&quot;&gt;&lt;a href=&quot;#Update-a-Database&quot; class=&quot;headerlink&quot; title=&quot;Update a Database&quot;&gt;&lt;/a&gt;Update a Database&lt;/h6&gt;&lt;pre&gt;&lt;code&gt;SQLiteDatabase db = mDbHelper.getReadableDatabase();

// New value for one column
ContentValues values = new ContentValues();
values.put(FeedEntry.COLUMN_NAME_TITLE, title);

// Which row to update, based on the ID
String selection = FeedEntry.COLUMN_NAME_ENTRY_ID + &amp;quot; LIKE ?&amp;quot;;
String[] selectionArgs = { String.valueOf(rowId) };

int count = db.update(
    FeedReaderDbHelper.FeedEntry.TABLE_NAME,
    values,
    selection,
    selectionArgs);
&lt;/code&gt;&lt;/pre&gt;</content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;activity之间的跳转和数据传递&quot;&gt;&lt;a href=&quot;#activity之间的跳转和数据传递&quot; class=&quot;headerlink&quot; title=&quot;activity之间的跳转和数据传递&quot;&gt;&lt;/a&gt;activity之间的跳转和数据传递&lt;/h4&gt;
    
    </summary>
    
      <category term="android" scheme="http://yoursite.com/categories/android/"/>
    
    
      <category term="android" scheme="http://yoursite.com/tags/android/"/>
    
  </entry>
  
  <entry>
    <title>Async</title>
    <link href="http://yoursite.com/2016/06/13/Async/"/>
    <id>http://yoursite.com/2016/06/13/Async/</id>
    <published>2016-06-13T05:15:23.000Z</published>
    <updated>2016-06-20T03:58:33.091Z</updated>
    
    <content type="html"></content>
    
    <summary type="html">
    
    </summary>
    
    
      <category term="nodejs" scheme="http://yoursite.com/tags/nodejs/"/>
    
  </entry>
  
</feed>
